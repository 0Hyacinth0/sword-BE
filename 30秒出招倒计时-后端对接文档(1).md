# 30 秒出招倒计时 - 后端对接文档

## 概述

战斗行动选择阶段增加 30 秒倒计时，超时后前端自动执行普攻。此功能为纯前端实现，后端无需提供额外 API。

---

## 机制说明

| 项目 | 说明 |
|:---|:---|
| 超时时间 | 30 秒（固定） |
| 超时行动 | 自动普攻（随机选择存活敌人） |
| 触发条件 | 轮到玩家行动时启动计时器 |
| 重置条件 | 玩家主动操作 / 回合结束 / 战斗结束 |
| 适用范围 | 所有战斗类型（PVE/PVP/副本/Boss） |

---

## 前端实现

- 计时器在 `BattleConsole.vue` 中以 `setInterval(1000)` 实现
- 监听 `waitingForPlayer` 状态控制计时器启停
- 超时后构造 `{ type: 'attack', actorUid, targetUid }` 行动并调用 `submitAction`
- 组件卸载时自动清理计时器

---

## 后端对接

**无需后端配合**。超时自动行动与玩家主动操作使用相同的 `submitAction` 接口，后端无法区分超时行动和玩家操作。
