# 多人副本战斗 - 后端对接文档

## 概述

本文档描述多人副本战斗（怪物血量按人数缩放、掉落分配）功能的前端实现与后端 API 对接规范。

---

## 1. 怪物属性缩放规则

多人副本中，怪物属性根据队伍人数进行动态缩放：

| 属性 | 缩放公式 | 说明 |
|:---|:---|:---|
| HP | `base × (1 + 0.5 × (n-1))` | n=队伍人数 |
| 物理攻击 | `base × (1 + 0.3 × (n-1))` | — |
| 魔法攻击 | `base × (1 + 0.3 × (n-1))` | — |
| 防御 | `base × (1 + 0.2 × (n-1))` | — |

> 前端当前为本地计算缩放，后端上线后应改为服务端计算并下发缩放后的怪物属性。

---

## 2. 掉落分配规则

### 2.1 独立掉落

每位队员**独立计算**掉落物品，互不影响。前端对每位 `RoomMember` 单独调用掉落计算函数。

### 2.2 掉落率倍率

| 副本类型 | 掉落率倍率 |
|:---|:---|
| 普通 | ×1.0 |
| 精英 | ×1.5 |

### 2.3 掉落物品结构

```typescript
interface MemberDropDistribution {
  characterId: string       // 角色 ID
  characterName: string     // 角色名
  drops: BattleRewardItem[] // 该成员获得的掉落物品
}

interface BattleRewardItem {
  itemId: number
  name: string
  quantity: number
  quality: 'common' | 'rare' | 'epic' | 'legendary'
}
```

---

## 3. 需要后端提供的 API

### 3.1 开始多人副本战斗

```
POST /dungeon/multi-battle/start
```

**请求参数：**

```json
{
  "roomId": "room-001",
  "dungeonId": "dungeon-shadow-cave",
  "floor": 1,
  "members": [
    {
      "characterId": "char-001",
      "characterName": "勇者",
      "profession": "Warrior",
      "level": 15
    },
    {
      "characterId": "char-002",
      "characterName": "小明",
      "profession": "Mage",
      "level": 14
    }
  ]
}
```

**响应：**

```json
{
  "code": 200,
  "message": "战斗开始",
  "data": {
    "battleId": "battle-multi-001",
    "enemies": [
      {
        "uid": "enemy-001",
        "name": "暗影狼",
        "stats": {
          "maxHp": 360,
          "hp": 360,
          "maxMp": 60,
          "mp": 60,
          "physicalAttack": 30,
          "magicAttack": 12,
          "defense": 12,
          "speed": 25,
          "dodgeRate": 0.08,
          "criticalRate": 0.05
        },
        "skills": [...]
      }
    ],
    "scaledBy": 2
  }
}
```

> 后端应返回**已缩放**的怪物属性，前端不再本地缩放。

---

### 3.2 楼层结算（多人掉落分配）

```
POST /dungeon/multi-battle/floor-complete
```

**请求参数：**

```json
{
  "battleId": "battle-multi-001",
  "dungeonId": "dungeon-shadow-cave",
  "floor": 1,
  "isBossFloor": false,
  "members": [
    { "characterId": "char-001", "characterName": "勇者" },
    { "characterId": "char-002", "characterName": "小明" }
  ]
}
```

**响应：**

```json
{
  "code": 200,
  "message": "楼层结算成功",
  "data": {
    "exp": 120,
    "gold": 50,
    "memberDrops": [
      {
        "characterId": "char-001",
        "characterName": "勇者",
        "drops": [
          { "itemId": 2001, "name": "暗影之牙", "quantity": 1, "quality": "rare" }
        ]
      },
      {
        "characterId": "char-002",
        "characterName": "小明",
        "drops": []
      }
    ]
  }
}
```

---

### 3.3 副本通关结算

```
POST /dungeon/multi-battle/complete
```

**请求参数：**

```json
{
  "battleId": "battle-multi-001",
  "dungeonId": "dungeon-shadow-cave",
  "totalFloors": 3,
  "floorHistory": [
    { "floor": 1, "outcome": "victory" },
    { "floor": 2, "outcome": "victory" },
    { "floor": 3, "outcome": "victory" }
  ],
  "members": [
    { "characterId": "char-001", "characterName": "勇者" },
    { "characterId": "char-002", "characterName": "小明" }
  ]
}
```

**响应：**

```json
{
  "code": 200,
  "message": "副本通关",
  "data": {
    "bonusExp": 200,
    "bonusGold": 100,
    "guaranteedItems": [
      { "itemId": 3001, "name": "暗影宝箱", "quantity": 1, "rarity": "epic" }
    ],
    "memberTotalDrops": [
      {
        "characterId": "char-001",
        "characterName": "勇者",
        "totalExp": 560,
        "totalGold": 250,
        "items": [...]
      },
      {
        "characterId": "char-002",
        "characterName": "小明",
        "totalExp": 560,
        "totalGold": 250,
        "items": [...]
      }
    ]
  }
}
```

---

## 4. AI 队友战斗单位

多人副本中，其他队员由 AI 控制自动行动。AI 队友属性根据职业和等级生成：

| 职业 | HP 倍率 | 物攻倍率 | 魔攻倍率 | 防御倍率 | 速度倍率 |
|:---|:---|:---|:---|:---|:---|
| Warrior | ×1.2 | ×1.1 | ×0.6 | ×1.2 | ×0.8 |
| Mage | ×0.8 | ×0.6 | ×1.3 | ×0.7 | ×1.0 |
| Hunter | ×0.9 | ×1.0 | ×0.8 | ×0.9 | ×1.3 |

> 后端上线后，应改为使用玩家实际属性（从数据库读取装备/培养数据），而非前端公式推算。

---

## 5. 前端当前 Mock 行为

| 功能 | Mock 实现 |
|:---|:---|
| 怪物缩放 | 前端 `applyMultiPlayerScaling()` 本地计算 |
| AI 队友 | 前端 `createAllyCombatantFromRoomMember()` 按公式生成 |
| 掉落分配 | 前端 `calculateDungeonDrops()` 为每位成员独立计算 |
| 楼层结算 | 前端 `handleFloorComplete()` 本地处理 |

后端对接后，以上逻辑应逐步迁移到服务端，前端仅负责展示。

---

## 6. 数据流

```
队长点击"开始挑战"
  → roomStore.startChallenge() (API: POST /dungeon-room/start)
  → dungeonStore.enterDungeon(dungeonId)
  → dungeonStore.startMultiPlayerFloorBattle(roomMembers)
    → 生成敌人 + 多人缩放 + 精英缩放
    → 创建玩家 + AI 队友战斗单位
    → battleStore.startWildBattleWithAllies(player, allies, enemies)
  → 跳转 BattleView
  → 战斗结束 → dungeonStore.handleFloorComplete()
    → 计算每位成员独立掉落 (memberDrops)
    → 显示楼层结算弹窗
```
