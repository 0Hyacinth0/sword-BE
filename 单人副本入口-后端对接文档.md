# 单人副本入口（选副本 → 连续战斗 → 结算） - 后端对接文档

## 概述

玩家在世界地图区域卡片上点击"副本"按钮后，进入该区域的副本选择界面。每个区域有普通和精英两种副本，每个副本包含 3-5 层连续战斗。玩家需要逐层击败敌人，最终通关获得奖励。中途可选择撤退带走已累积的奖励。当前版本为纯前端战斗，后续可扩展为服务器权威模式。

---

## 副本流程

```
玩家点击区域卡片"副本"按钮
  → 前端显示副本选择面板（DungeonPanel）
  → 玩家选择副本，前端验证等级要求
  → 前端初始化副本运行状态（dungeonStore.enterDungeon）
  → 前端生成第一层敌人并调用 battleStore.startWildBattle
  → 跳转到 BattleView 进行回合制战斗
  → 战斗胜利：
      → 显示楼层结算面板（DungeonFloorResultOverlay）
      → 玩家选择"继续" → 生成下一层敌人 → 继续战斗
      → 玩家选择"撤退" → 结算累积奖励 → 返回主页
  → 战斗失败：
      → 显示战败面板
      → 玩家点击"领取奖励并退出" → 结算累积奖励 → 返回主页
  → 通关最后一层：
      → 显示通关面板（含通关奖励）
      → 玩家点击"领取奖励" → 结算累积奖励 + 通关奖励 → 返回主页
```

---

## 核心数据结构

### 副本配置（前端硬编码）

```typescript
interface DungeonConfig {
  id: string                    // 副本 ID，如 "mist_forest_normal"
  name: string                  // 副本名称，如 "迷雾密林"
  areaId: string                // 所属区域 ID
  difficulty: 'normal' | 'elite' // 难度
  staminaCost: number           // 体力消耗
  levelRequirement: number      // 等级要求
  totalFloors: number           // 总层数
  floors: DungeonFloor[]        // 楼层配置
  rewards: DungeonRewardConfig  // 通关奖励
}

interface DungeonFloor {
  floorNumber: number           // 楼层编号（1-based）
  enemies: AreaMonster[]        // 该层敌人列表（1-3 个）
  isBossFloor: boolean          // 是否为 Boss 层
}

interface DungeonRewardConfig {
  guaranteedItems: {            // 通关保底物品
    itemId: number
    name: string
    rarity: string
    quantity: number
  }[]
  bonusExp: number              // 通关经验奖励
  bonusGold: number             // 通关金币奖励
}
```

### 副本运行状态（前端内存）

```typescript
interface DungeonRunState {
  dungeonId: string             // 当前副本 ID
  currentFloor: number          // 当前楼层
  totalFloors: number           // 总层数
  status: DungeonRunStatus      // 运行状态
  accumulatedRewards: BattleRewards  // 累积奖励
  floorHistory: DungeonFloorResult[] // 楼层历史
  playerHpPercent: number       // 玩家 HP 百分比（楼层间延续）
  playerMpPercent: number       // 玩家 MP 百分比（楼层间延续）
}

type DungeonRunStatus =
  | 'active'           // 进行中
  | 'floor_complete'   // 楼层完成（等待继续/撤退）
  | 'floor_defeat'     // 楼层失败
  | 'complete'         // 通关
  | 'retreated'        // 中途撤退

interface DungeonFloorResult {
  floorNumber: number
  outcome: 'victory' | 'defeat'
  rewards: BattleRewards | null
}
```

---

## 楼层间 HP/MP 延续规则

玩家在楼层间的 HP/MP 不会完全恢复，而是按以下规则延续：

| 属性 | 恢复规则 |
|:---|:---|
| 玩家 HP | 战后 HP 百分比 → 下层恢复至 maxHp 的 30%（若战后低于 30%） |
| 玩家 MP | 战后 MP 百分比 → 下层恢复至 maxMp 的 50%（若战后低于 50%） |
| 战宠 HP | 每层满恢复 |
| 战宠 MP | 每层满恢复 |

**实现逻辑**：

```typescript
// 记录战后 HP/MP 百分比
playerHpPercent = max(30, round((hp / maxHp) * 100))
playerMpPercent = round((mp / maxMp) * 100)

// 下层开始时修补
player.stats.hp = max(1, floor(maxHp * playerHpPercent / 100))
player.stats.mp = floor(maxMp * playerMpPercent / 100)
```

---

## 副本配置列表

当前共 8 个副本（4 区域 × 2 难度）：

| 副本 ID | 名称 | 区域 | 难度 | 层数 | 等级要求 | 体力消耗 |
|:---|:---|:---|:---:|:---:|:---:|:---:|
| mist_forest_normal | 迷雾密林 | 迷雾森林 | 普通 | 3 | 1 | 10 |
| mist_forest_elite | 暗影树海 | 迷雾森林 | 精英 | 4 | 8 | 20 |
| bone_wastes_normal | 白骨荒野 | 白骨荒野 | 普通 | 3 | 5 | 10 |
| bone_wastes_elite | 亡者峡谷 | 白骨荒野 | 精英 | 4 | 12 | 20 |
| flame_ridge_normal | 烈焰山脊 | 烈焰山脊 | 普通 | 3 | 10 | 10 |
| flame_ridge_elite | 熔岩深渊 | 烈焰山脊 | 精英 | 4 | 18 | 20 |
| frost_peak_normal | 冰霜之巅 | 冰霜之巅 | 普通 | 3 | 15 | 10 |
| frost_peak_elite | 极寒冰狱 | 冰霜之巅 | 精英 | 5 | 25 | 20 |

---

## 奖励计算规则

### 楼层奖励

每层战斗胜利后，根据击败的敌人计算奖励（与野外刷怪相同）：

```typescript
// 经验值
baseExp = sum(enemy.level × 5 + 10)
totalExp = floor(baseExp × difficultyMultiplier)  // elite: 1.5x

// 金币
baseGold = sum(enemy.level × 2 + 5)
totalGold = floor(baseGold × difficultyMultiplier)  // elite: 1.5x

// 掉落物品
// 根据敌人类型和稀有度随机掉落
```

### 通关奖励

通关最后一层后，额外获得副本配置中的通关奖励：

```typescript
totalExp = accumulatedExp + config.rewards.bonusExp
totalGold = accumulatedGold + config.rewards.bonusGold
totalItems = [...accumulatedItems, ...config.rewards.guaranteedItems]
```

### 撤退奖励

中途撤退时，保留已累积的楼层奖励，但不获得通关奖励。

### 失败奖励

战斗失败时，保留已累积的楼层奖励，但不获得当前层奖励和通关奖励。

---

## 前端实现（纯前端战斗）

当前版本的副本战斗完全在前端执行，不调用后端 API：

1. 副本配置由前端硬编码（`src/config/dungeon_config.ts`）
2. 楼层敌人由前端根据配置生成（复用 `createWildMonsterCombatant`）
3. 战斗引擎在前端本地运行（`battleEngine.ts`）
4. 奖励计算在前端本地执行
5. 无需后端创建/管理副本会话

---

## 后端对接方案（未来扩展）

### 模式一：后端验证模式

前端仍本地计算战斗过程，但结算时后端验证：

**POST** `/dungeon/enter`

进入副本，后端验证等级和体力。

**请求参数**：

```json
{
  "characterId": "uuid-string",
  "dungeonId": "mist_forest_normal"
}
```

**响应**：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "runId": "dungeon-run-uuid",
    "staminaConsumed": 10
  }
}
```

**POST** `/dungeon/floor/complete`

楼层完成，后端记录进度并累积奖励。

**请求参数**：

```json
{
  "runId": "dungeon-run-uuid",
  "floorNumber": 1,
  "outcome": "victory",
  "rewards": {
    "exp": 25,
    "gold": 15,
    "items": [...]
  },
  "playerHpPercent": 65,
  "playerMpPercent": 40
}
```

**POST** `/dungeon/retreat`

中途撤退，后端发放累积奖励。

**POST** `/dungeon/complete`

通关完成，后端发放累积奖励 + 通关奖励。

---

### 模式二：服务器权威模式

副本全过程由后端驱动：

**POST** `/dungeon/start`

进入副本并开始第一层战斗，后端返回战斗状态。

**POST** `/dungeon/action`

提交玩家行动，后端推进战斗并返回结果。

**POST** `/dungeon/next-floor`

继续下一层，后端生成敌人并返回战斗状态。

---

## 后端配置表（建议）

当需要后端管理副本配置时，建议新增以下配置表：

```sql
-- 副本配置表
CREATE TABLE dungeon_configs (
  id VARCHAR(32) PRIMARY KEY,
  name VARCHAR(50) NOT NULL,
  area_id VARCHAR(32) NOT NULL,
  difficulty ENUM('normal', 'elite') NOT NULL,
  stamina_cost INTEGER NOT NULL DEFAULT 10,
  level_requirement INTEGER NOT NULL DEFAULT 1,
  total_floors INTEGER NOT NULL,
  bonus_exp INTEGER NOT NULL DEFAULT 0,
  bonus_gold INTEGER NOT NULL DEFAULT 0,
  guaranteed_items JSON
);

-- 副本楼层配置表
CREATE TABLE dungeon_floors (
  id SERIAL PRIMARY KEY,
  dungeon_id VARCHAR(32) NOT NULL,
  floor_number INTEGER NOT NULL,
  enemies JSON NOT NULL,          -- [{ monsterId, level, type }]
  is_boss_floor BOOLEAN DEFAULT FALSE,
  FOREIGN KEY (dungeon_id) REFERENCES dungeon_configs(id)
);

-- 副本运行记录表
CREATE TABLE dungeon_runs (
  id UUID PRIMARY KEY,
  character_id UUID NOT NULL,
  dungeon_id VARCHAR(32) NOT NULL,
  current_floor INTEGER NOT NULL DEFAULT 1,
  status VARCHAR(20) NOT NULL,
  accumulated_exp INTEGER DEFAULT 0,
  accumulated_gold INTEGER DEFAULT 0,
  accumulated_items JSON,
  floor_history JSON,
  player_hp_percent INTEGER DEFAULT 100,
  player_mp_percent INTEGER DEFAULT 100,
  started_at TIMESTAMP NOT NULL,
  ended_at TIMESTAMP,
  FOREIGN KEY (character_id) REFERENCES characters(id),
  FOREIGN KEY (dungeon_id) REFERENCES dungeon_configs(id)
);
```

---

## 前端实现文件

| 文件 | 职责 |
|:---|:---|
| `src/types/dungeon.ts` | 副本类型定义 |
| `src/config/dungeon_config.ts` | 副本静态配置（8 个副本） |
| `src/api/dungeon.ts` | 副本 API 层（Mock 实现） |
| `src/stores/dungeon.ts` | 副本状态管理（运行生命周期） |
| `src/components/dungeon/DungeonPanel.vue` | 副本选择面板 UI |
| `src/components/dungeon/DungeonFloorResultOverlay.vue` | 楼层结算叠加层 |
| `src/views/HomeView.vue` | 中间面板切换（home/map/dungeon） |
| `src/views/BattleView.vue` | 副本战斗结束处理 |
| `src/components/map/WorldMapPanel.vue` | "副本"按钮事件 |

---

## 体力系统（待实现）

当前版本未实现体力消耗，后续对接后端时需要：

1. 进入副本时扣除体力（`staminaCost`）
2. 体力不足时禁止进入
3. 体力随时间恢复（如每 5 分钟恢复 1 点）

---

*文档版本: 1.0.0*
*最后更新: 2026-05-08*
