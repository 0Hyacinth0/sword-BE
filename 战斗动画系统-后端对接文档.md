# 战斗动画系统 - 后端对接文档

## 概述

战斗动画系统是纯前端实现的视觉反馈层，后端不需要实现动画逻辑。但后端需要确保战斗状态数据中包含足够的字段来驱动前端动画触发。本文档定义动画触发所需的数据结构和接口约定。

---

## 动画触发数据来源

所有战斗动画由前端根据以下数据自动触发：

### 1. 伤害结果 → 触发受击/飘字动画

```typescript
interface DamageResult {
  targetUid: string      // 受击单位 → 播放受击动画
  value: number          // 伤害值 → 飘字数字
  isCritical: boolean    // 是否暴击 → 暴击动画 + 震屏
  isDodged: boolean      // 是否闪避 → 闪避动画 + MISS 飘字
  isHeal: boolean        // 是否治疗 → 治疗光晕 + 绿色飘字
  sourceType: 'attack' | 'skill' | 'item' | 'buff'
}
```

**后端要求**：`BattleState.lastDamageResults` 必须在伤害计算阶段正确填充。

### 2. Buff 结算 → 触发 Buff 动画

```typescript
interface BuffSettlementResult {
  targetUid: string
  buffName: string
  isDebuff: boolean
  value: number
  remainingTurns: number
  expired: boolean
}
```

**后端要求**：`BattleState.lastBuffResults` 在 Buff 结算阶段填充。

---

## 前端动画映射表

| 触发条件 | 攻击者动画 | 目标动画 | 飘字 | 屏幕效果 |
|----------|-----------|---------|------|---------|
| 普通攻击命中 | 冲刺 | 红色闪烁+抖动 | 红色 `-{value}` | 无 |
| 暴击命中 | 冲刺 | 金色闪烁+强抖动 | 金色大号 `-{value}` | 震屏 |
| 闪避 | 冲刺 | 半透明闪烁 | 灰色 `MISS` | 无 |
| 治疗技能 | — | 绿色光晕 | 绿色 `+{value}` | 无 |
| Buff 应用 | — | 蓝色脉冲 | 蓝色 `BUFF` | 无 |
| Debuff 应用 | — | 蓝色脉冲 | 蓝色 `DEBUFF` | 无 |
| 单位死亡 | — | 淡出缩小 | 无 | 无 |

---

## 动画参数

### 浮动文字 (FCT)

| 参数 | 值 | 说明 |
|------|----|------|
| 普通伤害字号 | 20px | 红色 |
| 暴击字号 | 28px | 金色 + 光晕 |
| 治疗字号 | 20px | 绿色 |
| 闪避字号 | 16px | 灰色 |
| 显示时长 | 1.2s | 自动消失 |
| 移动距离 | 向上 45px | 弹出→上飘→淡出 |

### 单位动画

| 动画 | 时长 | 缓动 | 效果 |
|------|------|------|------|
| 攻击冲刺 | 0.3s | 弹性 | 向敌方方向移动 12px |
| 普通受击 | 0.35s | ease-out | 红色背景 + 水平抖动 4px |
| 暴击受击 | 0.5s | ease-out | 金色背景 + 强抖动 6px + 缩放 |
| 治疗光晕 | 0.6s | ease-out | 绿色 box-shadow 呼吸 |
| 闪避半透明 | 0.35s | ease-out | opacity 0.4→1 |
| 死亡淡出 | 0.6s | ease-out | 淡出+缩小+灰度 |
| Buff 脉冲 | 0.4s | ease-out | 蓝色 box-shadow 呼吸 |

### 屏幕效果

| 效果 | 时长 | 幅度 |
|------|------|------|
| 暴击震屏 | 0.3s | 最大偏移 3px |

---

## 前端实现文件

| 文件 | 职责 |
|------|------|
| `src/utils/battleAnimation.ts` | 动画状态管理器，管理浮动文字队列、单位动画状态、震屏 |
| `src/components/battle/CombatantBar.vue` | 单位动画 CSS + 浮动文字渲染容器 |
| `src/views/BattleView.vue` | 震屏效果绑定，watch 触发动画 |

---

## 后端注意事项

1. **DamageResult 完整性**：每次伤害/治疗/闪避事件必须生成完整的 `DamageResult`，前端据此触发动画
2. **lastDamageResults 更新时机**：必须在 `DAMAGE_CALCULATION` 或 `SETTLEMENT` 阶段更新
3. **lastBuffResults 更新时机**：必须在 `BUFF_SETTLEMENT` 阶段更新
4. **uid 一致性**：`targetUid` 和 `actorUid` 必须与 `Combatant.uid` 一致，前端通过 uid 关联动画到具体单位
5. **多目标技能**：AOE 技能的 `lastDamageResults` 应包含所有受影响目标的结果，前端会依次触发每个目标的动画

---

## 动画时序图

```
后端返回 BattleState 更新
       │
       ▼
BattleView watch lastDamageResults
       │
       ├─→ triggerDamageAnimations()
       │       │
       │       ├─→ 攻击者: setUnitAnim('attack')
       │       │         → CombatantBar 播放冲刺动画
       │       │
       │       └─→ 每个目标:
       │             ├─ addFloatingText() → CombatantBar FCT 容器渲染飘字
       │             └─ setUnitAnim('hit'|'crit-hit'|'dodge'|'heal')
       │                   → CombatantBar 播放对应动画
       │
       └─→ 暴击时: triggerScreenShake()
                   → BattleView 播放震屏动画
```

---

*文档版本: 1.0.0*
*最后更新: 2026-05-07*
