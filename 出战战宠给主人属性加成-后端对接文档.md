# 出战战宠给主人属性加成 - 后端对接文档

> 版本：v1.0.0
> 更新日期：2026-05-06
> 说明：定义战宠给主人的属性加成计算公式和接口要求

---

## 功能说明

当角色设置了出战战宠后，战宠会给主人提供额外的属性加成。加成值基于战宠自身属性的一定百分比，并受品质倍率影响。

前端在角色属性面板的"衍生属性"区域展示加成明细：基础 + 装备 + 战宠 = 总计。

---

## 加成计算公式

### 战宠给主人的属性加成

```
bonusHp = floor(pet.hp × 0.1 × rarity_multiplier)
bonusAttack = floor(pet.attack × 0.1 × rarity_multiplier)
bonusDefense = floor(pet.defense × 0.1 × rarity_multiplier)
```

### 品质倍率对照

| 品质 | 倍率 |
|:---:|:---:|
| N (1) | 1.0 |
| R (2) | 1.2 |
| SR (3) | 1.5 |
| SSR (4) | 2.0 |

### 计算示例

以 SR 品质战宠（rarity = 3, multiplier = 1.5）为例：

| 战宠属性 | 计算过程 | 主人加成 |
|:---|:---|:---:|
| HP = 180 | floor(180 × 0.1 × 1.5) | 27 |
| 攻击 = 25 | floor(25 × 0.1 × 1.5) | 3 |
| 防御 = 12 | floor(12 × 0.1 × 1.5) | 1 |

---

## 接口要求

### 1. `/pet/list` 和 `/pet/set-active` 响应中的 bonusToOwner 字段

战宠列表接口返回的每只战宠数据中，`bonusToOwner` 字段应包含该战宠给主人提供的属性加成计算结果。

**bonusToOwner 结构：**

```json
{
  "bonusToOwner": {
    "hp": 27,
    "attack": 3,
    "defense": 1,
    "criticalRate": null,
    "dodgeRate": null
  }
}
```

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `hp` | Integer / null | 生命值加成 |
| `attack` | Integer / null | 攻击力加成 |
| `defense` | Integer / null | 防御力加成 |
| `criticalRate` | Float / null | 暴击率加成（小数形式，如 0.05 = 5%） |
| `dodgeRate` | Float / null | 闪避率加成（小数形式，如 0.03 = 3%） |

### 2. `/character/detail` 响应应包含 activePet 字段

角色详情接口返回数据中，`activePet` 字段携带当前出战战宠的完整信息（含 `bonusToOwner`）。

```json
{
  "activePet": {
    "id": "pet-uuid-001",
    "petTypeId": 1001,
    "nickname": "小火焰",
    "level": 10,
    "rarity": 3,
    "isActive": true,
    "bonusToOwner": { "hp": 27, "attack": 3, "defense": 1 }
  }
}
```

### 3. 后端需在以下场景重新计算 bonusToOwner

| 场景 | 说明 |
|:---|:---|
| 设置出战战宠 | 切换出战时，新出战宠的 bonusToOwner 需重新计算 |
| 喂食升级 | 战宠属性变化后，bonusToOwner 需重新计算 |
| 进化 | 品质提升后，bonusToOwner 需重新计算 |
| 战宠装备变更 | 装备加成影响战宠属性后，bonusToOwner 需重新计算 |

### 4. 角色衍生属性应包含战宠加成

角色的衍生属性（physicalAttack、defense 等）后端计算时**应包含**出战战宠的加成：

```
角色总属性 = 基础属性(力量/智力/敏捷 → 衍生) + 装备加成 + 战宠加成
```

---

## 前端实现说明

### 属性加成明细展示

前端在角色属性面板的"衍生属性"区域下方展示加成来源明细：

- **基础**：力量/智力/敏捷 → 衍生属性
- **装备**：6 槽位装备属性汇总
- **战宠**：出战战宠的 bonusToOwner

使用 `calculateFullStats()` 函数统一计算：

```typescript
const statsBreakdown = calculateFullStats(
  { strength, intelligence, agility },
  profession,
  equipment,
  activePet?.bonusToOwner ?? null
)
// 返回 { base, equipment, pet, total }
```

### 计算逻辑一致性

前端 `attributeCalculator.ts` 中的 `getPetBonus()` 函数将 `PetOwnerBonus` 转换为标准 `DerivedStats` 格式，与基础属性、装备属性格式一致，便于叠加计算。

---

如有疑问请联系前端对接人。
