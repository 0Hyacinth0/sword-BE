# 安全加固（接口防刷、数值校验） - 后端对接文档

## 概述

前端实现接口防刷（节流、去重）和业务数值校验，防止恶意请求和非法数值操作。此功能为纯前端实现，后端无需提供额外 API，但建议后端同步实现相同校验逻辑作为双重保障。

---

## 1. 接口防刷

### 1.1 节流机制

前端对高频接口实施节流，同一 API 在指定时间窗口内只允许一次调用。

| 接口路径 | 节流间隔 | 说明 |
|:---|:---:|:---|
| `/battle/action` | 300ms | 战斗行动提交 |
| `/inventory/use` | 500ms | 物品使用 |
| `/inventory/discard` | 500ms | 物品丢弃 |
| `/character/attributes` | 500ms | 属性加点 |
| `/arena/match` | 2000ms | 竞技场匹配 |
| `/arena/settle` | 1000ms | 竞技结算 |
| `/pet/feed` | 500ms | 战宠喂食 |
| `/pet/evolve` | 1000ms | 战宠进化 |

**前端行为**：
- 超出节流间隔时，请求正常发出
- 在节流间隔内重复请求时，前端拦截并返回 `429` 错误（"操作过快，请稍后再试"）

### 1.2 去重机制

相同参数的并发请求只发一次，复用 Promise。

**适用场景**：
- 防止用户快速双击同一按钮导致重复请求
- 防止组件多次挂载时触发相同初始化请求

**前端行为**：
- 相同 key 的并发请求复用同一个 Promise
- 请求完成后自动清理缓存

---

## 2. 数值校验

### 2.1 前端校验函数

| 校验函数 | 参数 | 校验规则 |
|:---|:---|:---|
| `validateAttributePoints` | point, available | point >= 0 && point <= available |
| `validateItemQuantity` | quantity, owned | quantity > 0 && quantity <= owned |
| `validateScore` | score | score >= 0 |
| `validateLevel` | level | level > 0 && level <= 100 |

**前端行为**：
- 校验失败时显示提示 Toast，阻止后续请求
- 校验成功时继续发送 API 请求

### 2.2 后端同步校验（建议）

后端应在接收请求时执行相同校验逻辑，作为双重保障：

| 接口 | 建议校验 |
|:---|:---|
| `POST /character/update-attributes` | 加点总数 ≤ 可用自由点数 |
| `POST /inventory/use` | 使用数量 ≤ 拥有数量 |
| `POST /inventory/discard` | 丢弃数量 ≤ 拥有数量 |
| `POST /arena/settle` | 积分变化 ≥ 0（仅胜利加分） |
| `POST /pet/feed` | 道具数量 ≤ 拥有数量 |

---

## 3. 错误响应

前端拦截节流请求时返回：

```json
{
  "code": 429,
  "message": "操作过快，请稍后再试"
}
```

后端校验失败时应返回：

```json
{
  "code": 400,
  "message": "具体错误原因"
}
```

---

## 4. 前端实现位置

| 文件 | 职责 |
|:---|:---|
| `src/utils/apiGuard.ts` | throttleRequest、dedupeRequest、getThrottleInterval |
| `src/utils/validators.ts` | 数值校验函数 |
| `src/api/request.ts` | 请求拦截器节流检查 |

---

## 5. 使用示例

```typescript
// 战斗行动提交（防刷）
import { throttleRequest } from '../utils/apiGuard'

async function submitAction(action) {
  const { throttled } = await throttleRequest('battle-action', () => battleStore.submitAction(action), 300)
  if (throttled) {
    showToast('操作过快，请稍后再试')
    return
  }
}

// 属性加点（校验）
import { validateAttributePoints } from '../utils/validators'

function handleAddStrength() {
  const { valid, message } = validateAttributePoints(1, character.availablePoints)
  if (!valid) {
    showToast(message)
    return
  }
  // 执行加点...
}
```

---

## 6. 不做的功能

以下功能不在本次前端实现范围：

- CSRF 防护
- Token 刷新机制
- 请求签名/加密
- CSP/XSS 安全头
- 安全日志/审计