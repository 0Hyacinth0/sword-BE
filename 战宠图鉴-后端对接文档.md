# 战宠图鉴 - 后端对接文档

> 版本：v1.0.0
> 更新日期：2026-05-06
> 说明：定义战宠图鉴相关接口（战宠类型全量列表、收集进度）

---

## 功能说明

战宠图鉴页面展示游戏中所有战宠类型，已拥有的正常显示，未拥有的显示为剪影 + "???"。支持按元素筛选，显示收集进度，点击可查看详情（属性分布、进化链、技能列表、描述）。

---

## 接口定义

### GET `/pet/types` — 获取所有战宠类型（图鉴用）

返回全量战宠类型配置，前端用于渲染图鉴网格。

**响应数据：**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "total": 10,
    "types": [
      {
        "petTypeId": 1001,
        "name": "火焰精灵",
        "element": 1,
        "rarity": 3,
        "baseHp": 80,
        "baseAttack": 18,
        "baseDefense": 8,
        "baseSpeed": 14,
        "description": "诞生于熔岩的精灵，擅长火属性魔法攻击",
        "evolveTo": 1004,
        "evolveLevel": 15,
        "skills": [
          { "id": 1, "name": "火球术", "type": "active_attack", "power": 120, "cooldown": 2, "learnLevel": 1, "description": "发射火球造成120%攻击力的伤害" }
        ]
      }
    ]
  }
}
```

### PetTypeConfig（战宠类型配置）

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `petTypeId` | Integer | 战宠类型 ID |
| `name` | String | 战宠名称 |
| `element` | Integer | 元素类型（1-火, 2-水, 3-风, 4-地, 5-光, 6-暗） |
| `rarity` | Integer | 稀有度（1-N, 2-R, 3-SR, 4-SSR） |
| `baseHp` | Integer | 基础生命 |
| `baseAttack` | Integer | 基础攻击 |
| `baseDefense` | Integer | 基础防御 |
| `baseSpeed` | Integer | 基础速度 |
| `description` | String | 战宠描述 |
| `evolveTo` | Integer / null | 进化目标类型 ID，null 为最终形态 |
| `evolveLevel` | Integer / null | 进化所需等级 |
| `skills` | PetSkill[] | 该类型可学习的技能列表 |

---

### GET `/pet/owned-types/{characterId}` — 获取角色已拥有的战宠类型 ID 列表

**响应数据：**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "ownedTypeIds": [1001, 1002, 1003]
  }
}
```

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `ownedTypeIds` | Integer[] | 角色已拥有的战宠类型 ID 列表（去重） |

> **说明**：也可以复用 `/pet/list` 接口，前端从已有战宠中提取 `petTypeId` 列表。如果后端提供独立接口更高效。

---

## 前端实现说明

### 图鉴展示逻辑

1. **全量类型来源**：后端 `/pet/types` 接口或前端静态配置 `PET_TYPE_CONFIGS`
2. **收集状态**：从角色的战宠列表中提取已拥有的 `petTypeId`，在图鉴中标记为"已收集"
3. **筛选**：支持按元素类型（火/水/风/地/光/暗）筛选
4. **详情弹窗**：点击卡片展示属性条形图、进化链、技能列表、描述

### 进化链

前端通过 `evolveTo` 字段递归查找，构建从基础形态到最终形态的进化链：
```
火焰精灵 (SR) → 烈焰凤凰 (SSR)
雷鹰 (SR) → 风暴雷神 (SSR)
```

### 属性分布图

使用条形图展示 4 个维度（生命/攻击/防御/速度），每个维度的百分比 = `属性值 / 该维度最大值 × 100%`。

### 收集进度

展示格式：`已收集 3/10`（分子为已拥有类型数，分母为全量类型数）。

---

## 静态配置备注

当前前端使用 `config/pet_config.ts` 中的 `PET_TYPE_CONFIGS` 作为图鉴数据源，包含 10 种战宠类型。后端上线后应从 API 获取全量类型配置。

### 已配置战宠类型

| ID | 名称 | 元素 | 品质 | 可进化 |
|:---|:---|:---|:---:|:---:|
| 1001 | 火焰精灵 | 火 | SR | → 烈焰凤凰 |
| 1002 | 风狼 | 风 | R | - |
| 1003 | 冰晶凤凰 | 水 | SSR | - |
| 1004 | 烈焰凤凰 | 火 | SSR | - |
| 1005 | 土岩兽 | 地 | N | - |
| 1006 | 光辉精灵 | 光 | SR | - |
| 1007 | 暗影猫 | 暗 | R | - |
| 1008 | 水灵龟 | 水 | N | - |
| 1009 | 雷鹰 | 火 | SR | → 风暴雷神 |
| 1010 | 风暴雷神 | 火 | SSR | - |

---

如有疑问请联系前端对接人。
