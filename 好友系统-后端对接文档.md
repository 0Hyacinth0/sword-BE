# 好友系统 - 后端对接文档

## 概述

本文档描述好友系统的前后端对接规范。好友系统是社交模块的基础，支持玩家搜索、添加好友、管理好友列表、查看在线状态等功能。

---

## 前端实现概览

| 文件 | 作用 |
|:---|:---|
| `src/types/social.ts` | 好友相关类型定义 |
| `src/api/social.ts` | 好友 API（Mock + Real） |
| `src/stores/social.ts` | 好友状态管理 |
| `src/components/social/FriendPanel.vue` | 好友面板主组件 |

---

## 数据结构

### FriendInfo（好友信息）

```typescript
interface FriendInfo {
  characterId: string      // 好友角色 ID
  characterName: string    // 角色名
  profession: string       // 职业（Warrior/Mage/Hunter）
  level: number            // 等级
  status: FriendStatus     // 在线状态
  lastOnlineAt: string | null  // 最后在线时间（离线时）
  addedAt: string          // 好友关系建立时间
}
```

### FriendRequest（好友请求）

```typescript
interface FriendRequest {
  id: string               // 请求 ID
  fromCharacterId: string  // 发送者角色 ID
  fromCharacterName: string
  fromProfession: string
  fromLevel: number
  toCharacterId: string    // 接收者角色 ID
  status: RequestStatus    // pending/accepted/rejected
  createdAt: string
}
```

### SearchPlayerResult（搜索结果）

```typescript
interface SearchPlayerResult {
  characterId: string
  characterName: string
  profession: string
  level: number
  isFriend: boolean        // 是否已是好友
  hasPendingRequest: boolean  // 是否已发送请求
}
```

### FriendStatus（在线状态）

```typescript
type FriendStatus = 'online' | 'offline' | 'busy'
```

---

## 接口规范

### Base URL

```
/jeecg-boot/webgame
```

### 认证方式

- 采用 JWT 认证
- 请求头携带：`Authorization: Bearer <token>`
- 请求拦截器自动注入 `userId` 和 `characterId`

---

### 1. 获取好友列表

**GET `/social/friends`**

获取当前角色的好友列表、待处理请求、已发送请求。

**响应示例**：
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "friends": [
      {
        "characterId": "uuid-xxx",
        "characterName": "影刃",
        "profession": "Hunter",
        "level": 28,
        "status": "online",
        "lastOnlineAt": null,
        "addedAt": "2026-04-15T10:30:00Z"
      }
    ],
    "pendingRequests": [
      {
        "id": "req-uuid",
        "fromCharacterId": "uuid-yyy",
        "fromCharacterName": "雷霆战士",
        "fromProfession": "Warrior",
        "fromLevel": 18,
        "toCharacterId": "uuid-current",
        "status": "pending",
        "createdAt": "2026-05-12T09:00:00Z"
      }
    ],
    "sentRequests": []
  }
}
```

---

### 2. 搜索玩家

**GET `/social/search`**

根据角色名搜索玩家。

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|:---|:---|:---:|:---|
| keyword | string | 是 | 搜索关键词（至少 2 字符） |

**响应示例**：
```json
{
  "code": 200,
  "message": "搜索成功",
  "data": [
    {
      "characterId": "uuid-xxx",
      "characterName": "影刃",
      "profession": "Hunter",
      "level": 28,
      "isFriend": true,
      "hasPendingRequest": false
    }
  ]
}
```

---

### 3. 发送好友请求

**POST `/social/request`**

向目标角色发送好友请求。

**请求参数**：
```json
{
  "toCharacterId": "uuid-target"
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "好友请求已发送",
  "data": {
    "id": "req-uuid",
    "fromCharacterId": "uuid-current",
    "toCharacterId": "uuid-target",
    "status": "pending",
    "createdAt": "2026-05-14T10:00:00Z"
  }
}
```

**错误码**：
| code | 说明 |
|:---:|:---|
| 400 | 已经是好友 / 已发送过请求 |
| 404 | 目标角色不存在 |

---

### 4. 接受好友请求

**POST `/social/accept`**

接受好友请求，建立好友关系。

**请求参数**：
```json
{
  "requestId": "req-uuid"
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "好友请求已接受",
  "data": {
    "characterId": "uuid-friend",
    "characterName": "雷霆战士",
    "profession": "Warrior",
    "level": 18,
    "status": "online",
    "addedAt": "2026-05-14T10:05:00Z"
  }
}
```

---

### 5. 拒绝好友请求

**POST `/social/reject`**

拒绝好友请求。

**请求参数**：
```json
{
  "requestId": "req-uuid"
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "好友请求已拒绝",
  "data": null
}
```

---

### 6. 删除好友

**DELETE `/social/remove`**

删除好友关系。

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|:---|:---|:---:|:---|
| friendCharacterId | string | 是 | 好友角色 ID |

**响应示例**：
```json
{
  "code": 200,
  "message": "好友已删除",
  "data": null
}
```

---

### 7. 取消好友请求

**POST `/social/cancel`**

取消已发送的好友请求。

**请求参数**：
```json
{
  "requestId": "req-uuid"
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "好友请求已取消",
  "data": null
}
```

---

## 数据库表结构建议

### 好友关系表 (friendships)

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `id` | UUID | 主键 |
| `character_id_1` | UUID | 角色 1 ID（较小的 ID） |
| `character_id_2` | UUID | 角色 2 ID（较大的 ID） |
| `created_at` | TIMESTAMP | 建立好友时间 |

> **约束**：`(character_id_1, character_id_2)` 唯一索引，确保好友关系不重复。

### 好友请求表 (friend_requests)

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `id` | UUID | 主键 |
| `from_character_id` | UUID | 发送者角色 ID |
| `to_character_id` | UUID | 接收者角色 ID |
| `status` | ENUM | pending/accepted/rejected |
| `created_at` | TIMESTAMP | 创建时间 |
| `updated_at` | TIMESTAMP | 更新时间 |

> **约束**：`(from_character_id, to_character_id)` 唯一索引，确保同一对只能有一条请求。

---

## 在线状态实现

### 方案一：轮询（推荐初期）

前端每 30 秒调用 `GET /social/friends` 刷新好友列表，后端根据用户会话判断在线状态。

### 方案二：WebSocket（推荐后期）

建立 WebSocket 连接，后端推送好友上下线事件：

```json
// 好友上线
{ "event": "friend:online", "characterId": "uuid-xxx" }

// 好友下线
{ "event": "friend:offline", "characterId": "uuid-xxx", "lastOnlineAt": "2026-05-14T10:00:00Z" }
```

---

## 业务规则

1. **好友数量上限**：建议每个角色最多 50 个好友
2. **请求有效期**：好友请求 7 天未处理自动过期
3. **搜索限制**：搜索关键词至少 2 个字符，避免返回过多结果
4. **屏蔽功能**：后续可扩展黑名单功能

---

## 前端 Mock 数据

当前 Mock 包含：
- 3 个好友（1 在线、1 离线、1 忙碌）
- 2 条待处理请求
- 1 条已发送请求

Mock 数据在 `src/api/social.ts` 中定义，用于前端开发测试。

---

## 测试要点

1. **搜索功能**：输入至少 2 字符可搜索，返回匹配结果
2. **添加好友**：非好友可发送请求，已是好友或已申请则显示对应标签
3. **好友请求**：可接受或拒绝请求，接受后双方成为好友
4. **删除好友**：删除确认弹窗，确认后从列表移除
5. **在线状态**：在线好友显示绿色圆点，离线显示灰色

---

## 版本记录

| 版本 | 日期 | 说明 |
|:---:|:---:|:---|
| v1.0 | 2026-05-14 | 初始版本，前端 Mock 实现 |
