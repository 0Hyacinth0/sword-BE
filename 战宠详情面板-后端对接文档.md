# 战宠详情面板 - 后端对接文档

> 版本：v1.0.0
> 更新日期：2026-05-06
> 说明：定义战宠喂食、进化、重命名接口

---

## 功能说明

战宠详情面板提供标签页切换的养成交互，包含四个功能页：

1. **属性页**：展示战宠属性、主人加成、重命名功能
2. **技能页**：展示战宠已学技能列表
3. **进化页**：展示进化目标、条件校验、触发进化
4. **喂食页**：选择经验道具、数量，喂食升级

---

## 接口定义

### POST `/pet/feed` — 喂食战宠

使用经验道具给战宠增加经验值，可能触发升级。

**请求参数：**
```json
{
  "characterId": "uuid",
  "petId": "pet-uuid-001",
  "inventoryId": "inv-uuid-001",
  "quantity": 3
}
```

| 字段 | 类型 | 必填 | 说明 |
|:---|:---|:---:|:---|
| `characterId` | String (UUID) | 是 | 角色 ID |
| `petId` | String (UUID) | 是 | 战宠实例 ID |
| `inventoryId` | String (UUID) | 是 | 经验道具背包记录 ID |
| `quantity` | Integer | 是 | 使用数量（≥1） |

**后端校验逻辑：**

1. 校验角色存在
2. 校验战宠属于该角色
3. 校验背包中存在该经验道具且数量足够
4. 校验道具效果包含 `add_exp` 类型
5. 扣减背包道具数量（数量为 0 时删除记录）
6. 为战宠增加经验值，处理升级逻辑
7. 重新计算战宠属性和主人加成
8. 返回更新后的战宠列表

**响应数据：** 同 `/pet/list` 格式

---

### POST `/pet/evolve` — 进化战宠

满足进化条件时触发战宠进化，变更战宠类型、提升品质。

**请求参数：**
```json
{
  "characterId": "uuid",
  "petId": "pet-uuid-001"
}
```

| 字段 | 类型 | 必填 | 说明 |
|:---|:---|:---:|:---|
| `characterId` | String (UUID) | 是 | 角色 ID |
| `petId` | String (UUID) | 是 | 战宠实例 ID |

**后端校验逻辑：**

1. 校验角色存在
2. 校验战宠属于该角色
3. 查询战宠类型的进化配置（`pet_types.evolve_to`）
4. 校验战宠等级 ≥ 进化所需等级（`pet_types.evolve_level`）
5. 校验背包拥有足够的进化材料（`pet_evolve_items` 配置）
6. 扣减进化材料
7. 更新战宠类型为进化目标（`pet_type_id = evolve_to`）
8. 品质 +1（N→R→SR→SSR），最高 SSR
9. 保留 80% 当前经验进度：`exp = floor(exp × 0.8)`
10. 重新计算属性（新类型基础 + 等级成长 × 新品质倍率）
11. 习得进化专属技能（`learn_level = 0` 的技能）
12. 已装备的技能和装备保留
13. 重新计算主人加成
14. 返回更新后的战宠列表

**进化后属性计算：**
```
hp = (new_base_hp + level × new_hp_growth) × new_rarity_multiplier + equip_hp
attack = (new_base_attack + level × new_attack_growth) × new_rarity_multiplier + equip_attack
defense = (new_base_defense + level × new_defense_growth) × new_rarity_multiplier + equip_defense
speed = (new_base_speed + level × new_speed_growth) × new_rarity_multiplier + equip_speed
```

**响应数据：** 同 `/pet/list` 格式

---

### POST `/pet/rename` — 重命名战宠

修改战宠昵称。

**请求参数：**
```json
{
  "characterId": "uuid",
  "petId": "pet-uuid-001",
  "nickname": "新昵称"
}
```

| 字段 | 类型 | 必填 | 说明 |
|:---|:---|:---:|:---|
| `characterId` | String (UUID) | 是 | 角色 ID |
| `petId` | String (UUID) | 是 | 战宠实例 ID |
| `nickname` | String | 是 | 新昵称（1-12 字符） |

**后端校验逻辑：**

1. 校验角色存在
2. 校验战宠属于该角色
3. 校验昵称长度 1-12 字符
4. 校验昵称不含敏感词（可选）
5. 更新 `character_pets.nickname`
6. 返回更新后的战宠列表

**响应数据：** 同 `/pet/list` 格式

---

### GET `/pet/detail/{petId}` — 获取战宠详情

获取单个战宠的完整详情信息（含进化配置、描述等）。

**响应数据：**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": "pet-uuid-001",
    "petTypeId": 1001,
    "nickname": "小火焰",
    "level": 10,
    "exp": 8500,
    "maxExp": 10000,
    "rarity": 3,
    "isActive": true,
    "stats": { "hp": 180, "maxHp": 180, "attack": 25, "defense": 12, "speed": 18 },
    "bonusToOwner": { "hp": 18, "attack": 3, "defense": 1 },
    "skills": [
      { "id": 1, "name": "火球术", "type": "active_attack", "power": 120, "cooldown": 2, "description": "发射火球造成120%攻击力的伤害" }
    ],
    "equipment": { "armor": null, "accessory": null },
    "element": 1,
    "evolveTo": 1004,
    "evolveLevel": 15,
    "description": "火焰精灵是温暖的伙伴"
  }
}
```

---

## 前端调用流程

### 喂食流程
1. 前端从背包中筛选效果含 `add_exp` 的消耗品作为经验道具列表
2. 用户选择道具和数量，前端显示预计经验预览
3. 点击喂食，调用 `POST /pet/feed`
4. 成功后刷新战宠列表和背包数据

### 进化流程
1. 前端根据 `petTypeId` 查询进化配置（`evolveTo`、`evolveLevel`）
2. 前端查询进化材料配置，对比背包材料数量
3. 满足条件时进化按钮可用
4. 点击进化，调用 `POST /pet/evolve`
5. 成功后刷新战宠列表和角色数据（主人加成变化）

### 重命名流程
1. 用户在输入框修改昵称
2. 昵称与当前不同且不为空时，确认按钮可用
3. 点击确认，调用 `POST /pet/rename`
4. 成功后刷新战宠列表

---

## 静态配置依赖

前端使用以下静态配置（模拟后端 `pet_types` 等表）：

| 配置项 | 前端文件 | 对应后端表 |
|:---|:---|:---|
| 战宠类型配置 | `config/pet_config.ts` → `PET_TYPE_CONFIGS` | `pet_types` |
| 进化材料配置 | `config/pet_config.ts` → `EVOLVE_MATERIALS` | `pet_evolve_items` + `items` |
| 元素类型标签 | `config/pet_config.ts` → `ELEMENT_LABELS` | 枚举值 |
| 技能类型标签 | `config/pet_config.ts` → `SKILL_TYPE_LABELS` | 枚举值 |

> 后端上线后，这些静态配置应从 API 获取或在后端维护。当前前端 Mock 使用本地配置。

---

如有疑问请联系前端对接人。
