# 多人副本大厅 - 后端对接文档

## 概述

本文档描述多人副本大厅系统的前后端对接规范。副本大厅基于组队系统，让队伍成员可以一起进入副本挑战。

系统采用 **WebSocket 实时通信** 为主，REST API 轮询为降级方案。

---

## 前端实现概览

| 文件 | 作用 |
|:---|:---|
| `src/types/team.ts` | DungeonRoom、RoomMember 类型定义 |
| `src/api/dungeonRoom.ts` | 副本房间 API（Mock + Real） |
| `src/stores/dungeonRoom.ts` | 副本房间状态管理 |
| `src/components/team/DungeonRoomPanel.vue` | 副本房间面板组件 |
| `src/components/team/TeamPanel.vue` | 组队面板（集成大厅入口） |

---

## 数据结构

### DungeonRoom（副本房间）

```typescript
interface DungeonRoom {
  roomId: string           // 房间 ID
  teamId: string           // 关联的队伍 ID
  dungeonId: string        // 目标副本 ID
  dungeonName: string      // 副本名称
  difficulty: 'normal' | 'elite'  // 难度
  leaderId: string         // 队长角色 ID
  members: RoomMember[]    // 房间成员（含准备状态）
  status: RoomStatus       // 房间状态
  createdAt: string        // 创建时间
}
```

### RoomMember（房间成员）

```typescript
interface RoomMember {
  characterId: string      // 角色ID
  characterName: string    // 角色名
  profession: string       // 职业
  level: number            // 等级
  readyStatus: ReadyStatus // 准备状态
}
```

### ReadyStatus（准备状态）

```typescript
type ReadyStatus = 'not_ready' | 'ready'
```

### RoomStatus（房间状态）

```typescript
type RoomStatus = 'waiting' | 'ready' | 'starting' | 'in_progress'
```

- `waiting`：等待成员准备
- `ready`：全员准备就绪，等待队长开始
- `starting`：队长已点击开始，正在初始化战斗
- `in_progress`：战斗进行中

---

## WebSocket 协议

### 连接地址

```
ws://{host}/jeecg-boot/webgame/ws/dungeon-room?token={jwt_token}
```

### 认证

连接时通过 URL 参数传递 JWT Token，服务端验证后建立连接。

### 消息格式

所有 WebSocket 消息采用 JSON 格式：

```json
{
  "event": "事件名称",
  "data": { ... }
}
```

---

### 客户端发送事件

#### 1. 创建房间

```json
{
  "event": "room:create",
  "data": {
    "teamId": "team-uuid",
    "dungeonId": "mist_forest_normal"
  }
}
```

**响应**：
```json
{
  "event": "room:created",
  "data": {
    "roomId": "room-uuid",
    "dungeonId": "mist_forest_normal",
    "dungeonName": "迷雾密林",
    "difficulty": "normal",
    "leaderId": "uuid-leader",
    "members": [
      {
        "characterId": "uuid-leader",
        "characterName": "雷霆战士",
        "profession": "Warrior",
        "level": 25,
        "readyStatus": "ready"
      }
    ],
    "status": "waiting",
    "createdAt": "2026-05-14T10:00:00Z"
  }
}
```

**广播**：其他队伍成员收到：
```json
{
  "event": "room:invited",
  "data": {
    "roomId": "room-uuid",
    "dungeonName": "迷雾密林",
    "leaderName": "雷霆战士"
  }
}
```

---

#### 2. 切换准备状态

```json
{
  "event": "room:ready",
  "data": {
    "roomId": "room-uuid"
  }
}
```

**广播**（所有房间成员）：
```json
{
  "event": "room:member_ready_changed",
  "data": {
    "roomId": "room-uuid",
    "characterId": "uuid-xxx",
    "readyStatus": "ready"
  }
}
```

**房间状态变更通知**：
```json
{
  "event": "room:status_changed",
  "data": {
    "roomId": "room-uuid",
    "status": "ready"
  }
}
```

---

#### 3. 开始挑战

```json
{
  "event": "room:start",
  "data": {
    "roomId": "room-uuid"
  }
}
```

**广播**（所有房间成员）：
```json
{
  "event": "room:started",
  "data": {
    "roomId": "room-uuid",
    "dungeonId": "mist_forest_normal",
    "members": [...],
    "monsterScale": 1.5
  }
}
```

`monsterScale` 为怪物属性缩放倍率，根据人数计算。

---

#### 4. 离开房间

```json
{
  "event": "room:leave",
  "data": {
    "roomId": "room-uuid"
  }
}
```

**广播**：
```json
{
  "event": "room:member_left",
  "data": {
    "roomId": "room-uuid",
    "characterId": "uuid-xxx",
    "characterName": "冰霜法师"
  }
}
```

---

#### 5. 取消房间（队长）

```json
{
  "event": "room:cancel",
  "data": {
    "roomId": "room-uuid"
  }
}
```

**广播**：
```json
{
  "event": "room:cancelled",
  "data": {
    "roomId": "room-uuid"
  }
}
```

---

## REST API（降级轮询方案）

当 WebSocket 不可用时，前端降级为 REST API 轮询（每 2 秒）。

### Base URL

```
/jeecg-boot/webgame
```

### 1. 创建副本房间

**POST `/dungeon-room/create`**

```json
// 请求
{
  "teamId": "team-uuid",
  "dungeonId": "mist_forest_normal"
}

// 响应
{
  "code": 200,
  "message": "房间创建成功",
  "data": {
    "roomId": "room-uuid",
    "teamId": "team-uuid",
    "dungeonId": "mist_forest_normal",
    "dungeonName": "迷雾密林",
    "difficulty": "normal",
    "leaderId": "uuid-leader",
    "members": [...],
    "status": "waiting",
    "createdAt": "2026-05-14T10:00:00Z"
  }
}
```

### 2. 获取房间信息

**GET `/dungeon-room/:roomId`**

```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "roomId": "room-uuid",
    ...
  }
}
```

### 3. 切换准备状态

**POST `/dungeon-room/ready`**

```json
// 请求
{
  "roomId": "room-uuid"
}

// 响应
{
  "code": 200,
  "message": "状态已更新",
  "data": { ... }
}
```

### 4. 开始挑战

**POST `/dungeon-room/start`**

```json
// 请求
{
  "roomId": "room-uuid"
}

// 响应
{
  "code": 200,
  "message": "挑战开始",
  "data": {
    "roomId": "room-uuid",
    "status": "in_progress",
    "monsterScale": 1.5,
    ...
  }
}
```

**错误码**：
| code | 说明 |
|:---:|:---|
| 400 | 还有成员未准备 |
| 403 | 不是队长，无权开始 |

### 5. 离开房间

**POST `/dungeon-room/leave`**

```json
// 请求
{
  "roomId": "room-uuid"
}

// 响应
{
  "code": 200,
  "message": "已离开房间",
  "data": null
}
```

### 6. 取消房间

**DELETE `/dungeon-room/cancel/:roomId`**

```json
{
  "code": 200,
  "message": "房间已取消",
  "data": null
}
```

---

## 怪物属性缩放公式

多人副本中，怪物属性根据队伍人数缩放：

```
怪物生命值 = 基础生命值 × (1 + 0.5 × (队伍人数 - 1))
怪物攻击力 = 基础攻击力 × (1 + 0.3 × (队伍人数 - 1))
怪物防御力 = 基础防御力 × (1 + 0.2 × (队伍人数 - 1))
```

**示例**：

| 人数 | HP 倍率 | ATK 倍率 | DEF 倍率 |
|:---:|:---:|:---:|:---:|
| 1 | 1.0x | 1.0x | 1.0x |
| 2 | 1.5x | 1.3x | 1.2x |
| 3 | 2.0x | 1.6x | 1.4x |
| 4 | 2.5x | 1.9x | 1.6x |

---

## 数据库表结构建议

### 副本房间表 (dungeon_rooms)

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `id` | UUID | 主键 |
| `team_id` | UUID (FK → teams) | 关联队伍 ID |
| `dungeon_id` | String | 副本配置 ID |
| `leader_id` | UUID (FK → characters) | 队长角色 ID |
| `status` | ENUM: waiting/ready/starting/in_progress/completed/cancelled | 房间状态 |
| `monster_scale` | Decimal | 怪物缩放倍率 |
| `created_at` | TIMESTAMP | 创建时间 |
| `updated_at` | TIMESTAMP | 更新时间 |

### 副本房间成员表 (dungeon_room_members)

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `id` | UUID | 主键 |
| `room_id` | UUID (FK → dungeon_rooms) | 房间 ID |
| `character_id` | UUID (FK → characters) | 成员角色 ID |
| `ready_status` | ENUM: not_ready/ready | 准备状态 |
| `joined_at` | TIMESTAMP | 加入时间 |

> **约束**：`character_id` 唯一索引。

---

## 业务规则

1. **创建条件**：只有在队伍中的队长可以创建副本房间
2. **房间人数**：与队伍人数一致，最大 4 人
3. **准备要求**：所有成员（包括队长）必须准备后才可开始
4. **队长特权**：创建房间、取消房间、开始挑战
5. **成员离开**：成员离开房间后房间仍然存在；队长离开则自动取消
6. **超时机制**：房间创建后 10 分钟未开始，自动取消
7. **掉线重连**：WebSocket 断开后 30 秒内重连，房间状态保留
8. **战斗同步**：副本开始后，所有成员进入相同的战斗实例

---

## 前端 Mock 数据

当前 Mock 包含：
- 创建房间后队长自动准备
- 其他成员 2-3 秒后自动准备（模拟实时行为）
- 全员准备后队长可开始挑战

Mock 数据在 `src/api/dungeonRoom.ts` 中定义，用于前端开发测试。

---

## 测试要点

1. **创建房间**：队长在队伍面板点击"选择副本"，选择副本后进入房间
2. **副本选择**：显示所有 8 个副本，等级不足的显示锁定
3. **准备状态**：成员可切换准备/取消准备
4. **自动准备**：Mock 模式下其他成员 2-3 秒后自动准备
5. **开始挑战**：全员准备后队长"开始挑战"按钮高亮可点击
6. **取消房间**：队长可取消房间，返回队伍面板
7. **离开房间**：成员可离开房间，返回队伍面板
8. **返回队伍**：所有状态正确清理

---

## 版本记录

| 版本 | 日期 | 说明 |
|:---:|:---:|:---|
| v1.0 | 2026-05-14 | 初始版本，前端 Mock + WebSocket 协议定义 |
