# 战宠装备穿脱 - 后端对接文档

> 版本：v1.0.0
> 更新日期：2026-05-06
> 说明：定义战宠装备穿戴/卸下接口

---

## 功能说明

战宠拥有 2 个装备槽位：护甲（armor）和饰品（accessory）。可从角色背包中选择装备穿戴到战宠身上，或从战宠身上卸下装备回到背包。

- 穿戴装备后，战宠属性获得装备加成
- 卸下装备后，装备回到角色背包
- 同一件装备不可同时被角色和战宠使用

---

## 接口定义

### POST `/pet/equip-item` — 给战宠穿戴装备

从角色背包中选择装备穿戴到战宠的指定槽位。

**请求参数：**
```json
{
  "characterId": "uuid",
  "petId": "pet-uuid-001",
  "inventoryId": "inv-uuid-001",
  "slotType": "armor"
}
```

| 字段 | 类型 | 必填 | 说明 |
|:---|:---|:---:|:---|
| `characterId` | String (UUID) | 是 | 角色 ID |
| `petId` | String (UUID) | 是 | 战宠实例 ID |
| `inventoryId` | String (UUID) | 是 | 背包中的装备记录 ID |
| `slotType` | String | 是 | 槽位类型：`armor`（护甲）或 `accessory`（饰品） |

**后端校验逻辑：**

1. 校验角色存在
2. 校验战宠属于该角色
3. 校验 `slotType` 为 `armor` 或 `accessory`
4. 校验背包记录存在且属于该角色
5. 校验物品类别为 `equipment`（装备类）
6. 如果槽位已有装备，先将旧装备回到背包（自动替换）
7. 从背包移除该装备记录
8. 更新 `character_pets` 表的 `equip_armor` 或 `equip_accessory` 字段
9. 重新计算战宠属性（含装备加成）和主人加成
10. 返回更新后的战宠列表

**响应数据：** 同 `/pet/list` 格式，每只战宠的 `equipment` 字段包含完整装备信息：

```json
{
  "equipment": {
    "armor": {
      "id": "eq-uuid",
      "name": "火焰护甲",
      "rarity": "Rare",
      "slotType": "chest",
      "stats": { "hp": 30, "defense": 8 },
      "enhanceLevel": 2
    },
    "accessory": null
  }
}
```

---

### POST `/pet/unequip-item` — 卸下战宠装备

将战宠指定槽位的装备卸下，回到角色背包。

**请求参数：**
```json
{
  "characterId": "uuid",
  "petId": "pet-uuid-001",
  "slotType": "armor"
}
```

| 字段 | 类型 | 必填 | 说明 |
|:---|:---|:---:|:---|
| `characterId` | String (UUID) | 是 | 角色 ID |
| `petId` | String (UUID) | 是 | 战宠实例 ID |
| `slotType` | String | 是 | 槽位类型：`armor` 或 `accessory` |

**后端校验逻辑：**

1. 校验角色存在
2. 校验战宠属于该角色
3. 校验 `slotType` 为 `armor` 或 `accessory`
4. 校验该槽位有装备（不为 null）
5. 将装备添加回角色背包（新建 inventory 记录）
6. 更新 `character_pets` 表的对应槽位为 null
7. 重新计算战宠属性（移除装备加成）和主人加成
8. 返回更新后的战宠列表

**响应数据：** 同 `/pet/list` 格式

---

## 数据结构

### PetEquipment（战宠装备）

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `armor` | Equipment / null | 护甲装备（完整装备对象，含属性和品质） |
| `accessory` | Equipment / null | 饰品装备（完整装备对象，含属性和品质） |

### 装备对属性的影响

战宠穿上装备后，装备属性加到战宠基础属性上：

```
hp = (base_hp + level × hp_growth) × rarity_multiplier + equip_armor.stats.hp + equip_accessory.stats.hp
defense = ... + equip_armor.stats.defense + equip_accessory.stats.defense
```

### 数据库存储

`character_pets` 表中：

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `equip_armor` | UUID (FK → character_items) | 护甲装备，可 null |
| `equip_accessory` | UUID (FK → character_items) | 饰品装备，可 null |

> 穿戴时从 `character_items` 表移动记录到战宠关联，卸下时移回。

---

## 前端交互流程

1. 用户在战宠详情面板的属性标签页中看到装备槽位
2. 空槽位显示"未装备"和"装备"按钮
3. 已装备槽位显示装备名（品质颜色）、属性加成、"卸下"和"替换"按钮
4. 点击"装备"/"替换"按钮，弹出装备选择面板
5. 面板中列出背包中所有装备类物品（名称、品质、属性、数量）
6. 选择装备后调用 `POST /pet/equip-item`
7. 点击"卸下"按钮调用 `POST /pet/unequip-item`
8. 操作后刷新战宠列表和背包数据

---

如有疑问请联系前端对接人。
