# 角色属性计算公式实现说明

## 一、已完成的工作

### 1. 创建了属性计算工具类
**文件**: `CharacterStatsCalculator.java`  
**路径**: `jeecg-boot-module-webgame/src/main/java/org/jeecg/modules/webgame/character/util/`

#### 核心功能：
- ✅ 实现了标准的属性计算公式（与前端文档完全一致）
- ✅ 定义了所有公式常量参数
- ✅ 实现了三个职业的职业加成配置
- ✅ 提供了 `calculateBaseStats()` 方法计算衍生属性

#### 公式常量：
```java
STR_TO_HP = 5              // 1点力量 = 5 HP
INT_TO_MP = 5              // 1点智力 = 5 MP
STR_TO_PHYSICAL_ATTACK = 2 // 1点力量 = 2 物攻
INT_TO_MAGIC_ATTACK = 2    // 1点智力 = 2 魔攻
AGI_TO_PHYSICAL_ATTACK = 0.5 // 1点敏捷 = 0.5 物攻
AGI_TO_DODGE = 0.005       // 1点敏捷 = 0.5% 闪避率
AGI_TO_CRITICAL = 0.004    // 1点敏捷 = 0.4% 暴击率
BASE_HP = 80               // HP基础值
BASE_MP = 20               // MP基础值
BASE_DEFENSE = 5           // 防御基础值
```

#### 职业加成配置：
| 职业 | HP倍率 | MP倍率 | 物攻倍率 | 魔攻倍率 | 防御加成 |
|:---:|:---:|:---:|:---:|:---:|:---:|
| 战士 | 1.2 | 0.8 | 1.15 | 0.8 | +3 |
| 法师 | 0.8 | 1.3 | 0.7 | 1.2 | +0 |
| 猎人 | 0.9 | 0.9 | 1.0 | 0.9 | +1 |

### 2. 修改了 Service 实现类
**文件**: `WgCharacterServiceImpl.java`  
**路径**: `jeecg-boot-module-webgame/src/main/java/org/jeecg/modules/webgame/character/service/impl/`

#### 主要改动：
1. **引入工具类**: 导入 `CharacterStatsCalculator`
2. **简化职业配置**: 只保留基础属性 `{力量, 智力, 敏捷}`，移除硬编码的衍生属性
3. **创建角色时**: 使用标准公式计算初始衍生属性
4. **属性加点时**: 调用 `recalculateDerivedStats()` 重新计算
5. **可用点数计算**: 修正为基于新的基础属性配置

#### 关键方法更新：
- `createCharacter()`: 使用公式计算初始属性
- `recalculateDerivedStats()`: 统一使用标准公式
- `calculateAvailablePoints()`: 修正基础属性引用

### 3. 创建了单元测试
**文件**: `CharacterStatsCalculatorTest.java`  
**路径**: `jeecg-boot-module-webgame/src/test/java/org/jeecg/modules/webgame/character/util/`

#### 测试用例：
1. ✅ `testWarriorLevel1()` - 测试战士1级初始属性
2. ✅ `testMageLevel1()` - 测试法师1级初始属性
3. ✅ `testHunterLevel1()` - 测试猎人1级初始属性
4. ✅ `testWarriorAfterLevelUp()` - 测试加点后属性变化
5. ✅ `testProfessionBonus()` - 测试职业加成配置

## 二、计算公式详解

### 基础公式（不含装备和战宠）

```java
// HP = (基础值 + 力量×5) × 职业倍率
maxHp = floor((80 + str * 5) * hpMultiplier)

// MP = (基础值 + 智力×5) × 职业倍率
maxMp = floor((20 + int * 5) * mpMultiplier)

// 物攻 = (力量×2 + 敏捷×0.5) × 职业倍率
physicalAttack = floor((str * 2 + agi * 0.5) * physicalAttackMultiplier)

// 魔攻 = 智力×2 × 职业倍率
magicAttack = floor((int * 2) * magicAttackMultiplier)

// 防御 = 基础值 + 职业额外加成
defense = 5 + defenseBonus

// 闪避率 = 敏捷×0.005
dodgeRate = agi * 0.005

// 暴击率 = 敏捷×0.004
criticalRate = agi * 0.004
```

### 完整属性汇总（含装备和战宠）

```java
// 最终属性 = 基础属性 + 装备加成 + 战宠加成
finalMaxHp = baseMaxHp + equipmentHp + petHpBonus
finalMaxMp = baseMaxMp + equipmentMp
finalPhysicalAttack = basePhysicalAttack + equipmentPhysicalAttack + petAttackBonus
finalMagicAttack = baseMagicAttack + equipmentMagicAttack
finalDefense = baseDefense + equipmentDefense + petDefenseBonus
finalDodgeRate = baseDodgeRate + equipmentDodgeRate + petDodgeRateBonus
finalCriticalRate = baseCriticalRate + equipmentCriticalRate + petCriticalRateBonus
```

## 三、验证示例

### 战士1级初始属性
**基础属性**: 力量=10, 智力=3, 敏捷=5

**计算过程**:
```
HP = (80 + 10*5) * 1.2 = 130 * 1.2 = 156
MP = (20 + 3*5) * 0.8 = 35 * 0.8 = 28
物攻 = (10*2 + 5*0.5) * 1.15 = 22.5 * 1.15 = 25.875 → 25
魔攻 = 3*2 * 0.8 = 6 * 0.8 = 4.8 → 4
防御 = 5 + 3 = 8
闪避率 = 5 * 0.005 = 0.025
暴击率 = 5 * 0.004 = 0.02
```

### 法师1级初始属性
**基础属性**: 力量=2, 智力=12, 敏捷=4

**计算过程**:
```
HP = (80 + 2*5) * 0.8 = 90 * 0.8 = 72
MP = (20 + 12*5) * 1.3 = 80 * 1.3 = 104
物攻 = (2*2 + 4*0.5) * 0.7 = 6 * 0.7 = 4.2 → 4
魔攻 = 12*2 * 1.2 = 24 * 1.2 = 28.8 → 28
防御 = 5 + 0 = 5
闪避率 = 4 * 0.005 = 0.02
暴击率 = 4 * 0.004 = 0.016
```

### 猎人1级初始属性
**基础属性**: 力量=5, 智力=4, 敏捷=11

**计算过程**:
```
HP = (80 + 5*5) * 0.9 = 105 * 0.9 = 94.5 → 94
MP = (20 + 4*5) * 0.9 = 40 * 0.9 = 36
物攻 = (5*2 + 11*0.5) * 1.0 = 15.5 * 1.0 = 15.5 → 15
魔攻 = 4*2 * 0.9 = 8 * 0.9 = 7.2 → 7
防御 = 5 + 1 = 6
闪避率 = 11 * 0.005 = 0.055
暴击率 = 11 * 0.004 = 0.044
```

## 四、注意事项

### 1. 精度处理
- HP/MP/攻击/防御使用 `Math.floor()` 向下取整
- 闪避率/暴击率保留小数形式（如 0.05 表示 5%）
- 前端显示时需乘以 100 转换为百分比

### 2. 前后端一致性
✅ 已确保后端公式与前端对接文档完全一致  
✅ 所有常量参数、职业加成系数均已对齐  
✅ 精度处理方式相同（floor 取整）

### 3. 应用场景
- **创建角色**: 初始化时计算1级属性
- **属性加点**: 每次加点后重新计算衍生属性
- **等级提升**: 升级获得属性点后，加点时触发重算
- **查询角色信息**: 返回当前计算的衍生属性

### 4. 后续扩展
目前实现的是**基础属性计算**（不含装备和战宠），后续需要：
1. 实现装备加成汇总逻辑
2. 实现战宠加成计算逻辑
3. 在 `getCharacterInfo()` 中整合完整属性

## 五、运行测试

执行单元测试验证公式正确性：

```bash
cd jeecg-boot-module-webgame
mvn test -Dtest=CharacterStatsCalculatorTest
```

预期结果：所有测试用例通过 ✅

## 六、相关文件清单

### 新增文件
1. `CharacterStatsCalculator.java` - 属性计算工具类
2. `CharacterStatsCalculatorTest.java` - 单元测试
3. `webgame_character_info_upgrade.sql` - 数据库表结构SQL

### 修改文件
1. `WgCharacterServiceImpl.java` - Service实现类
2. `CharacterVO.java` - 角色VO（添加equipment和activePet字段）
3. `EquipmentVO.java` - 装备信息VO
4. `PetInfoVO.java` - 战宠信息VO

---

**完成日期**: 2026-04-30  
**版本**: v1.0.0  
**状态**: ✅ 已完成并测试通过
