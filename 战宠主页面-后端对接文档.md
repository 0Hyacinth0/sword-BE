# 战宠主页面 - 后端对接文档

> 版本：v1.0.0
> 更新日期：2026-05-06
> 说明：定义战宠列表和出战切换接口

---

## 功能说明

展示角色拥有的所有战宠（最多 3 只），标记当前出战战宠，支持切换出战。每只战宠显示头像、昵称、等级、品质边框。

---

## 接口定义

### GET `/pet/list/{characterId}` — 获取战宠列表

**响应数据：**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "pets": [
      {
        "id": "pet-uuid-001",
        "petTypeId": 1001,
        "nickname": "小火焰",
        "level": 10,
        "exp": 8500,
        "maxExp": 10000,
        "rarity": 3,
        "isActive": true,
        "stats": { "hp": 180, "maxHp": 180, "attack": 25, "defense": 12, "speed": 18 },
        "bonusToOwner": { "hp": 18, "attack": 3, "defense": 1 }
      }
    ],
    "capacity": { "max": 3, "current": 1 }
  }
}
```

### POST `/pet/set-active` — 设置出战战宠

**请求参数：**
```json
{
  "characterId": "uuid",
  "petId": "pet-uuid-001"
}
```

**后端校验逻辑：**

1. 校验角色存在
2. 校验战宠属于该角色
3. 将所有战宠的 `isActive` 设为 false，目标战宠设为 true
4. 重新计算出战战宠给主人的属性加成
5. 返回更新后的战宠列表

**响应数据：** 同 `/pet/list` 格式

---

## 数据结构

### PetInfo（战宠实例）

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `id` | String (UUID) | 战宠实例唯一标识 |
| `petTypeId` | Integer | 战宠类型 ID |
| `nickname` | String | 战宠昵称 |
| `level` | Integer | 等级 |
| `exp` | Integer | 当前经验 |
| `maxExp` | Integer | 升级所需经验 |
| `rarity` | Integer (1-4) | 稀有度（N/R/SR/SSR） |
| `isActive` | Boolean | 是否出战 |
| `stats` | PetStats | 属性（HP/攻击/防御/速度） |
| `bonusToOwner` | PetOwnerBonus | 给主人的属性加成 |

### PetCapacity（容量信息）

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `max` | Integer | 最大容量（3） |
| `current` | Integer | 当前数量 |

---

## 稀有度对照

| 值 | 标签 | 颜色 |
|:---|:---|:---|
| 1 | N | 灰色 `#8e8e93` |
| 2 | R | 蓝色 `#0071e3` |
| 3 | SR | 紫色 `#af52de` |
| 4 | SSR | 金色 `#ff9500` |

---

如有疑问请联系前端对接人。
