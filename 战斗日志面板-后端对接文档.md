# 战斗日志面板 - 后端对接文档

## 概述

战斗日志面板用于实时展示战斗过程中的所有事件记录。后端需要确保每次战斗状态变化时生成完整的日志条目。本文档定义日志条目的数据结构、类型规范和接口约定。

---

## 日志条目数据结构

### BattleLogEntry

```typescript
interface BattleLogEntry {
  /** 回合数 */
  round: number
  /** 日志类型 */
  type: LogEntryType
  /** 日志内容（人类可读的描述文本） */
  message: string
  /** 相关行动者 UID（可选） */
  actorUid?: string
  /** 相关目标 UID（可选） */
  targetUid?: string
  /** 时间戳（毫秒） */
  timestamp: number
}
```

### LogEntryType 枚举

| 类型 | 标签 | 图标 | 颜色 | 说明 |
|:---|:---|:---|:---|:---|
| `system` | 系统 | 📢 | 灰色（斜体） | 回合开始、战斗结束等系统消息 |
| `action` | 行动 | ⚡ | 默认色 | 防御、逃跑等一般行动 |
| `damage` | 伤害 | ⚔ | 红色 | 普通伤害 |
| `critical` | 暴击 | 💥 | 金色（加粗） | 暴击伤害 |
| `dodge` | 闪避 | 💨 | 灰色 | 目标闪避 |
| `miss` | 未命中 | ❌ | 灰色 | 攻击未命中 |
| `heal` | 治疗 | 💚 | 绿色 | 恢复生命 |
| `buff` | 增益 | ✨ | 紫色 | Buff/Debuff 施加/消失 |
| `death` | 阵亡 | 💀 | 红色（加粗） | 单位死亡 |
| `flee` | 逃跑 | 🏃 | 金色 | 逃跑成功/失败 |
| `reward` | 奖励 | 🎁 | 绿色（加粗） | 战斗奖励 |

---

## 日志生成时机

后端在以下时机必须生成日志条目：

| 时机 | type | message 格式 | actorUid | targetUid |
|:---|:---|:---|:---|:---|
| 战斗开始 | `system` | `"战斗开始！"` | — | — |
| 回合开始 | `system` | `"—— 第 N 回合 ——"` | — | — |
| 普通攻击命中 | `damage` | `"{攻击者} 攻击 {目标}，造成 {伤害} 点伤害"` | 攻击者 uid | 目标 uid |
| 暴击 | `critical` | `"{攻击者} 攻击 {目标}，暴击！造成 {伤害} 点伤害（克制！）"` | 攻击者 uid | 目标 uid |
| 闪避 | `dodge` | `"{目标} 闪避了 {攻击者} 的攻击！"` | 攻击者 uid | 目标 uid |
| 技能攻击 | `damage`/`critical` | `"{攻击者} 使用 {技能名} 对 {目标} 造成 {伤害} 点伤害"` | 攻击者 uid | 目标 uid |
| 治疗 | `heal` | `"{施法者} 使用 {技能名}，恢复 {目标} {治疗量} HP"` | 施法者 uid | 目标 uid |
| Buff 施加 | `buff` | `"{目标} 获得了 {Buff名} 效果（增益/减益，{回合} 回合）"` | — | 目标 uid |
| Buff 消失 | `buff` | `"{目标} 的 {Buff名} 效果消失了"` | — | 目标 uid |
| 单位死亡 | `death` | `"{死亡者} 被击败了！"` | — | 死亡者 uid |
| 防御 | `action` | `"{行动者} 进入防御姿态"` | 行动者 uid | — |
| 逃跑成功 | `flee` | `"{行动者} 成功逃离了战斗！"` | 行动者 uid | — |
| 逃跑失败 | `flee` | `"{行动者} 逃跑失败！"` | 行动者 uid | — |
| DoT 伤害 | `damage` | `"{目标} 受到 {Buff名} 的效果，损失 {伤害} HP"` | — | 目标 uid |
| MP 不足 | `system` | `"{行动者} 的 MP 不足，无法使用 {技能名}"` | 行动者 uid | — |
| 技能冷却中 | `system` | `"{技能名} 冷却中，还需 {回合} 回合"` | 行动者 uid | — |
| 战宠阵亡 | `system` | `"{战宠} 阵亡，{主人} 失去了战宠属性加成！"` | 战宠 uid | 主人 uid |

---

## 日志筛选分类

前端支持 5 个筛选标签，后端无需额外处理（前端基于 type 字段自行筛选）：

| 筛选标签 | 包含的 LogEntryType |
|:---|:---|
| 全部 | 所有类型 |
| 伤害 | `damage`, `critical`, `dodge`, `miss` |
| 治疗 | `heal` |
| Buff | `buff` |
| 系统 | `system`, `action`, `death`, `flee`, `reward` |

---

## 接口约定

### 日志数据来源

日志条目存储在 `BattleState.log` 数组中，随着战斗推进不断追加。

**前端获取方式**：
- 每次提交行动后，`BattleState` 整体返回，前端从 `state.log` 读取日志
- 无需单独的日志查询接口

### 后端要求

1. **日志完整性**：每个战斗事件必须生成对应的日志条目，不能遗漏
2. **actorUid / targetUid**：必须与 `Combatant.uid` 一致，前端可能用于关联单位
3. **message 格式**：保持一致的人类可读格式，包含关键数值（伤害量、治疗量等）
4. **timestamp**：使用服务器时间戳（毫秒），前端用于可选的时间显示
5. **round**：与 `BattleState.round` 一致
6. **日志顺序**：`log` 数组中的条目按时间顺序排列（先发生的事件在前面）

---

## 日志复制格式

用户可点击"复制日志"按钮导出日志，格式如下：

```
[R1] [系统] 战斗开始！
[R1] [系统] —— 第 1 回合 ——
[R1] [伤害] 勇者 攻击 暗影狼，造成 25 点伤害
[R1] [暴击] 小火焰 使用 烈焰冲击 对 哥布林盗贼 造成 48 点伤害（暴击！）
[R1] [伤害] 暗影狼 攻击 勇者，造成 12 点伤害
[R2] [系统] —— 第 2 回合 ——
```

---

## 前端实现文件

| 文件 | 职责 |
|:---|:---|
| `src/components/battle/BattleLog.vue` | 日志面板组件（显示、筛选、分组、复制） |
| `src/types/battle.ts` | BattleLogEntry / LogEntryType 类型定义 |
| `src/views/BattleView.vue` | 传递 entries 和 currentRound 给 BattleLog |

---

## 日志面板功能清单

| 功能 | 说明 |
|:---|:---|
| 类型着色 | 11 种日志类型各有独立颜色和样式 |
| 类型标签 | 每条日志显示类型标签（系统/伤害/暴击等） |
| 类型图标 | 每条日志前显示对应图标 |
| 回合分组 | 按回合分组显示，回合之间有分隔线 |
| 回合标签 | 非分组模式每条日志前显示 R1/R2 回合号 |
| 类型筛选 | 5 个筛选标签：全部/伤害/治疗/Buff/系统 |
| 筛选计数 | 每个筛选标签显示匹配的日志条数 |
| 时间戳切换 | 可切换显示 mm:ss 时间戳 |
| 分组切换 | 可切换是否按回合分组 |
| 复制日志 | 一键复制全部日志到剪贴板 |
| 复制提示 | 复制成功后显示 toast 提示 |
| 自动滚动 | 新日志到达时自动滚动到底部 |
| 空状态 | 无日志和无匹配日志两种空状态提示 |
| 响应式 | 移动端隐藏时间戳和筛选图标，调整高度 |

---

*文档版本: 1.0.0*
*最后更新: 2026-05-07*
