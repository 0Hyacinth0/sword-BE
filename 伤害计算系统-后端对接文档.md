# 伤害计算系统 - 后端对接文档

## 概述

本文档定义回合制战斗中完整的伤害计算公式体系，包含：物理/魔法攻击、防御减免、暴击、闪避、元素克制。前后端公式必须完全一致。

---

## 伤害计算流程

```
1. 计算攻击力和防御力（含 Buff/Debuff 修正）
2. 基础伤害 = floor(攻击力 × 技能倍率) - floor(防御力 × 0.5)
3. 随机波动 = max(1, floor(基础伤害 × (1 ± 10%)))
4. 元素克制 = floor(波动伤害 × 克制倍率)
5. 暴击判定 = floor(克制伤害 × 1.5)  （暴击时）
6. 闪避判定 = 0  （闪避时）
7. 最终伤害 = 以上结果
```

---

## 核心公式

### 1. 属性修正（Buff/Debuff）

```
effectiveStat = baseStat + Σ(buffValue) + Σ(debuffValue)
// debuffValue 为负数，最低不低于 0
```

### 2. 基础伤害

```
baseDamage = max(0, floor(attacker.physicalAttack × skillMultiplier) - floor(defender.defense × 0.5))
```

| 参数 | 说明 |
|------|------|
| physicalAttack | 攻击者物理攻击（含 Buff） |
| skillMultiplier | 技能倍率（普通攻击 = 1.0，技能 = power/100） |
| defense | 防御者防御力（含 Buff） |
| 0.5 | 防御有效系数（防御只抵消 50%） |

### 3. 随机波动

```
variance = 1 + random(-0.1, 0.1)
damageAfterVariance = max(1, floor(baseDamage × variance))
```

### 4. 元素克制

```
elementMultiplier = {
  1.3   // 攻击者克制防御者
  0.7   // 攻击者被克制
  1.0   // 无克制关系或无元素
}
damageAfterElement = max(1, floor(damageAfterVariance × elementMultiplier))
```

**克制关系表：**

| 攻击 → 防御 | 关系 |
|-------------|------|
| 火 → 风 | 克制 |
| 风 → 地 | 克制 |
| 地 → 水 | 克制 |
| 水 → 火 | 克制 |
| 光 ↔ 暗 | 互克 |

### 5. 暴击判定

```
critRate = min(attacker.criticalRate, 0.8)  // 上限 80%
isCritical = random() < critRate
critMultiplier = isCritical ? 1.5 : 1.0
damageAfterCrit = max(1, floor(damageAfterElement × critMultiplier))
```

### 6. 闪避判定

```
dodgeRate = min(defender.dodgeRate, 0.8)  // 上限 80%
isDodged = random() < dodgeRate
finalDamage = isDodged ? 0 : damageAfterCrit
```

---

## 治疗量计算

```
baseHeal = floor(caster.magicAttack × skillMultiplier)
variance = 1 + random(-0.1, 0.1)
healAmount = max(1, floor(baseHeal × variance))
actualHeal = min(healAmount, target.maxHp - target.hp)
```

---

## 常量参数汇总

| 常量 | 值 | 说明 |
|------|----|------|
| DEFENSE_EFFICIENCY | 0.5 | 防御有效系数 |
| DAMAGE_VARIANCE | 0.1 | 伤害波动范围 (±10%) |
| MIN_DAMAGE | 1 | 最低伤害保底 |
| CRIT_MULTIPLIER | 1.5 | 暴击伤害倍率 |
| MAX_CRIT_RATE | 0.8 | 暴击率上限 |
| MAX_DODGE_RATE | 0.8 | 闪避率上限 |
| ADVANTAGE_MULTIPLIER | 1.3 | 克制伤害倍率 |
| DISADVANTAGE_MULTIPLIER | 0.7 | 被克制伤害倍率 |

---

## 伤害计算明细类型 (DamageBreakdown)

后端在战斗日志或结算中可返回此明细，用于前端展示：

```typescript
interface DamageBreakdown {
  attackerRawAttack: number       // 攻击者原始攻击力
  skillMultiplier: number         // 技能倍率
  attackerBuffedAttack: number    // Buff 加成后攻击力
  defenderRawDefense: number      // 防御者原始防御力
  defenderBuffedDefense: number   // Buff 加成后防御力
  effectiveDefense: number        // 有效防御 (defense × 0.5)
  baseDamage: number              // 基础伤害
  varianceFactor: number          // 随机波动系数
  damageAfterVariance: number     // 波动后伤害
  elementAdvantage: 1 | -1 | 0   // 元素克制关系
  elementMultiplier: number       // 元素倍率
  damageAfterElement: number      // 元素调整后伤害
  isCritical: boolean             // 是否暴击
  critMultiplier: number          // 暴击倍率
  damageAfterCrit: number         // 暴击后伤害
  isDodged: boolean               // 是否闪避
  finalDamage: number             // 最终伤害
}
```

---

## 元素编号

| 编号 | 元素 | 颜色 |
|------|------|------|
| 0 | 无 | - |
| 1 | 火 | #ff6b35 |
| 2 | 水 | #3b82f6 |
| 3 | 风 | #22c55e |
| 4 | 地 | #a16207 |
| 5 | 光 | #eab308 |
| 6 | 暗 | #7c3aed |

---

## Java 伪代码

```java
public DamageBreakdown calculateDamage(CombatantStats attacker, CombatantStats defender,
                                        double skillMultiplier, int attackerElement, int defenderElement) {
    DamageBreakdown result = new DamageBreakdown();

    // 1. 基础伤害
    int baseDamage = Math.max(0,
        (int)(attacker.physicalAttack * skillMultiplier) -
        (int)(defender.defense * 0.5));

    // 2. 随机波动
    double variance = 1 + (Math.random() * 2 - 1) * 0.1;
    int damageAfterVariance = Math.max(1, (int)(baseDamage * variance));

    // 3. 元素克制
    int advantage = getElementAdvantage(attackerElement, defenderElement);
    double elementMultiplier = advantage == 1 ? 1.3 : advantage == -1 ? 0.7 : 1.0;
    int damageAfterElement = Math.max(1, (int)(damageAfterVariance * elementMultiplier));

    // 4. 暴击
    double critRate = Math.min(attacker.criticalRate, 0.8);
    boolean isCritical = Math.random() < critRate;
    double critMultiplier = isCritical ? 1.5 : 1.0;
    int damageAfterCrit = isCritical ?
        Math.max(1, (int)(damageAfterElement * 1.5)) : damageAfterElement;

    // 5. 闪避
    double dodgeRate = Math.min(defender.dodgeRate, 0.8);
    boolean isDodged = Math.random() < dodgeRate;
    int finalDamage = isDodged ? 0 : damageAfterCrit;

    // ... 填充 result 并返回
    return result;
}
```

---

## 注意事项

1. **公式一致性**：前后端伤害计算必须得到相同结果（相同输入 → 相同输出，随机部分除外）
2. **保底伤害**：任何情况下最终伤害最低为 1（闪避除外）
3. **百分比上限**：暴击率和闪避率上限均为 80%，防止极端属性导致必暴/必闪
4. **元素为0**：元素编号 0 表示无元素，不参与克制计算
5. **防御穿透**：当前版本未实现，预留扩展空间
