# 战宠技能槽位管理 - 后端对接文档

> 版本：v1.0.0
> 更新日期：2026-05-06
> 说明：定义战宠技能装备/卸下接口

---

## 功能说明

每只战宠拥有 3 个技能槽位，可将已学会的技能装备到槽位中。战斗时战宠使用已装备的技能。支持装备和卸下操作。

- 战宠根据等级自动学会技能（`learn_level` 条件）
- 进化时可习得进化专属技能（`learn_level = 0`）
- 已学会的技能可自由装备到 3 个槽位
- 同一技能不可重复装备到多个槽位

---

## 接口定义

### POST `/pet/equip-skill` — 装备技能到槽位

将已学会的技能装备到指定槽位。如果槽位已有技能，则替换。

**请求参数：**
```json
{
  "characterId": "uuid",
  "petId": "pet-uuid-001",
  "skillId": 1,
  "slotIndex": 0
}
```

| 字段 | 类型 | 必填 | 说明 |
|:---|:---|:---:|:---|
| `characterId` | String (UUID) | 是 | 角色 ID |
| `petId` | String (UUID) | 是 | 战宠实例 ID |
| `skillId` | Integer | 是 | 要装备的技能 ID |
| `slotIndex` | Integer | 是 | 槽位索引（0/1/2） |

**后端校验逻辑：**

1. 校验角色存在
2. 校验战宠属于该角色
3. 校验 `slotIndex` 在 0-2 范围内
4. 查询 `pet_learned_skills` 表确认该战宠已学会此技能
5. 校验该技能未装备在其他槽位（同技能不可重复）
6. 更新 `character_pets` 表的 `skill_slot_{slotIndex+1}` 字段为 `skillId`
7. 返回更新后的战宠列表

**响应数据：** 同 `/pet/list` 格式

---

### POST `/pet/unequip-skill` — 卸下技能

将指定槽位的技能卸下。

**请求参数：**
```json
{
  "characterId": "uuid",
  "petId": "pet-uuid-001",
  "slotIndex": 0
}
```

| 字段 | 类型 | 必填 | 说明 |
|:---|:---|:---:|:---|
| `characterId` | String (UUID) | 是 | 角色 ID |
| `petId` | String (UUID) | 是 | 战宠实例 ID |
| `slotIndex` | Integer | 是 | 槽位索引（0/1/2） |

**后端校验逻辑：**

1. 校验角色存在
2. 校验战宠属于该角色
3. 校验 `slotIndex` 在 0-2 范围内
4. 校验该槽位有技能（不为 null）
5. 更新 `character_pets` 表的 `skill_slot_{slotIndex+1}` 字段为 null
6. 返回更新后的战宠列表

**响应数据：** 同 `/pet/list` 格式

---

## 数据结构

### 技能槽位存储

`character_pets` 表中：

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `skill_slot_1` | Integer (FK → pet_skills) | 槽位 1，可 null |
| `skill_slot_2` | Integer (FK → pet_skills) | 槽位 2，可 null |
| `skill_slot_3` | Integer (FK → pet_skills) | 槽位 3，可 null |

### 已学技能判断

后端通过以下逻辑确定战宠已学会的技能列表：

1. 查询 `pet_types` 表获取该战宠类型的 `pet_type_id`
2. 查询 `pet_skills` 表，筛选 `pet_type_ids` 包含该 `pet_type_id` 的技能
3. 过滤已满足等级条件：`learn_level > 0 AND learn_level <= pet.level` 或 `learn_level = 0`（进化专属，仅进化后可学）
4. 结果即为该战宠已学会的技能

### PetInfo 返回格式扩展

```json
{
  "skills": [
    { "id": 1, "name": "火球术", "type": "active_attack", "power": 120, "cooldown": 2 }
  ],
  "learnedSkills": [
    { "id": 1, "name": "火球术", "type": "active_attack", "power": 120, "cooldown": 2, "learnLevel": 1 },
    { "id": 2, "name": "灼烧", "type": "passive", "learnLevel": 5 },
    { "id": 3, "name": "火焰护盾", "type": "active_support", "cooldown": 3, "learnLevel": 8 }
  ]
}
```

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `skills` | PetSkill[] | 当前已装备的技能（最多 3 个，对应 3 个槽位） |
| `learnedSkills` | PetSkill[] | 已学会的全部技能（含 learnLevel 信息） |

---

## 前端交互流程

1. 用户点击战宠卡片展开详情面板
2. 切换到"技能"标签页
3. 显示 3 个技能槽位（已装备显示技能名，空显示"空槽位"）
4. 下方显示已学技能总数和列表
5. 点击"装备"/"替换"按钮，弹出技能选择面板
6. 选择面板中列出已学但未装备的技能
7. 点击技能完成装备，调用 `POST /pet/equip-skill`
8. 点击"卸下"按钮卸下技能，调用 `POST /pet/unequip-skill`

---

## 技能学习规则

| 条件 | 说明 |
|:---|:---|
| 等级达标 | 战宠等级 ≥ 技能的 `learn_level` 时自动学会 |
| 进化专属 | `learn_level = 0` 的技能仅进化后可学 |
| 类型匹配 | 技能的 `pet_type_ids` 包含当前战宠类型 |

---

如有疑问请联系前端对接人。
