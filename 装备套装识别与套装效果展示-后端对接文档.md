# 套装识别与套装效果展示 - 后端对接文档

> 版本：v1.0.0
> 更新日期：2026-05-06
> 说明：套装效果为纯前端计算展示功能，后端只需提供套装基础数据

---

## 功能说明

当玩家装备多件同套装装备时，达到指定件数（2件/4件等）激活套装属性加成效果，在装备栏下方展示套装进度和激活效果。

---

## 数据来源

套装所需数据均来自现有接口返回：

| 数据 | 来源 | 字段 |
|:---|:---|:---|
| 装备套装ID | 角色装备详情 | `Equipment.setId` |
| 装备套装名称 | 角色装备详情 | `Equipment.setName` |

---

## 前端计算逻辑

1. 遍历六槽位装备 `character.equipment`
2. 统计各 `setId` 的已装备数量
3. 根据套装配置表判断激活效果
4. 生成 `SetBonus` 数据传递给 UI 展示

---

## 套装配置表

前端维护套装效果配置（静态配置表），后端无需提供：

```typescript
interface SetConfig {
  setId: string          // 套装 ID
  setName: string        // 套装名称
  totalCount: number     // 套装总件数
  bonuses: Array<{
    requiredCount: number  // 需要装备件数（如 2件、4件）
    description: string    // 效果描述
    stats: EquipmentStats  // 属性加成
  }>
}
```

示例配置：
| 套装ID | 套装名称 | 总件数 | 2件效果 | 4件效果 |
|:---|:---|:---|:---|:---|
| `set-001` | 勇者之证 | 2 | 物攻+10, 生命+50 | - |
| `set-002` | 冰霜之心 | 3 | 魔攻+8, 魔力+40 | 智力+5, 暴击+3% |

---

## UI 展示

**套装效果区域**（装备栏下方）：
- 套装名称 + 已装备数量（如 `冰霜之心 2/3`）
- 进度条显示收集进度
- 各档效果列表，已激活效果高亮显示

**装备详情卡片**：
- 显示装备所属套装名称

---

## 后端无需改动

此功能为纯前端计算展示，后端只需确保装备数据中包含 `setId` 和 `setName` 字段即可。套装效果配置由前端维护，便于灵活调整。

---

如有疑问请联系前端对接人。