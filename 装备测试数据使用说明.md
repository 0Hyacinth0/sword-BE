# 装备系统测试数据使用指南

> 用户名：123456  
> 创建日期：2026-05-09

---

## 📋 测试数据说明

已创建完整的装备系统测试数据，包含：
- ✅ 9件装备物品模板（武器、防具、饰品）
- ✅ 9件背包中的装备物品（含随机词条）
- ✅ 5件已穿戴的装备（含强化等级）

---

## 🔧 使用步骤

### 步骤1：查询用户和角色ID

首先在数据库中执行以下SQL，获取实际的UUID：

```sql
-- 查询用户ID
SELECT id FROM wg_user WHERE username = '123456';

-- 查询该用户的第一个角色ID
SELECT id FROM wg_character 
WHERE user_id = (SELECT id FROM wg_user WHERE username = '123456') 
LIMIT 1;
```

假设查询结果：
- 用户ID: `a1b2c3d4-e5f6-7890-abcd-ef1234567890`
- 角色ID: `b2c3d4e5-f6a7-8901-bcde-f12345678901`

### 步骤2：修改测试数据SQL

打开文件：`V3.9.2_8__test_equipment_data.sql`

将所有 `'char-test-uuid'` 替换为实际的角色ID，例如：
```sql
-- 修改前
(UUID(), 'char-test-uuid', 'equip-weapon-001', 1, NOW(), ...)

-- 修改后
(UUID(), 'b2c3d4e5-f6a7-8901-bcde-f12345678901', 'equip-weapon-001', 1, NOW(), ...)
```

**提示：** 可以使用文本编辑器的"查找替换"功能批量替换。

### 步骤3：执行SQL脚本

在MySQL客户端中执行修改后的SQL脚本：

```bash
mysql -u root -p web-game < V3.9.2_8__test_equipment_data.sql
```

或者直接在数据库管理工具（如 Navicat、DataGrip）中运行。

---

## 📦 测试数据详情

### 装备物品模板（wg_item_template）

| 物品ID | 名称 | 类型 | 槽位 | 稀有度 | 等级需求 | 基础属性 |
|:---|:---|:---|:---|:---|:---:|:---|
| equip-weapon-001 | 精钢长剑 | 装备 | weapon | Rare | 5 | 物攻+15, 力量+3 |
| equip-weapon-002 | 法师法杖 | 装备 | weapon | Epic | 10 | 魔攻+20, 智力+5 |
| equip-weapon-003 | 传说之剑 | 装备 | weapon | Legendary | 20 | 物攻+35, 力量+8, 暴击+5% |
| equip-helmet-001 | 铁盔 | 装备 | helmet | Normal | 1 | 防御+3, 生命+20 |
| equip-chest-001 | 铁甲胸铠 | 装备 | chest | Normal | 1 | 防御+8, 生命+50 |
| equip-chest-002 | 史诗铠甲 | 装备 | chest | Epic | 15 | 防御+18, 生命+120, 魔力+50 |
| equip-legs-001 | 战靴 | 装备 | legs | Normal | 1 | 防御+4, 敏捷+2 |
| equip-accessory-001 | 力量戒指 | 装备 | accessory1 | Rare | 8 | 力量+5, 物攻+5 |
| equip-accessory-002 | 智力项链 | 装备 | accessory2 | Rare | 8 | 智力+6, 魔攻+8 |

### 背包物品（wg_character_inventory）

共9件装备在背包中，其中：
- **普通品质**：无随机词条
- **稀有品质**：1-2条随机词条
- **史诗品质**：3条随机词条
- **传说品质**：4条随机词条

示例（传说之剑的随机词条）：
```json
[
  {"key": "physicalAttack", "value": 8},
  {"key": "strength", "value": 5},
  {"key": "criticalRate", "value": 0.03},
  {"key": "agility", "value": 4}
]
```

### 已穿戴装备（wg_character_equipments）

| 槽位 | 装备名称 | 强化等级 | 随机词条 |
|:---|:---|:---:|:---|
| weapon | 精钢长剑 | +0 | 敏捷+2, 闪避+2% |
| helmet | 铁盔 | +0 | 无 |
| chest | 铁甲胸铠 | **+2** | 无 |
| legs | 战靴 | +0 | 无 |
| accessory1 | 力量戒指 | +0 | 力量+3 |
| accessory2 | *(空)* | - | - |

---

## 🧪 测试场景

### 场景1：查看角色装备列表

```bash
GET /webgame/equipment/list/{characterId}
Header: X-Access-Token: {token}
```

**预期结果：**
- 返回6个槽位的装备信息
- weapon、helmet、chest、legs、accessory1 有装备
- accessory2 为 null

### 场景2：从背包装备新装备

```bash
POST /webgame/equipment/equip
Header: X-Access-Token: {token}
Body:
{
  "characterId": "{角色ID}",
  "inventoryId": "{背包物品ID}",
  "slotType": "accessory2"
}
```

**预期结果：**
- 智力项链穿戴到 accessory2 槽位
- 背包中该物品消失

### 场景3：卸下装备

```bash
POST /webgame/equipment/unequip
Header: X-Access-Token: {token}
Body:
{
  "characterId": "{角色ID}",
  "slotType": "weapon"
}
```

**预期结果：**
- 精钢长剑从装备栏移除
- 精钢长剑回到背包

### 场景4：替换装备

```bash
POST /webgame/equipment/equip
Header: X-Access-Token: {token}
Body:
{
  "characterId": "{角色ID}",
  "inventoryId": "{法师法杖的背包ID}",
  "slotType": "weapon"
}
```

**预期结果：**
- 如果weapon槽位已有装备，旧装备自动回到背包
- 法师法杖穿戴到weapon槽位

### 场景5：强化装备

```bash
POST /webgame/equipment/enhance
Header: X-Access-Token: {token}
Body:
{
  "characterId": "{角色ID}",
  "slotType": "chest"
}
```

**预期结果：**
- 铁甲胸铠从 +2 强化到 +3（成功率100%）
- 返回强化结果

### 场景6：等级不足测试

尝试穿戴需要20级的"传说之剑"到1级角色上：

```bash
POST /webgame/equipment/equip
Body:
{
  "characterId": "{角色ID}",
  "inventoryId": "{传说之剑的背包ID}",
  "slotType": "weapon"
}
```

**预期结果：**
- 返回错误："等级不足，需要 20 级才能穿戴该装备"

### 场景7：背包容量限制测试

当背包已有32种物品时，尝试卸下装备：

```bash
POST /webgame/equipment/unequip
Body:
{
  "characterId": "{角色ID}",
  "slotType": "weapon"
}
```

**预期结果：**
- 返回错误："背包已满（32/32），无法卸下装备"

---

## 📊 验证SQL

执行以下SQL验证数据是否正确插入：

```sql
-- 1. 查看装备模板
SELECT item_id, name, type, slot_type, rarity, level_requirement 
FROM wg_item_template 
WHERE item_id LIKE 'equip-%'
ORDER BY item_id;

-- 2. 查看角色背包中的装备
SELECT ci.id, it.name, ci.quantity, ci.extra_stats
FROM wg_character_inventory ci
LEFT JOIN wg_item_template it ON ci.item_id = it.item_id
WHERE ci.character_id = '{角色ID}'
ORDER BY ci.create_time DESC;

-- 3. 查看角色已穿戴的装备
SELECT ce.slot_type, it.name, ce.enhance_level, ce.extra_stats
FROM wg_character_equipments ce
LEFT JOIN wg_item_template it ON ce.item_id = it.item_id
WHERE ce.character_id = '{角色ID}'
ORDER BY ce.slot_type;
```

---

## ⚠️ 注意事项

1. **UUID替换**：务必将SQL中的所有 `'char-test-uuid'` 替换为实际的角色ID
2. **事务安全**：所有INSERT操作都在同一事务中，确保数据一致性
3. **唯一约束**：`wg_character_equipments` 表的 `(character_id, slot_type)` 有唯一约束，每个槽位只能有一件装备
4. **外键约束**：确保角色ID在 `wg_character` 表中存在
5. **Flyway迁移**：如果启用了Flyway，脚本会自动执行；如果禁用，需手动执行

---

## 🎯 测试检查清单

- [ ] 装备模板正确插入（9件）
- [ ] 背包物品正确插入（9件）
- [ ] 已穿戴装备正确插入（5件）
- [ ] 随机词条JSON格式正确
- [ ] 等级需求字段有值
- [ ] 可以通过API查询装备列表
- [ ] 可以正常穿戴/卸下装备
- [ ] 等级校验生效
- [ ] 背包容量限制生效
- [ ] 强化功能正常

---

如有问题请查看日志或联系后端开发人员。
