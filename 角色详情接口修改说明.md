# 角色详情接口修改说明

## 修改概述

根据《角色详情-后端对接文档.md》的要求，对现有接口和数据库进行了以下修改：

---

## 一、接口修改

### 1. 属性加点接口路径变更

**原接口：**
- 路径：`POST /webgame/character/add-points/{characterId}`
- 参数：characterId（路径参数）+ AttributePointDTO（请求体）

**新接口：**
- 路径：`POST /webgame/character/update-attributes`
- 参数：AttributePointDTO（请求体，包含characterId字段）

**修改文件：**
- `WgCharacterController.java` - 更新接口路径和方法签名
- `IWgCharacterService.java` - 更新服务接口定义
- `WgCharacterServiceImpl.java` - 更新服务实现逻辑

### 2. DTO字段名称调整

**原字段名：**
- `strengthPoint` - 力量加点
- `intelligencePoint` - 智力加点
- `agilityPoint` - 敏捷加点

**新字段名（与前端文档对齐）：**
- `str` - 力量加点
- `intelligence` - 智力加点
- `agi` - 敏捷加点
- `characterId` - 新增角色ID字段

**修改文件：**
- `AttributePointDTO.java`

---

## 二、VO字段补充

### CharacterVO新增字段

为了符合前端对接文档要求，在CharacterVO中添加了以下字段：

1. **userId** - 用户ID
2. **createTime** - 创建时间
3. **updateTime** - 更新时间

**修改文件：**
- `CharacterVO.java`
- `WgCharacterServiceImpl.java` - 更新convertToVO方法

---

## 三、数据库表创建

创建了wg_character表的Flyway迁移脚本：

**文件位置：**
`jeecg-module-system/jeecg-system-start/src/main/resources/flyway/sql/mysql/V3.9.2_0__webgame_character.sql`

**表结构说明：**
- 主键：id (varchar(36))
- 用户关联：user_id (varchar(36))
- 角色信息：character_name, profession, level, experience
- 基础属性：strength, intelligence, agility
- 衍生属性：hp, mp, physical_attack, magic_attack, defense, dodge_rate, critical_rate
- 战宠加成：bonus_hp, bonus_physical_attack, bonus_magic_attack, bonus_defense
- 审计字段：create_time, update_time, del_flag
- 索引：user_id索引、character_name索引

---

## 四、接口响应格式

### 获取角色详情接口

**路径：** `GET /webgame/character/info/{characterId}`

**响应数据结构：**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": "uuid",
    "userId": "uuid",
    "characterName": "剑圣无名",
    "profession": 1,
    "professionName": "战士",
    "level": 15,
    "experience": 2340,
    "nextLevelExp": 3000,
    "availablePoints": 3,
    "strength": 10,
    "intelligence": 3,
    "agility": 5,
    "hp": 500,
    "maxHp": 500,
    "mp": 50,
    "maxMp": 50,
    "physicalAttack": 20,
    "magicAttack": 6,
    "defense": 15,
    "dodgeRate": 0.05,
    "criticalRate": 0.03,
    "createTime": "2026-04-20T08:00:00Z",
    "updateTime": "2026-04-28T10:00:00Z",
    "equipment": {...},
    "activePet": {...}
  }
}
```

### 属性加点接口

**路径：** `POST /webgame/character/update-attributes`

**请求体：**
```json
{
  "characterId": "uuid",
  "str": 2,
  "intelligence": 0,
  "agi": 1
}
```

**响应数据：**
```json
{
  "code": 200,
  "message": "属性加点成功",
  "data": {
    "id": "uuid",
    "strength": 12,
    "intelligence": 3,
    "agility": 6,
    "availablePoints": 0,
    "maxHp": 160,
    "maxMp": 35,
    "physicalAttack": 24,
    "magicAttack": 6,
    "dodgeRate": 0.06,
    "criticalRate": 0.048,
    "hp": 160,
    "mp": 35
  }
}
```

---

## 五、校验规则

属性加点接口实现了以下校验：

1. **角色ID校验**：characterId不能为空
2. **角色存在性校验**：角色必须存在且未删除
3. **加点数值校验**：各属性增加值必须为非负整数
4. **可用点数校验**：str + intelligence + agi 不能超过角色的availablePoints
5. **衍生属性重算**：加点后自动重新计算所有衍生属性

---

## 六、注意事项

1. **装备和战宠功能**：当前实现中，equipment和activePet字段返回null，需要后续实现装备系统和战宠系统来填充这些数据。

2. **属性计算公式**：使用CharacterStatsCalculator工具类进行属性计算，公式如下：
   - maxHp = (80 + strength × 5) × 职业倍率
   - maxMp = (20 + intelligence × 5) × 职业倍率
   - physicalAttack = (strength × 2 + agility × 0.5) × 职业倍率
   - magicAttack = (intelligence × 2) × 职业倍率
   - defense = 5 + 职业额外加成
   - dodgeRate = agility × 0.005
   - criticalRate = agility × 0.004

3. **职业基础属性**：
   - 战士：力量10、智力3、敏捷5
   - 法师：力量2、智力12、敏捷4
   - 猎人：力量5、智力4、敏捷11

4. **可用属性点计算**：
   - 可用点 = (等级 - 1) × 5 - 已分配点数
   - 已分配点数 = (当前力量 - 基础力量) + (当前智力 - 基础智力) + (当前敏捷 - 基础敏捷)

---

## 七、测试建议

1. 执行Flyway迁移脚本创建wg_character表
2. 测试创建角色接口
3. 测试获取角色详情接口，验证返回字段完整性
4. 测试属性加点接口，验证：
   - 正常加点场景
   - 可用点数不足场景
   - 负数加点场景
   - 角色不存在场景
5. 验证加点后衍生属性是否正确重算

---

*修改日期：2026-05-08*
*对应文档版本：角色详情-后端对接文档.md v2026-04-30*
