# 组队系统 - 后端对接文档

## 概述

本文档描述组队系统的前后端对接规范。组队系统是多人协作的基础，支持创建队伍、邀请好友、申请加入、成员管理等功能。

---

## 前端实现概览

| 文件 | 作用 |
|:---|:---|
| `src/types/team.ts` | 组队相关类型定义 |
| `src/api/team.ts` | 组队 API（Mock + Real） |
| `src/stores/team.ts` | 组队状态管理 |
| `src/components/team/TeamPanel.vue` | 组队面板主组件 |

---

## 数据结构

### TeamMember（队伍成员）

```typescript
interface TeamMember {
  characterId: string      // 成员角色 ID
  characterName: string    // 角色名
  profession: string       // 职业（Warrior/Mage/Hunter）
  level: number            // 等级
  role: TeamRole           // 角色：leader/member
  status: 'online' | 'offline' | 'ready'  // 在线状态
  joinedAt: string         // 加入时间
}
```

### TeamInfo（队伍信息）

```typescript
interface TeamInfo {
  id: string               // 队伍 ID
  leaderId: string         // 队长角色 ID
  leaderName: string       // 队长角色名
  members: TeamMember[]    // 成员列表
  maxMembers: number       // 最大人数（默认 4）
  status: TeamStatus       // 队伍状态
  targetDungeon?: string   // 目标副本（可选）
  createdAt: string        // 创建时间
}
```

### TeamApplication（入队申请）

```typescript
interface TeamApplication {
  id: string               // 申请 ID
  teamId: string           // 队伍 ID
  applicantId: string      // 申请者角色 ID
  applicantName: string    // 申请者角色名
  applicantProfession: string  // 申请者职业
  applicantLevel: number   // 申请者等级
  status: ApplicationStatus    // 申请状态
  createdAt: string        // 申请时间
}
```

### TeamStatus（队伍状态）

```typescript
type TeamStatus = 'open' | 'closed' | 'in_dungeon'
```

- `open`：招募中，其他玩家可申请加入
- `closed`：已关闭，不再接受申请
- `in_dungeon`：副本中，队伍正在挑战副本

### TeamRole（成员角色）

```typescript
type TeamRole = 'leader' | 'member'
```

---

## 接口规范

### Base URL

```
/jeecg-boot/webgame
```

### 认证方式

- 采用 JWT 认证
- 请求头携带：`Authorization: Bearer <token>`
- 请求拦截器自动注入 `userId` 和 `characterId`

---

### 1. 创建队伍

**POST `/team/create`**

创建一支新队伍，创建者自动成为队长。

**请求参数**：无（自动使用当前角色）

**响应示例**：
```json
{
  "code": 200,
  "message": "队伍创建成功",
  "data": {
    "id": "team-uuid",
    "leaderId": "uuid-current",
    "leaderName": "雷霆战士",
    "members": [
      {
        "characterId": "uuid-current",
        "characterName": "雷霆战士",
        "profession": "Warrior",
        "level": 25,
        "role": "leader",
        "status": "online",
        "joinedAt": "2026-05-14T10:00:00Z"
      }
    ],
    "maxMembers": 4,
    "status": "open",
    "createdAt": "2026-05-14T10:00:00Z"
  }
}
```

**错误码**：
| code | 说明 |
|:---:|:---|
| 400 | 已在队伍中，不能重复创建 |

---

### 2. 获取公开队伍列表

**GET `/team/list`**

获取所有状态为 `open` 的队伍列表，供玩家浏览和申请。

**请求参数**：无

**响应示例**：
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": "team-uuid",
      "leaderId": "uuid-leader",
      "leaderName": "暗影猎手",
      "members": [...],
      "maxMembers": 4,
      "status": "open",
      "targetDungeon": "mist_forest_normal",
      "createdAt": "2026-05-12T10:00:00Z"
    }
  ]
}
```

---

### 3. 获取我的队伍

**GET `/team/my`**

获取当前角色所在的队伍信息。

**请求参数**：无

**响应示例**：
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": "team-uuid",
    "leaderId": "uuid-leader",
    "leaderName": "雷霆战士",
    "members": [...],
    "maxMembers": 4,
    "status": "open",
    "createdAt": "2026-05-14T10:00:00Z"
  }
}
```

**未在队伍中时**：
```json
{
  "code": 200,
  "message": "获取成功",
  "data": null
}
```

---

### 4. 邀请好友加入队伍

**POST `/team/invite`**

队长邀请好友加入队伍。被邀请者收到邀请通知后可接受或拒绝。

**请求参数**：
```json
{
  "characterId": "uuid-friend"
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "邀请已发送",
  "data": {
    "characterId": "uuid-friend",
    "characterName": "冰霜法师",
    "profession": "Mage",
    "level": 22,
    "role": "member",
    "status": "online",
    "joinedAt": "2026-05-14T10:30:00Z"
  }
}
```

**错误码**：
| code | 说明 |
|:---:|:---|
| 400 | 未在队伍中 / 队伍已满 / 该玩家已在队伍中 |
| 403 | 不是队长，无权邀请 |

---

### 5. 申请加入队伍

**POST `/team/apply`**

向目标队伍提交入队申请。

**请求参数**：
```json
{
  "teamId": "team-uuid"
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "申请已提交",
  "data": {
    "id": "app-uuid",
    "teamId": "team-uuid",
    "applicantId": "uuid-current",
    "applicantName": "火焰之心",
    "applicantProfession": "Mage",
    "applicantLevel": 20,
    "status": "pending",
    "createdAt": "2026-05-14T09:00:00Z"
  }
}
```

**错误码**：
| code | 说明 |
|:---:|:---|
| 400 | 已在队伍中 / 已申请过 / 队伍已满 / 队伍已关闭 |
| 404 | 队伍不存在 |

---

### 6. 获取入队申请列表

**GET `/team/applications/:teamId`**

获取指定队伍的待处理申请列表（仅队长可查看）。

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|:---|:---|:---:|:---|
| teamId | string | 是 | 队伍 ID（路径参数） |

**响应示例**：
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": "app-uuid",
      "teamId": "team-uuid",
      "applicantId": "uuid-applicant",
      "applicantName": "火焰之心",
      "applicantProfession": "Mage",
      "applicantLevel": 20,
      "status": "pending",
      "createdAt": "2026-05-14T09:00:00Z"
    }
  ]
}
```

**错误码**：
| code | 说明 |
|:---:|:---|
| 403 | 不是队长，无权查看 |

---

### 7. 接受入队申请

**POST `/team/accept`**

队长接受入队申请，申请者自动加入队伍。

**请求参数**：
```json
{
  "applicationId": "app-uuid"
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "已接受申请",
  "data": {
    "characterId": "uuid-applicant",
    "characterName": "火焰之心",
    "profession": "Mage",
    "level": 20,
    "role": "member",
    "status": "online",
    "joinedAt": "2026-05-14T10:05:00Z"
  }
}
```

**错误码**：
| code | 说明 |
|:---:|:---|
| 400 | 队伍已满 |
| 403 | 不是队长 |
| 404 | 申请不存在 |

---

### 8. 拒绝入队申请

**POST `/team/reject`**

队长拒绝入队申请。

**请求参数**：
```json
{
  "applicationId": "app-uuid"
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "已拒绝申请",
  "data": null
}
```

---

### 9. 踢出队伍成员

**DELETE `/team/kick/:characterId`**

队长将成员踢出队伍。

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|:---|:---|:---:|:---|
| characterId | string | 是 | 成员角色 ID（路径参数） |

**响应示例**：
```json
{
  "code": 200,
  "message": "已踢出成员",
  "data": null
}
```

**错误码**：
| code | 说明 |
|:---:|:---|
| 403 | 不是队长 / 不能踢出自己 |
| 404 | 成员不在队伍中 |

---

### 10. 离开队伍

**POST `/team/leave`**

当前角色离开所在队伍。队长离开时自动转让队长给最早加入的成员；若队伍只剩队长一人，则自动解散。

**请求参数**：无

**响应示例**：
```json
{
  "code": 200,
  "message": "已离开队伍",
  "data": null
}
```

**错误码**：
| code | 说明 |
|:---:|:---|
| 400 | 未在队伍中 |

---

### 11. 解散队伍

**DELETE `/team/disband`**

队长解散队伍，所有成员被移出。

**请求参数**：无

**响应示例**：
```json
{
  "code": 200,
  "message": "队伍已解散",
  "data": null
}
```

**错误码**：
| code | 说明 |
|:---:|:---|
| 400 | 未在队伍中 |
| 403 | 不是队长 |

---

### 12. 转让队长

**POST `/team/change-leader`**

队长将队长权限转让给指定成员。

**请求参数**：
```json
{
  "characterId": "uuid-new-leader"
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "队长已转让",
  "data": {
    "id": "team-uuid",
    "leaderId": "uuid-new-leader",
    "leaderName": "冰霜法师",
    "members": [...],
    "maxMembers": 4,
    "status": "open",
    "createdAt": "2026-05-14T10:00:00Z"
  }
}
```

**错误码**：
| code | 说明 |
|:---:|:---|
| 400 | 目标不是队伍成员 / 不能转让给自己 |
| 403 | 不是队长 |

---

### 13. 切换队伍开放状态

**POST `/team/status`**

队长切换队伍的招募状态（开放/关闭）。

**请求参数**：
```json
{
  "status": "closed"
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "状态已更新",
  "data": {
    "id": "team-uuid",
    "leaderId": "uuid-leader",
    "leaderName": "雷霆战士",
    "members": [...],
    "maxMembers": 4,
    "status": "closed",
    "createdAt": "2026-05-14T10:00:00Z"
  }
}
```

**错误码**：
| code | 说明 |
|:---:|:---|
| 400 | 未在队伍中 / 无效状态值 |
| 403 | 不是队长 |

---

## 数据库表结构建议

### 队伍表 (teams)

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `id` | UUID | 主键 |
| `leader_id` | UUID (FK → characters) | 队长角色 ID |
| `max_members` | Integer (默认 4) | 最大人数 |
| `status` | ENUM: open/closed/in_dungeon | 队伍状态 |
| `target_dungeon` | String (可 null) | 目标副本标识 |
| `created_at` | TIMESTAMP | 创建时间 |
| `updated_at` | TIMESTAMP | 更新时间 |

### 队伍成员表 (team_members)

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `id` | UUID | 主键 |
| `team_id` | UUID (FK → teams) | 队伍 ID |
| `character_id` | UUID (FK → characters) | 成员角色 ID |
| `role` | ENUM: leader/member | 成员角色 |
| `joined_at` | TIMESTAMP | 加入时间 |

> **约束**：`character_id` 唯一索引，确保一个角色只能在一个队伍中。`(team_id, character_id)` 联合索引。

### 入队申请表 (team_applications)

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `id` | UUID | 主键 |
| `team_id` | UUID (FK → teams) | 队伍 ID |
| `applicant_id` | UUID (FK → characters) | 申请者角色 ID |
| `status` | ENUM: pending/accepted/rejected | 申请状态 |
| `created_at` | TIMESTAMP | 创建时间 |
| `updated_at` | TIMESTAMP | 更新时间 |

> **约束**：`(team_id, applicant_id)` 唯一索引，确保同一队伍同一申请者只有一条记录。

### 队伍邀请表 (team_invitations)

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `id` | UUID | 主键 |
| `team_id` | UUID (FK → teams) | 队伍 ID |
| `inviter_id` | UUID (FK → characters) | 邀请者角色 ID |
| `invitee_id` | UUID (FK → characters) | 被邀请者角色 ID |
| `status` | ENUM: pending/accepted/rejected | 邀请状态 |
| `created_at` | TIMESTAMP | 创建时间 |

> **约束**：`(team_id, invitee_id)` 唯一索引。

---

## WebSocket 事件（推荐后期实现）

队伍系统建议使用 WebSocket 实现实时通知：

```json
// 队伍成员变动
{ "event": "team:member_joined", "teamId": "team-uuid", "member": {...} }
{ "event": "team:member_left", "teamId": "team-uuid", "characterId": "uuid-xxx" }
{ "event": "team:member_kicked", "teamId": "team-uuid", "characterId": "uuid-xxx" }

// 队长变更
{ "event": "team:leader_changed", "teamId": "team-uuid", "newLeaderId": "uuid-xxx" }

// 队伍状态变更
{ "event": "team:status_changed", "teamId": "team-uuid", "status": "closed" }

// 队伍解散
{ "event": "team:disbanded", "teamId": "team-uuid" }

// 收到入队申请（队长）
{ "event": "team:application_received", "teamId": "team-uuid", "application": {...} }

// 收到队伍邀请（被邀请者）
{ "event": "team:invitation_received", "teamId": "team-uuid", "inviterName": "雷霆战士" }
```

---

## 业务规则

1. **队伍人数上限**：默认 4 人
2. **唯一队伍约束**：一个角色只能同时在一个队伍中
3. **队长权限**：邀请、踢人、转让队长、切换状态、解散队伍
4. **成员权限**：离开队伍
5. **队长离开**：自动转让给最早加入的成员；若只剩队长一人，队伍自动解散
6. **申请有效期**：入队申请 24 小时未处理自动过期
7. **邀请有效期**：队伍邀请 24 小时未响应自动过期
8. **副本中限制**：队伍状态为 `in_dungeon` 时，不能踢人、离开、解散

---

## 前端 Mock 数据

当前 Mock 包含：
- 2 支公开队伍（1 支 2 人、1 支 1 人）
- 1 条入队申请
- 创建队伍后自动成为队长

Mock 数据在 `src/api/team.ts` 中定义，用于前端开发测试。

---

## 测试要点

1. **创建队伍**：创建后自动成为队长，显示队伍面板
2. **公开队伍列表**：显示所有招募中的队伍，可申请加入
3. **邀请好友**：队长可邀请在线好友，已在队中的好友显示"已在队中"
4. **入队申请**：队长可查看、接受或拒绝申请
5. **踢出成员**：队长可踢出成员，需确认弹窗
6. **转让队长**：队长可转让给其他成员
7. **离开队伍**：成员可离开，需确认弹窗
8. **解散队伍**：队长可解散，需确认弹窗
9. **切换状态**：队长可切换招募中/已关闭状态

---

## 版本记录

| 版本 | 日期 | 说明 |
|:---:|:---:|:---|
| v1.0 | 2026-05-14 | 初始版本，前端 Mock 实现 |
