# 装备强化/升级功能 - 后端对接文档

> 版本：v1.0.0
> 更新日期：2026-05-06
> 说明：定义装备强化接口、数据结构和校验规则

---

## 功能说明

玩家通过消耗材料和金币提升装备的强化等级（+0 ~ +10），强化后装备基础属性获得百分比加成。

---

## 数据结构

### Equipment 新增字段

```json
{
  "enhanceLevel": 3
}
```

| 字段 | 类型 | 默认值 | 说明 |
|:---|:---|:---|:---|
| `enhanceLevel` | Integer | 0 | 强化等级（0-10） |

---

## 接口定义

### POST `/equipment/enhance` — 强化装备

**请求参数：**
```json
{
  "characterId": "uuid",
  "slotType": "weapon"
}
```

| 参数 | 类型 | 必填 | 说明 |
|:---|:---|:---|:---|
| `characterId` | String | 是 | 角色 UUID |
| `slotType` | String | 是 | 装备槽位类型 |

**后端校验逻辑：**

1. 校验角色存在
2. 校验指定槽位有装备
3. 校验强化等级未达上限（10）
4. 校验背包材料数量充足
5. 校验金币充足
6. 扣除材料和金币
7. 判定强化是否成功（按成功率）
8. 成功：`enhanceLevel + 1`，更新装备数据
9. 失败：材料已消耗但不提升等级

**响应数据：**
```json
{
  "code": 200,
  "message": "强化成功",
  "data": {
    "success": true,
    "newLevel": 4,
    "message": "强化成功！精钢长剑 强化至 +4",
    "character": { ... }
  }
}
```

---

## 强化规则（前端配置，后端需同步）

### 等级上限
- 最大强化等级：**+10**

### 各稀有度每级属性加成倍率

| 稀有度 | 每级加成 |
|:---|:---|
| 普通 | +5% 基础属性 |
| 稀有 | +6% |
| 史诗 | +8% |
| 传说 | +10% |

### 材料消耗与成功率

| 强化等级区间 | 消耗材料 | 金币 | 成功率 |
|:---|:---|:---|:---|
| +0 → +3 | 铁矿石 ×3 | 100 | 100% |
| +3 → +6 | 精钢矿石 ×2 | 300 | 90% |
| +6 → +8 | 秘法水晶 ×1 | 800 | 70% |
| +8 → +10 | 秘法水晶 ×2 + 龙鳞碎片 ×1 | 2000 | 50% |

### 属性计算公式

```
强化后属性 = 基础属性 + round(基础属性 × 加成倍率 × 强化等级)
```

---

## 数据库字段

| 表 | 字段 | 类型 | 说明 |
|:---|:---|:---|:---|
| `character_equipments` | `enhance_level` | INT (默认 0) | 强化等级 |

---

## 前端展示

- 装备详情卡片：名称后显示 `+N` 强化等级（金色）
- 属性值显示强化后总值，括号内显示基础值
- 属性对比弹窗：提示当前装备包含强化加成

---

如有疑问请联系前端对接人。
