# 战宠系统 - 角色表字段扩展说明

## 📋 修改概述

为支持即将引入的战宠系统，需要在 `wg_character`（游戏角色表）中增加**战宠属性加成**相关字段。

---

## 🔧 数据库变更

### 新增字段

在 `wg_character` 表中新增以下 4 个字段：

| 字段名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `bonus_hp` | `int` | `0` | 战宠加成-生命值 |
| `bonus_physical_attack` | `int` | `0` | 战宠加成-物理攻击 |
| `bonus_magic_attack` | `int` | `0` | 战宠加成-魔法攻击 |
| `bonus_defense` | `int` | `0` | 战宠加成-防御力 |

### 字段位置

新字段添加在 `critical_rate`（暴击率）之后，`create_time`（创建时间）之前。

### 执行迁移脚本

**方式 1: 直接执行 SQL 文件**

```bash
mysql -u root -p jeecg-boot < migration_add_pet_bonus_fields.sql
```

**方式 2: 在 MySQL 客户端中执行**

```sql
source G:/Work/网页小游戏/网页小游戏后端/jeecg-boot/db/migration_add_pet_bonus_fields.sql;
```

**方式 3: 使用 Navicat/DBeaver 等工具**

打开 `migration_add_pet_bonus_fields.sql` 文件，执行全部 SQL 语句。

> **注意**: 迁移脚本已做幂等性处理，可以安全地重复执行，不会报错。

---

## 💻 Java 代码变更

### 1. 实体类 (WgCharacter.java)

**位置**: `jeecg-boot-module-webgame/src/main/java/org/jeecg/modules/webgame/character/entity/WgCharacter.java`

**新增字段**:

```java
/**战宠加成-生命值*/
private Integer bonusHp;

/**战宠加成-物理攻击*/
private Integer bonusPhysicalAttack;

/**战宠加成-魔法攻击*/
private Integer bonusMagicAttack;

/**战宠加成-防御力*/
private Integer bonusDefense;
```

### 2. VO 类 (CharacterVO.java)

**位置**: `jeecg-boot-module-webgame/src/main/java/org/jeecg/modules/webgame/character/vo/CharacterVO.java`

**新增字段**:

```java
/**战宠加成-生命值*/
private Integer bonusHp;

/**战宠加成-物理攻击*/
private Integer bonusPhysicalAttack;

/**战宠加成-魔法攻击*/
private Integer bonusMagicAttack;

/**战宠加成-防御力*/
private Integer bonusDefense;
```

### 3. Service 实现 (WgCharacterServiceImpl.java)

**位置**: `jeecg-boot-module-webgame/src/main/java/org/jeecg/modules/webgame/character/service/impl/WgCharacterServiceImpl.java`

**修改方法**: `convertToVO()`

**新增代码**:

```java
// 战宠加成（后续由战宠模块更新）
vo.setBonusHp(character.getBonusHp() != null ? character.getBonusHp() : 0);
vo.setBonusPhysicalAttack(character.getBonusPhysicalAttack() != null ? character.getBonusPhysicalAttack() : 0);
vo.setBonusMagicAttack(character.getBonusMagicAttack() != null ? character.getBonusMagicAttack() : 0);
vo.setBonusDefense(character.getBonusDefense() != null ? character.getBonusDefense() : 0);
```

---

## 📄 API 接口文档变更

### 更新的接口

#### 1. GET `/webgame/character/list` - 获取角色列表

**响应字段新增**:

```json
{
  "code": 200,
  "data": [
    {
      "id": "...",
      "characterName": "...",
      ...
      "bonusHp": 0,
      "bonusPhysicalAttack": 0,
      "bonusMagicAttack": 0,
      "bonusDefense": 0,
      ...
    }
  ]
}
```

#### 2. POST `/webgame/character/create` - 创建角色

**响应示例新增**:

```json
{
  "code": 200,
  "message": "角色创建成功",
  "data": {
    "id": "...",
    "characterName": "...",
    ...
    "bonusHp": 0,
    "bonusPhysicalAttack": 0,
    "bonusMagicAttack": 0,
    "bonusDefense": 0,
    ...
  }
}
```

---

## 🎮 业务逻辑说明

### 战宠加成计算规则

根据《战宠系统-后端对接文档》，出战战宠会给主人提供属性加成：

```
bonusHp = floor(pet.hp × 0.1 × rarity_multiplier)
bonusPhysicalAttack = floor(pet.attack × 0.1 × rarity_multiplier)
bonusMagicAttack = floor(pet.magicAttack × 0.1 × rarity_multiplier)
bonusDefense = floor(pet.defense × 0.1 × rarity_multiplier)
```

**品质倍率对照**:

| 品质 | 倍率 |
|------|------|
| N (普通) | 1.0 |
| R (稀有) | 1.2 |
| SR (史诗) | 1.5 |
| SSR (传说) | 2.0 |

### 当前状态

目前这些字段的值都是 **0**（默认值），因为战宠模块尚未开发。

**后续战宠模块开发完成后**，会在以下场景自动更新这些字段：

1. **设置出战战宠**: 计算出战战宠的加成，更新到角色表
2. **取消出战**: 将加成字段重置为 0
3. **战宠升级/进化**: 重新计算加成并更新
4. **战宠装备变更**: 重新计算加成并更新

---

## ✅ 验证步骤

### 1. 数据库验证

执行查询确认字段已添加：

```sql
DESCRIBE wg_character;

-- 或者
SHOW COLUMNS FROM wg_character LIKE 'bonus%';
```

**预期输出**:

```
+-----------------------+---------+------+-----+---------+-------+
| Field                 | Type    | Null | Key | Default | Extra |
+-----------------------+---------+------+-----+---------+-------+
| bonus_hp              | int     | YES  |     | 0       |       |
| bonus_physical_attack | int     | YES  |     | 0       |       |
| bonus_magic_attack    | int     | YES  |     | 0       |       |
| bonus_defense         | int     | YES  |     | 0       |       |
+-----------------------+---------+------+-----+---------+-------+
```

### 2. 接口验证

重启服务后，调用角色列表接口：

```bash
curl -X GET http://localhost:8080/jeecg-boot/webgame/character/list \
  -H "X-Access-Token: YOUR_TOKEN"
```

**检查响应中是否包含新字段**:

```json
{
  "code": 200,
  "data": [
    {
      "bonusHp": 0,
      "bonusPhysicalAttack": 0,
      "bonusMagicAttack": 0,
      "bonusDefense": 0,
      ...
    }
  ]
}
```

### 3. Swagger 验证

访问 **http://localhost:8080/jeecg-boot/doc.html**

在 **"游戏角色接口"** 分组中，查看角色相关接口的响应参数，确认新增了 4 个战宠加成字段。

---

## 📝 注意事项

### 1. 向后兼容

- ✅ 新字段有默认值 `0`，不影响现有数据
- ✅ 旧代码不依赖这些字段，不会报错
- ✅ 前端可以选择性展示这些字段（暂时显示为 0）

### 2. 数据迁移

如果数据库中已有角色数据，执行迁移脚本后：
- 所有现有角色的战宠加成字段都会自动设置为 `0`
- 不需要手动更新历史数据

### 3. 性能影响

- 新增 4 个 `int` 字段，对性能影响极小
- 查询时会自动返回这些字段，无需额外 JOIN

### 4. 后续开发

战宠模块开发时，需要：
1. 创建战宠相关的 5 张表（见《战宠系统-后端对接文档》）
2. 实现战宠 CRUD 接口
3. 实现战宠加成计算逻辑
4. 在设置出战战宠时，自动更新角色的 `bonus_*` 字段

---

## 🚀 下一步计划

按照《开发方案(2).md》中的 Sprint 2 计划：

1. **Step 1**: 创建战宠相关的 5 张数据库表
2. **Step 2**: 实现战宠 CRUD 接口（列表/详情/重命名/设置出战）
3. **Step 3**: 实现战宠养成逻辑（喂食、进化、技能、装备）
4. **Step 4**: 实现副本掉落战宠的逻辑
5. **Step 5-8**: 前端页面开发和战斗集成

**当前修改是为 Step 2 和 Step 3 做准备**，确保角色表能够存储战宠加成数据。

---

## 📞 问题反馈

如有任何问题，请查阅以下文档：
- [API接口文档.md](./API接口文档.md)
- [战宠系统-后端对接文档.md](./战宠系统-后端对接文档.md)
- [开发方案(2).md](./开发方案\(2\).md)
