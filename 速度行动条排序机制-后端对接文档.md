# 速度行动条排序机制 - 后端对接文档

## 概述

本文档定义基于速度值的行动条（ATB, Active Time Battle）排序机制。与传统的"每回合所有人按速度排序行动一次"不同，行动值系统允许速度高的单位在慢速单位行动前行动多次，模拟真实战斗中的速度优势。

---

## 核心概念

### 行动值 (Action Value)

每个参战单位拥有一个 `actionValue` 字段，表示当前累积的行动值。

```
行动值阈值 (ACTION_VALUE_THRESHOLD) = 1000
```

### 行动值累积规则

每 tick 所有存活单位的行动值累加：

```
actionValue += effectiveSpeed
```

当 `actionValue >= ACTION_VALUE_THRESHOLD` 时，该单位可以行动。

### 实际速度计算

```
effectiveSpeed = max(1, baseSpeed + Σ(speedBuff.value))
```

速度受 Buff/Debuff 影响，最低为 1。

---

## 行动流程

```
1. 初始化
   所有单位 actionValue = random(0, 500) + speed × 2

2. Tick 循环
   while (没有单位达到阈值):
     for each unit:
       actionValue += effectiveSpeed

3. 行动
   所有达到阈值的单位按 actionValue 降序行动
   行动后 actionValue -= ACTION_VALUE_THRESHOLD

4. 下一轮 Tick
   回到步骤 2
```

### 示例

| 单位 | 速度 | Tick 1 | Tick 2 | ... | 行动顺序 |
|------|------|--------|--------|-----|----------|
| 哥布林 | 20 | 20 | 40 | ... | 最先行动 |
| 战宠 | 16 | 16 | 32 | ... | 第二 |
| 暗影狼 | 15 | 15 | 30 | ... | 第三 |
| 玩家 | 14 | 14 | 28 | ... | 最后 |

哥布林速度 20，只需 50 个 tick 就能行动一次。玩家速度 14，需要约 72 个 tick。
因此哥布林可以比玩家更快地获得第二次行动机会。

---

## 数据结构

### Combatant 新增字段

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| actionValue | number | 0 | 当前行动值累积量 |

### ActionOrderEntry（新增类型）

| 字段 | 类型 | 说明 |
|------|------|------|
| uid | string | 参战单位 uid |
| name | string | 显示名称 |
| side | 'ally' \| 'enemy' | 阵营 |
| type | 'player' \| 'pet' \| 'enemy' | 类型 |
| actionValue | number | 当前行动值 |
| actionValuePercent | number | 行动值百分比 (0~1) |
| isAlive | boolean | 是否存活 |

### BattleState 新增字段

| 字段 | 类型 | 说明 |
|------|------|------|
| actionOrderPreview | ActionOrderEntry[] | 行动顺序预览（未来 N 个行动者） |

---

## API 变更

### 1. 发起战斗响应变更

`POST /battle/start` 返回的 enemies 中每个单位新增 `actionValue` 字段（初始为 0）。

```json
{
  "data": {
    "enemies": [
      {
        "uid": "enemy-001",
        "actionValue": 0,
        ...
      }
    ]
  }
}
```

### 2. 提交行动响应变更

`POST /battle/action` 返回的 `battleState` 新增 `actionOrderPreview` 字段。

```json
{
  "data": {
    "battleState": {
      "actionOrderPreview": [
        {
          "uid": "ally-player",
          "name": "勇者",
          "side": "ally",
          "type": "player",
          "actionValue": 1050,
          "actionValuePercent": 1.0,
          "isAlive": true
        },
        {
          "uid": "enemy-002",
          "name": "哥布林盗贼",
          "side": "enemy",
          "type": "enemy",
          "actionValue": 820,
          "actionValuePercent": 0.82,
          "isAlive": true
        }
      ]
    }
  }
}
```

---

## 后端计算逻辑

### 行动值 Tick

```java
// 伪代码
public List<String> tickActionValues(List<Combatant> combatants) {
    List<String> ready = new ArrayList<>();
    int maxIterations = 200;

    while (ready.isEmpty() && maxIterations-- > 0) {
        for (Combatant c : combatants) {
            if (!c.isAlive()) continue;
            int effectiveSpeed = getEffectiveSpeed(c);
            c.setActionValue(c.getActionValue() + effectiveSpeed);
            if (c.getActionValue() >= ACTION_VALUE_THRESHOLD) {
                ready.add(c.getUid());
            }
        }
    }

    // 按 actionValue 降序排序
    ready.sort((a, b) -> {
        Combatant ca = findByUid(combatants, a);
        Combatant cb = findByUid(combatants, b);
        return cb.getActionValue() - ca.getActionValue();
    });

    return ready;
}
```

### 行动后重置

```java
public void resetAfterAction(Combatant actor) {
    actor.setActionValue(actor.getActionValue() - ACTION_VALUE_THRESHOLD);
}
```

### 行动顺序预览生成

```java
public List<ActionOrderEntry> generatePreview(List<Combatant> combatants, int count) {
    // 克隆用于模拟
    List<Combatant> clones = deepCloneAlive(combatants);
    List<ActionOrderEntry> preview = new ArrayList<>();

    for (int i = 0; i < count; i++) {
        // Tick 直到有人满
        String readyUid = tickUntilReady(clones);
        if (readyUid == null) break;

        Combatant actor = findByUid(clones, readyUid);
        preview.add(toEntry(actor));

        // 重置行动值
        actor.setActionValue(actor.getActionValue() - ACTION_VALUE_THRESHOLD);
    }

    return preview;
}
```

---

## 公式汇总

| 公式 | 说明 |
|------|------|
| `ACTION_VALUE_THRESHOLD = 1000` | 行动阈值 |
| `effectiveSpeed = max(1, baseSpeed + Σ(buffValue))` | 实际速度 |
| `tickActionValue += effectiveSpeed` | 每 tick 累积 |
| `初始值 = random(0, 500) + speed × 2` | 战斗开始时 |
| `行动后 = actionValue - threshold` | 行动后扣除 |

---

## 注意事项

1. **前后端一致性**：行动值计算公式前后端必须完全一致，避免行动顺序不同步
2. **速度 Buff 影响**：当单位身上有速度类 Buff/Debuff 时，行动值累积速度实时变化
3. **死亡处理**：死亡单位的行动值不再增长
4. **行动顺序预览**：预览基于当前状态模拟，实际行动时可能因 Buff 变化导致顺序改变
5. **防作弊**：后端为权威端，前端展示的行动顺序仅供参考，实际由后端计算确认
6. **性能**：`maxIterations = 200` 确保不会无限循环，正常情况下 50~80 个 tick 即有人达到阈值
