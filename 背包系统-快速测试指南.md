# 背包系统 - 快速测试指南

## 🚀 前置准备

### 1. 启动应用

确保应用已启动并且数据库迁移脚本已执行。

### 2. 创建测试角色

```bash
curl -X POST "http://localhost:8080/webgame/character/create" \
  -H "Content-Type: application/json" \
  -H "X-Access-Token: YOUR_TOKEN" \
  -d '{
    "characterName": "测试战士",
    "profession": 1
  }'
```

**记录返回的 `characterId`**，后续测试会用到。

### 3. 添加测试物品到背包

由于还没有获取物品的接口，需要手动插入测试数据：

```sql
-- 连接到数据库
mysql -u root -p jeecg-boot

-- 插入测试物品（替换 YOUR_CHARACTER_ID）
INSERT INTO character_inventory (id, character_id, item_id, quantity, obtained_at) VALUES
(UUID(), 'YOUR_CHARACTER_ID', 1001, 10, NOW()),  -- 小型生命药水 x10
(UUID(), 'YOUR_CHARACTER_ID', 1004, 5, NOW()),   -- 魔法药水 x5
(UUID(), 'YOUR_CHARACTER_ID', 1006, 3, NOW()),   -- 经验卷轴（小）x3
(UUID(), 'YOUR_CHARACTER_ID', 2001, 50, NOW()),  -- 铁矿石 x50
(UUID(), 'YOUR_CHARACTER_ID', 2005, 2, NOW());   -- 战宠进化石 x2
```

---

## 🧪 测试场景

### 场景1：获取背包列表

```bash
curl -X GET "http://localhost:8080/webgame/inventory/list/YOUR_CHARACTER_ID" \
  -H "X-Access-Token: YOUR_TOKEN"
```

**预期结果：**
- 返回5种物品的列表
- 每种物品包含完整的模板信息
- 按获取时间倒序排列

---

### 场景2：使用生命药水（正常）

#### 步骤1：查看当前HP

```bash
curl -X GET "http://localhost:8080/webgame/character/info/YOUR_CHARACTER_ID" \
  -H "X-Access-Token: YOUR_TOKEN"
```

记录当前的 `hp` 和 `maxHp`。

#### 步骤2：获取背包中生命药水的 inventoryId

从场景1的响应中找到 itemId=1001 的记录，记录其 `id` 字段（即 inventoryId）。

#### 步骤3：使用1个生命药水

```bash
curl -X POST "http://localhost:8080/webgame/inventory/use" \
  -H "Content-Type: application/json" \
  -H "X-Access-Token: YOUR_TOKEN" \
  -d '{
    "characterId": "YOUR_CHARACTER_ID",
    "inventoryId": "INVENTORY_ID_FROM_STEP2",
    "quantity": 1
  }'
```

**预期结果：**
```json
{
  "code": 200,
  "message": "成功使用 小型生命药水×1，恢复 50 点生命值",
  "data": {
    "effects": ["恢复 50 HP"],
    "message": "成功使用 小型生命药水×1，恢复 50 点生命值"
  }
}
```

#### 步骤4：验证HP增加

再次调用角色详情接口，确认 `hp` 增加了50（不超过maxHp）。

---

### 场景3：HP已满时使用生命药水

#### 步骤1：将HP加满

直接修改数据库：

```sql
UPDATE wg_character SET hp = max_hp WHERE id = 'YOUR_CHARACTER_ID';
```

#### 步骤2：尝试使用生命药水

重复场景2的步骤3。

**预期结果：**
```json
{
  "code": 500,
  "message": "HP 已满，无需恢复",
  "data": null
}
```

---

### 场景4：使用经验卷轴

```bash
curl -X POST "http://localhost:8080/webgame/inventory/use" \
  -H "Content-Type: application/json" \
  -H "X-Access-Token: YOUR_TOKEN" \
  -d '{
    "characterId": "YOUR_CHARACTER_ID",
    "inventoryId": "经验卷轴的inventoryId",
    "quantity": 1
  }'
```

**预期结果（未升级）：**
```json
{
  "code": 200,
  "message": "成功使用 经验卷轴（小）×1，获得 100 经验值",
  "data": {
    "effects": ["获得 100 经验值"],
    "message": "成功使用 经验卷轴（小）×1，获得 100 经验值"
  }
}
```

**预期结果（升级）：**
```json
{
  "code": 200,
  "message": "恭喜升级！获得 3 点属性点",
  "data": {
    "effects": ["获得 100 经验值"],
    "message": "成功使用 经验卷轴（小）×1，获得 100 经验值，等级提升至 2！"
  }
}
```

---

### 场景5：丢弃物品

```bash
curl -X POST "http://localhost:8080/webgame/inventory/discard" \
  -H "Content-Type: application/json" \
  -H "X-Access-Token: YOUR_TOKEN" \
  -d '{
    "characterId": "YOUR_CHARACTER_ID",
    "inventoryId": "铁矿石的inventoryId",
    "quantity": 10
  }'
```

**预期结果：**
```json
{
  "code": 200,
  "message": "丢弃成功",
  "data": null
}
```

**验证：** 再次获取背包列表，确认铁矿石数量从50变为40。

---

### 场景6：丢弃全部物品

```bash
curl -X POST "http://localhost:8080/webgame/inventory/discard" \
  -H "Content-Type: application/json" \
  -H "X-Access-Token: YOUR_TOKEN" \
  -d '{
    "characterId": "YOUR_CHARACTER_ID",
    "inventoryId": "铁矿石的inventoryId",
    "quantity": 40
  }'
```

**预期结果：**
- 丢弃成功
- 背包列表中不再显示该物品（记录被删除）

---

### 场景7：使用材料（应该失败）

```bash
curl -X POST "http://localhost:8080/webgame/inventory/use" \
  -H "Content-Type: application/json" \
  -H "X-Access-Token: YOUR_TOKEN" \
  -d '{
    "characterId": "YOUR_CHARACTER_ID",
    "inventoryId": "铁矿石的inventoryId",
    "quantity": 1
  }'
```

**预期结果：**
```json
{
  "code": 500,
  "message": "该物品无法使用",
  "data": null
}
```

---

### 场景8：数量不足

```bash
curl -X POST "http://localhost:8080/webgame/inventory/use" \
  -H "Content-Type: application/json" \
  -H "X-Access-Token: YOUR_TOKEN" \
  -d '{
    "characterId": "YOUR_CHARACTER_ID",
    "inventoryId": "某个物品的inventoryId",
    "quantity": 999
  }'
```

**预期结果：**
```json
{
  "code": 500,
  "message": "数量不足",
  "data": null
}
```

---

### 场景9：连续升级测试

#### 步骤1：将角色设置为1级，经验为0

```sql
UPDATE wg_character SET level = 1, experience = 0 WHERE id = 'YOUR_CHARACTER_ID';
```

#### 步骤2：使用大型经验卷轴（2000经验）

1级升到2级需要100经验，2000经验足够升多级。

```bash
curl -X POST "http://localhost:8080/webgame/inventory/use" \
  -H "Content-Type: application/json" \
  -H "X-Access-Token: YOUR_TOKEN" \
  -d '{
    "characterId": "YOUR_CHARACTER_ID",
    "inventoryId": "大型经验卷轴的inventoryId",
    "quantity": 1
  }'
```

**预期结果：**
- 角色应该升到5级左右（具体看经验公式计算）
- `levelsGained` > 1
- `pointsGained` = `levelsGained` × 3
- 剩余经验正确计算

---

### 场景10：权限校验

尝试操作其他角色的背包：

```bash
curl -X POST "http://localhost:8080/webgame/inventory/use" \
  -H "Content-Type: application/json" \
  -H "X-Access-Token: YOUR_TOKEN" \
  -d '{
    "characterId": "OTHER_CHARACTER_ID",
    "inventoryId": "YOUR_INVENTORY_ID",
    "quantity": 1
  }'
```

**预期结果：**
```json
{
  "code": 500,
  "message": "无权操作该角色背包",
  "data": null
}
```

---

## 📊 测试检查清单

- [ ] 获取背包列表成功
- [ ] 使用生命药水恢复HP
- [ ] HP已满时提示错误
- [ ] 使用魔法药水恢复MP
- [ ] MP已满时提示错误
- [ ] 使用经验卷轴获得经验
- [ ] 经验足够时触发升级
- [ ] 连续升级正确处理
- [ ] 丢弃物品减少数量
- [ ] 丢弃全部物品删除记录
- [ ] 使用材料返回错误
- [ ] 数量不足返回错误
- [ ] 无权操作返回错误
- [ ] 物品使用后数量正确扣减
- [ ] 堆叠规则正确（同类物品合并）

---

## 🔍 调试技巧

### 1. 查看日志

应用日志会输出详细的信息：

```
获取背包列表成功: characterId=xxx, itemCount=5
使用物品成功: characterId=xxx, itemId=1001, quantity=1, effect=heal_hp
丢弃物品成功: characterId=xxx, itemId=2001, quantity=10
```

### 2. 直接查询数据库

```sql
-- 查看角色背包
SELECT ci.*, it.item_name, it.category 
FROM character_inventory ci
LEFT JOIN wg_item_template it ON ci.item_id = it.item_id
WHERE ci.character_id = 'YOUR_CHARACTER_ID';

-- 查看角色状态
SELECT id, character_name, level, experience, hp, max_hp, mp, max_mp 
FROM wg_character 
WHERE id = 'YOUR_CHARACTER_ID';
```

### 3. 重置测试数据

```sql
-- 清空背包
DELETE FROM character_inventory WHERE character_id = 'YOUR_CHARACTER_ID';

-- 重置角色等级和经验
UPDATE wg_character SET level = 1, experience = 0 WHERE id = 'YOUR_CHARACTER_ID';

-- 重新插入测试物品
INSERT INTO character_inventory (id, character_id, item_id, quantity, obtained_at) VALUES
(UUID(), 'YOUR_CHARACTER_ID', 1001, 10, NOW()),
(UUID(), 'YOUR_CHARACTER_ID', 1004, 5, NOW()),
(UUID(), 'YOUR_CHARACTER_ID', 1006, 3, NOW()),
(UUID(), 'YOUR_CHARACTER_ID', 2001, 50, NOW()),
(UUID(), 'YOUR_CHARACTER_ID', 2005, 2, NOW());
```

---

## ⚠️ 常见问题

### Q1: 接口返回404？

**检查：**
- URL路径是否正确
- 角色ID是否存在
- Shiro配置是否放行 `/webgame/inventory/**`

### Q2: 接口返回500错误？

**检查：**
- 数据库表是否创建成功
- 物品数据是否初始化
- 查看应用日志定位具体错误

### Q3: Redis序列化异常？

**解决：**
```bash
redis-cli KEYS "shiro:*" | xargs redis-cli DEL
```
然后重启应用。

### Q4: 物品使用后数量没变化？

**检查：**
- 事务是否提交
- 是否有异常导致回滚
- 查看数据库实际数据

---

*测试指南版本：2026-05-08*  
*适用项目：网页小游戏后端*
