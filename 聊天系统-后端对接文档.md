# 聊天系统 - 后端对接文档

## 概述

本文档描述聊天系统的前后端对接规范。聊天系统支持世界频道（公开广播）和私聊（一对一），采用 WebSocket 实时推送，REST API 作为降级方案。

---

## 前端实现概览

| 文件 | 作用 |
|:---|:---|
| `src/types/chat.ts` | 聊天消息、会话类型定义 |
| `src/api/chat.ts` | 聊天 API（Mock + Real） |
| `src/stores/chat.ts` | 聊天状态管理 |
| `src/components/social/ChatPanel.vue` | 聊天面板主组件 |

---

## 数据结构

### ChatMessage（聊天消息）

```typescript
interface ChatMessage {
  id: string               // 消息 ID
  senderId: string         // 发送者角色 ID
  senderName: string       // 发送者角色名
  senderProfession: string // 发送者职业
  channel: ChatChannel     // 频道类型
  targetId?: string        // 私聊目标 ID（仅私聊）
  targetName?: string      // 私聊目标名称（仅私聊）
  content: string          // 消息内容
  timestamp: string        // 发送时间
}
```

### PrivateConversation（私聊会话）

```typescript
interface PrivateConversation {
  targetId: string         // 对方角色 ID
  targetName: string       // 对方角色名
  targetProfession: string // 对方职业
  lastMessage: string      // 最后一条消息
  lastTime: string         // 最后消息时间
  unreadCount: number      // 未读消息数
}
```

### ChatChannel（频道类型）

```typescript
type ChatChannel = 'world' | 'private'
```

---

## WebSocket 协议

### 连接地址

```
ws://{host}/jeecg-boot/webgame/ws/chat?token={jwt_token}
```

### 认证

连接时通过 URL 参数传递 JWT Token，服务端验证后建立连接。

---

### 服务端推送事件

#### 1. 世界频道消息

```json
{
  "event": "chat:world",
  "data": {
    "id": "msg-uuid",
    "senderId": "uuid-sender",
    "senderName": "雷霆战士",
    "senderProfession": "Warrior",
    "channel": "world",
    "content": "有人一起刷副本吗？",
    "timestamp": "2026-05-14T10:00:00Z"
  }
}
```

#### 2. 私聊消息

```json
{
  "event": "chat:private",
  "data": {
    "id": "msg-uuid",
    "senderId": "uuid-sender",
    "senderName": "影刃",
    "senderProfession": "Hunter",
    "channel": "private",
    "targetId": "uuid-receiver",
    "targetName": "当前玩家",
    "content": "在吗？",
    "timestamp": "2026-05-14T10:00:00Z"
  }
}
```

#### 3. 系统消息

```json
{
  "event": "chat:system",
  "data": {
    "content": "玩家「影刃」已上线",
    "type": "info"
  }
}
```

---

### 客户端发送事件

#### 发送消息

```json
{
  "event": "chat:send",
  "data": {
    "channel": "world",
    "content": "大家好！",
    "targetId": null
  }
}
```

私聊时：
```json
{
  "event": "chat:send",
  "data": {
    "channel": "private",
    "content": "你好！",
    "targetId": "uuid-target"
  }
}
```

---

## REST API（降级方案）

### Base URL

```
/jeecg-boot/webgame
```

### 1. 获取世界频道消息

**GET `/chat/world`**

获取最近的世界频道消息（默认最近 100 条）。

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|:---|:---|:---:|:---|
| limit | integer | 否 | 消息条数（默认 100，最大 200） |
| before | string | 否 | 游标：获取该消息 ID 之前的消息（分页用） |

**响应示例**：
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": "msg-uuid",
      "senderId": "uuid-sender",
      "senderName": "雷霆战士",
      "senderProfession": "Warrior",
      "channel": "world",
      "content": "有人一起刷副本吗？",
      "timestamp": "2026-05-14T09:30:00Z"
    }
  ]
}
```

---

### 2. 获取私聊会话列表

**GET `/chat/conversations`**

获取当前角色的所有私聊会话。

**响应示例**：
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "targetId": "uuid-target",
      "targetName": "影刃",
      "targetProfession": "Hunter",
      "lastMessage": "好的，明天见！",
      "lastTime": "2026-05-14T08:00:00Z",
      "unreadCount": 0
    }
  ]
}
```

---

### 3. 获取私聊记录

**GET `/chat/private/:targetId`**

获取与指定角色的私聊消息记录。

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|:---|:---|:---:|:---|
| targetId | string | 是 | 目标角色 ID（路径参数） |
| limit | integer | 否 | 消息条数（默认 50） |
| before | string | 否 | 游标分页 |

**响应示例**：
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": "msg-uuid",
      "senderId": "uuid-me",
      "senderName": "当前玩家",
      "senderProfession": "Warrior",
      "channel": "private",
      "targetId": "uuid-target",
      "targetName": "影刃",
      "content": "明天一起刷副本？",
      "timestamp": "2026-05-14T07:55:00Z"
    }
  ]
}
```

---

### 4. 发送消息

**POST `/chat/send`**

发送聊天消息。

**请求参数**：
```json
{
  "channel": "world",
  "content": "大家好！",
  "targetId": null
}
```

**私聊时**：
```json
{
  "channel": "private",
  "content": "你好！",
  "targetId": "uuid-target"
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "发送成功",
  "data": {
    "id": "msg-uuid",
    "senderId": "uuid-current",
    "senderName": "当前玩家",
    "senderProfession": "Warrior",
    "channel": "world",
    "content": "大家好！",
    "timestamp": "2026-05-14T10:00:00Z"
  }
}
```

**错误码**：
| code | 说明 |
|:---:|:---|
| 400 | 消息内容为空 / 超过长度限制 |
| 403 | 被对方拉黑 |
| 404 | 私聊目标不存在 |

---

## 数据库表结构建议

### 聊天消息表 (chat_messages)

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `id` | UUID | 主键 |
| `sender_id` | UUID (FK → characters) | 发送者角色 ID |
| `channel` | ENUM: world/private | 频道类型 |
| `target_id` | UUID (FK → characters, 可 null) | 私聊目标 ID |
| `content` | TEXT | 消息内容（最大 500 字） |
| `created_at` | TIMESTAMP | 发送时间 |

> **索引**：`channel + created_at` 用于世界频道查询；`sender_id + target_id + created_at` 用于私聊查询。

### 私聊会话表 (chat_conversations)

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `id` | UUID | 主键 |
| `user1_id` | UUID (FK → characters) | 较小角色 ID |
| `user2_id` | UUID (FK → characters) | 较大角色 ID |
| `last_message` | TEXT | 最后一条消息内容 |
| `last_time` | TIMESTAMP | 最后消息时间 |
| `updated_at` | TIMESTAMP | 更新时间 |

> **约束**：`(user1_id, user2_id)` 唯一索引。

### 未读消息计数表 (chat_unread)

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `id` | UUID | 主键 |
| `conversation_id` | UUID (FK → chat_conversations) | 会话 ID |
| `character_id` | UUID (FK → characters) | 角色ID |
| `unread_count` | Integer | 未读数量 |

---

## 业务规则

1. **消息长度限制**：单条消息最大 500 字符
2. **发送频率限制**：世界频道每 5 秒 1 条，私聊每 2 秒 1 条
3. **世界频道保留**：最近 200 条消息，超出自动清除
4. **私聊保留**：每个会话保留最近 500 条消息
5. **敏感词过滤**：后端对消息内容进行敏感词过滤，替换为 `***`
6. **离线消息**：私聊消息在对方离线时保存，上线后推送未读消息
7. **消息撤回**：可撤回 2 分钟内发送的消息（后续扩展）
8. **屏蔽功能**：被屏蔽的用户无法发送私聊消息

---

## 前端 Mock 数据

当前 Mock 包含：
- 世界频道：5 条来自不同玩家的消息
- 私聊会话：2 个会话（影刃、冰霜法师）
- 私聊记录：每个会话 3-4 条历史消息
- 模拟回复：发送消息后 2-3 秒自动收到模拟回复

Mock 数据在 `src/api/chat.ts` 中定义，用于前端开发测试。

---

## 测试要点

1. **世界频道**：显示历史消息，可发送新消息
2. **消息气泡**：自己的消息靠右蓝色，他人靠左灰色
3. **私聊会话列表**：按最后消息时间排序，显示未读数
4. **私聊消息**：点击会话进入聊天界面，显示历史消息
5. **发送消息**：输入框回车或点击发送按钮
6. **模拟回复**：发送后 2-3 秒收到自动回复
7. **未读标记**：会话列表显示红色未读计数
8. **自动滚动**：新消息时自动滚动到底部

---

## 版本记录

| 版本 | 日期 | 说明 |
|:---:|:---:|:---|
| v1.0 | 2026-05-14 | 初始版本，前端 Mock + WebSocket 协议定义 |
