# 装备系统 - 后端对接文档

> 版本：v1.0.0
> 更新日期：2026-05-01
> 说明：装备栏 UI、装备数据结构、装备属性计算规范

---

## 一、装备槽位

角色拥有 6 个装备槽位：

| 槽位 | 字段名 | 说明 |
|:---|:---|:---|
| 武器 | `weapon` | 武器槽（剑/法杖/弓等） |
| 头盔 | `helmet` | 头部防具 |
| 胸甲 | `chest` | 躯干防具 |
| 护腿 | `legs` | 腿部防具 |
| 饰品1 | `accessory1` | 饰品槽位1 |
| 饰品2 | `accessory2` | 饰品槽位2 |

---

## 二、数据库表结构

### 装备静态配置表 (equipments)

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `equipment_id` | INT (PK) | 装备模板 ID |
| `name` | VARCHAR(64) | 装备名称 |
| `slot_type` | ENUM('weapon','helmet','chest','legs','accessory1','accessory2') | 槽位类型 |
| `rarity` | ENUM('Normal','Rare','Epic','Legendary') | 稀有度 |
| `base_stats` | JSON | 基础属性 `{ "physicalAttack": 15, "defense": 8 }` |
| `set_id` | VARCHAR(32) | 套装 ID（可为 NULL） |
| `set_name` | VARCHAR(64) | 套装名称（可为 NULL） |
| `description` | VARCHAR(512) | 装备描述 |
| `icon_url` | VARCHAR(255) | 图标资源路径 |
| `level_requirement` | INT | 等级需求 |

### 角色装备表 (character_equipments)

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `id` | UUID (PK) | 装备实例唯一标识 |
| `character_id` | UUID (FK → characters) | 所属角色 |
| `equipment_id` | INT (FK → equipments) | 装备模板 ID |
| `slot_type` | ENUM | 当前装备在哪个槽位 |
| `extra_stats` | JSON | 随机词条（额外属性） |
| `enhance_level` | INT (默认 0) | 强化等级 |

> **说明**：角色装备表存储装备实例，每件装备独立一条记录。`extra_stats` 为稀有以上装备的随机附加属性。

---

## 三、装备数据结构（前端）

### 装备信息

```typescript
interface Equipment {
  id: string                 // 装备 UUID
  name: string               // 装备名称
  rarity: EquipmentRarity    // 稀有度
  slotType: EquipmentSlotType // 槽位类型
  stats: EquipmentStats      // 装备属性
  setId?: string             // 套装 ID
  setName?: string           // 套装名称
  iconUrl?: string           // 图标 URL
  description?: string       // 装备描述
  levelRequirement?: number  // 等级需求
}
```

### 装备属性

```typescript
interface EquipmentStats {
  physicalAttack?: number    // 物理攻击
  magicAttack?: number       // 魔法攻击
  defense?: number           // 防御力
  hp?: number                // 生命值加成
  mp?: number                // 魔法值加成
  criticalRate?: number      // 暴击率加成（百分比）
  dodgeRate?: number         // 闪避率加成（百分比）
  strength?: number          // 力量加成
  intelligence?: number      // 智力加成
  agility?: number           // 敏捷加成
}
```

### 六槽位结构

```typescript
interface EquipmentSlots {
  weapon: Equipment | null
  helmet: Equipment | null
  chest: Equipment | null
  legs: Equipment | null
  accessory1: Equipment | null
  accessory2: Equipment | null
}
```

---

## 四、角色详情接口中的装备数据

装备数据作为角色详情的一部分返回，无需单独接口。

**GET `/character/detail/{characterId}`** 的 `equipment` 字段：

```json
{
  "equipment": {
    "weapon": {
      "id": "eq-001",
      "name": "精钢长剑",
      "rarity": "Rare",
      "slotType": "weapon",
      "stats": { "physicalAttack": 15, "strength": 3 },
      "setId": "set-001",
      "setName": "勇者之证"
    },
    "helmet": {
      "id": "eq-005",
      "name": "铁盔",
      "rarity": "Normal",
      "slotType": "helmet",
      "stats": { "defense": 3, "hp": 20 }
    },
    "chest": {
      "id": "eq-002",
      "name": "铁甲胸铠",
      "rarity": "Normal",
      "slotType": "chest",
      "stats": { "defense": 8, "hp": 50 }
    },
    "legs": {
      "id": "eq-006",
      "name": "战靴",
      "rarity": "Normal",
      "slotType": "legs",
      "stats": { "defense": 4, "agility": 2 }
    },
    "accessory1": {
      "id": "eq-007",
      "name": "力量戒指",
      "rarity": "Rare",
      "slotType": "accessory1",
      "stats": { "strength": 5, "physicalAttack": 5 }
    },
    "accessory2": null
  }
}
```

> 空槽位返回 `null`。

---

## 五、品质体系渲染规范

| 稀有度 | 英文名 | 边框颜色 | 底纹渐变 |
|:---|:---:|:---:|:---|
| 普通 | Normal | #6e6e73 | 8% 透明度灰色 |
| 稀有 | Rare | #0071e3 | 18%→8% 蓝色渐变 |
| 史诗 | Epic | #af52de | 18%→8% 紫色渐变 |
| 传说 | Legendary | #ff9500 | 18%→8% 橙色渐变 |

**渲染效果：**
- 边框：2px 实线，颜色对应稀有度
- 底纹：`linear-gradient(135deg, {color}20 0%, {color}0a 100%)`
- 图标：槽位图标，颜色对应稀有度
- 悬浮光效：斜向扫光动画

---

## 六、属性名称映射

| 字段名 | 中文名 | 说明 |
|:---|:---|:---|
| physicalAttack | 物攻 | 物理攻击力 |
| magicAttack | 魔攻 | 魔法攻击力 |
| defense | 防御 | 防御力 |
| hp | 生命 | 生命值加成 |
| mp | 魔力 | 魔法值加成 |
| criticalRate | 暴击 | 暴击率加成 |
| dodgeRate | 闪避 | 闪避率加成 |
| strength | 力量 | 力量属性加成 |
| intelligence | 智力 | 智力属性加成 |
| agility | 敏捷 | 敏捷属性加成 |

---

## 七、图标资源规范

`iconUrl` 字段存储装备图标资源路径：

```
/assets/items/equipment/{equipmentId}.png
```

**前期方案**：无美术资源时 `iconUrl` 返回 `null`，前端使用槽位类型图标占位：

| 槽位 | 图标（Lucide） |
|:---|:---:|
| weapon | Sword |
| helmet | HardHat |
| chest | Shield |
| legs | Footprints |
| accessory1 | Gem |
| accessory2 | Gem |

**图标尺寸建议：**

| 场景 | 尺寸 |
|:---|:---|
| 装备槽位图标 | 32×32 px |
| 装备详情图标 | 48×48 px |

---

## 八、后续接口（待开发）

以下接口属于装备穿戴/卸下功能（阶段三后续条目），此处列出规范供参考：

| 接口 | 方法 | 说明 |
|:---|:---:|:---|
| `/equipment/equip` | POST | 从背包装备到槽位 |
| `/equipment/unequip` | POST | 从槽位卸下回背包 |
| `/equipment/enhance` | POST | 装备强化 |

---

如有疑问请联系前端对接人。
