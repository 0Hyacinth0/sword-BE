# 随机词条生成与展示 - 后端对接文档

> 版本：v1.0.0
> 更新日期：2026-05-06
> 说明：定义随机词条的数据结构和接口约定

---

## 功能说明

稀有以上装备在获取时随机生成附加属性（随机词条），增加装备的多样性和收集价值。词条在后端生成并持久化，前端负责展示。

---

## 词条规则

| 稀有度 | 随机词条数量 | 说明 |
|:---|:---|:---|
| 普通 (Normal) | 0 | 无随机词条 |
| 稀有 (Rare) | 1-2 | 少量附加属性 |
| 史诗 (Epic) | 3-4 | 较多附加属性 |
| 传说 (Legendary) | 4-6 | 大量附加属性 + 可能含特殊被动效果 |

---

## 数据结构

### ExtraStat（随机词条）

```json
{
  "key": "physicalAttack",
  "value": 5
}
```

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `key` | String | 属性 key，取值同 EquipmentStats 的字段名 |
| `value` | Number | 属性值（百分比类为小数，如 0.05 表示 5%） |

### 可选属性 key 列表

| key | 中文名 | 值类型 |
|:---|:---|:---|
| `physicalAttack` | 物攻 | 整数 |
| `magicAttack` | 魔攻 | 整数 |
| `defense` | 防御 | 整数 |
| `hp` | 生命 | 整数 |
| `mp` | 魔力 | 整数 |
| `criticalRate` | 暴击 | 小数（0.01 = 1%） |
| `dodgeRate` | 闪避 | 小数（0.01 = 1%） |
| `strength` | 力量 | 整数 |
| `intelligence` | 智力 | 整数 |
| `agility` | 敏捷 | 整数 |

---

## 后端接口约定

### 1. 装备获取时生成词条

**触发时机**：装备首次进入角色背包时（副本掉落、商店购买、任务奖励等）

**后端职责**：
- 根据装备稀有度随机生成 `extraStats`
- 词条 key 不与装备基础 `stats` 重复
- 词条值在对应稀有度的范围内随机
- 将 `extraStats` 持久化到 `character_inventory` 表的 `extra_stats` 字段

### 2. 接口返回数据扩展

**背包物品接口** `GET /inventory/list/{characterId}`

返回的 `InventoryItem` 新增字段：
```json
{
  "id": "inv-xxx",
  "characterId": "char-xxx",
  "itemId": 3002,
  "item": { ... },
  "quantity": 1,
  "obtainedAt": "2026-05-01T10:00:00Z",
  "extraStats": [
    { "key": "agility", "value": 2 },
    { "key": "dodgeRate", "value": 0.01 }
  ]
}
```

**角色详情接口** `GET /character/info/{characterId}`

返回的 `Equipment` 新增字段：
```json
{
  "id": "eq-001",
  "name": "精钢长剑",
  "rarity": "Rare",
  "slotType": "weapon",
  "stats": { "physicalAttack": 15, "strength": 3 },
  "extraStats": [
    { "key": "criticalRate", "value": 0.02 },
    { "key": "agility", "value": 2 }
  ]
}
```

### 3. 装备穿戴/卸下

穿戴时：将背包物品的 `extraStats` 复制到装备栏的 `Equipment.extraStats`
卸下时：将装备栏的 `extraStats` 复制回背包物品

---

## 前端展示逻辑

- **物品详情弹窗**：稀有以上装备显示"随机词条"区域，绿色高亮
- **装备详情卡片**：基础属性下方用虚线分隔显示随机词条
- **属性对比**：差异计算时合并基础属性 + 随机词条

---

## 数据库字段

| 表 | 字段 | 类型 | 说明 |
|:---|:---|:---|:---|
| `character_inventory` | `extra_stats` | JSON | 背包装备实例的随机词条 |
| `character_equipments` | `extra_stats` | JSON | 已装备实例的随机词条 |

---

如有疑问请联系前端对接人。
