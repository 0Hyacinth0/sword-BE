# 品质体系渲染 - 后端对接文档

> 版本：v1.0.0
> 更新日期：2026-05-06
> 说明：品质体系渲染为纯前端功能，后端无需新增接口

---

## 功能说明

根据物品/装备的稀有度（`rarity`）字段，在 UI 上渲染不同品质等级的视觉效果，包括：
- **品质边框颜色**：不同稀有度对应不同颜色边框
- **品质底纹渐变**：格子内根据稀有度显示半透明渐变背景
- **品质角标**：左上角显示品质标签（普通/稀有/史诗/传说）
- **品质光效**：史诗和传说品质具有加强扫光和呼吸/脉动动画
- **暗色模式适配**：所有品质颜色在暗色模式下自动切换为对应的亮色系

---

## 数据来源

品质渲染所需数据均来自现有接口返回的 `rarity` 字段：

| 数据 | 来源 | 字段 |
|:---|:---|:---|
| 物品稀有度 | 背包物品详情 | `InventoryItem.item.rarity` |
| 装备稀有度 | 角色装备数据 | `CharacterInfo.equipment[slotType].rarity` |

---

## 稀有度枚举值

| 稀有度 | 枚举值 | 中文标签 | 亮色模式色值 | 暗色模式色值 |
|:---|:---|:---|:---|:---|
| 普通 | `Normal` | 普通 | `#6e6e73` | `#f4f1ff` |
| 稀有 | `Rare` | 稀有 | `#0071e3` | `#59a6ff` |
| 史诗 | `Epic` | 史诗 | `#af52de` | `#c282ff` |
| 传说 | `Legendary` | 传说 | `#ff9500` | `#ff9b52` |

---

## 前端实现说明

### CSS 变量统一管理

品质颜色通过 CSS 变量定义在 `variables.css` 中，自动适配暗色模式：

```css
:root {
  --rarity-normal: #6e6e73;
  --rarity-rare: #0071e3;
  --rarity-epic: #af52de;
  --rarity-legendary: #ff9500;
}

[data-theme='dark'] {
  --rarity-normal: #f4f1ff;
  --rarity-rare: #59a6ff;
  --rarity-epic: #c282ff;
  --rarity-legendary: #ff9b52;
}
```

### 品质光效等级

| 稀有度 | 边框 | 底纹 | 扫光 | 特殊效果 |
|:---|:---|:---|:---|:---|
| 普通 | 稀有度颜色边框 | 淡渐变 | 基础扫光 | 无 |
| 稀有 | 稀有度颜色边框 | 淡渐变 | 基础扫光 | 无 |
| 史诗 | 稀有度颜色边框 | 加强渐变 | 加强扫光 | 呼吸光效（3s 循环） |
| 传说 | 稀有度颜色边框 | 强渐变 | 强烈扫光 | 脉动光晕（2.5s 循环） |

### 应用场景

- **背包格子**（`BackpackGrid.vue`）：物品格子边框、底纹、角标、光效
- **装备槽位**（`CharacterEquipmentGrid.vue`）：装备格子边框、底纹、角标、光效
- **物品详情弹窗**（`ItemDetailModal.vue`）：名称颜色、图标边框颜色、背景色
- **装备详情卡片**（`CharacterEquipmentGrid.vue`）：装备名称颜色

---

## 后端无需改动

此功能为纯前端渲染逻辑，后端无需新增接口或修改数据结构。后端只需确保物品和装备数据中包含正确的 `rarity` 字段即可。

---

如有疑问请联系前端对接人。
