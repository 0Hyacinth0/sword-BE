# 世界地图页面 - 后端对接文档

## 概述

世界地图页面展示游戏中的 4 个区域，玩家根据角色等级解锁不同区域。当前版本使用前端静态配置 + Mock API，后续可迁移至后端数据库管理区域数据。

---

## 数据结构

### MapArea

```typescript
interface MapArea {
  id: string                  // 区域 ID
  name: string                // 区域名称
  icon: string                // emoji 图标
  levelRange: [number, number] // 等级范围 [min, max]
  description: string         // 区域描述
  monsters: AreaMonster[]     // 怪物列表
  drops: AreaDrop[]           // 掉落预览
  unlockLevel: number         // 解锁所需等级
}
```

### AreaMonster

```typescript
interface AreaMonster {
  id: string
  name: string
  level: number
  type: 'normal' | 'elite' | 'boss'
}
```

### AreaDrop

```typescript
interface AreaDrop {
  itemId: number
  name: string
  rarity: 'Rare' | 'Epic' | 'Legendary'
}
```

### AreaStatus

```typescript
type AreaStatus = 'current' | 'unlocked' | 'locked' | 'undiscovered'
```

**状态判定逻辑**：
- `current`：角色等级在区域等级范围内（levelRange[0] <= level <= levelRange[1]）
- `unlocked`：角色等级 >= 解锁等级（unlockLevel），但不在当前等级范围内
- `locked`：角色等级 < 解锁等级，但该区域是下一个可解锁的区域
- `undiscovered`：角色等级远低于解锁等级（跳过了至少一个区域）

---

## 区域配置

### 当前：前端静态配置

区域数据硬编码在 `src/config/map_config.ts`，4 个区域：

| 区域 ID | 名称 | 等级范围 | 解锁等级 | 怪物 | 掉落 |
|:---|:---|:---:|:---:|:---|:---|
| `mist_forest` | 迷雾森林 🌲 | 1-10 | 1 | 史莱姆、小精灵、森林蜘蛛(精英)、古树守卫(Boss) | 木盾碎片(R)、精灵之尘(R)、树心法杖(E) |
| `bone_wasteland` | 白骨荒野 💀 | 11-20 | 11 | 骷髅、僵尸、骨法师(精英)、骷髅将军(Boss) | 骨剑(R)、亡灵护符(E)、将军战盔(L) |
| `fire_valley` | 火焰山谷 🔥 | 21-30 | 21 | 火元素、炎魔、熔岩石魔(精英)、炼狱领主(Boss) | 烈焰之刃(E)、熔岩核心(E)、炼狱权杖(L) |
| `frost_snowfield` | 冰霜雪原 ❄️ | 31-40 | 31 | 冰巨人、雪狼、冰霜女巫(精英)、霜龙(Boss) | 霜之哀伤(E)、冰晶护甲(E)、龙魂之心(L) |

### 未来：后端数据库管理

当区域数量增加或需要动态调整时，可迁移到后端：

**建议数据库表**：

```sql
-- 区域配置表
CREATE TABLE map_areas (
  id VARCHAR(32) PRIMARY KEY,
  name VARCHAR(50) NOT NULL,
  icon VARCHAR(10),
  level_min INTEGER NOT NULL,
  level_max INTEGER NOT NULL,
  description TEXT,
  unlock_level INTEGER NOT NULL,
  sort_order INTEGER DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 区域怪物配置表
CREATE TABLE area_monsters (
  id VARCHAR(32) PRIMARY KEY,
  area_id VARCHAR(32) REFERENCES map_areas(id),
  name VARCHAR(50) NOT NULL,
  level INTEGER NOT NULL,
  type ENUM('normal', 'elite', 'boss') NOT NULL,
  sort_order INTEGER DEFAULT 0
);

-- 区域掉落配置表
CREATE TABLE area_drops (
  id SERIAL PRIMARY KEY,
  area_id VARCHAR(32) REFERENCES map_areas(id),
  item_id INTEGER REFERENCES items(id),
  rarity ENUM('Rare', 'Epic', 'Legendary') NOT NULL,
  drop_rate DECIMAL(5,4) DEFAULT 0.1
);
```

---

## API 接口

### Base URL

```
/jeecg-boot/webgame
```

### 认证

需要 JWT Token，请求头 `Authorization: Bearer <token>`

---

### 1. 获取区域列表

**GET** `/map/areas`

获取所有已配置的区域列表。

**请求参数**：无

**响应**：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    {
      "id": "mist_forest",
      "name": "迷雾森林",
      "icon": "🌲",
      "levelRange": [1, 10],
      "description": "笼罩在浓雾中的古老森林...",
      "unlockLevel": 1,
      "monsters": [...],
      "drops": [...]
    }
  ]
}
```

**后端逻辑**：
1. 查询 `map_areas` 表（按 sort_order 排序）
2. 关联查询 `area_monsters` 和 `area_drops`
3. 组装返回

---

### 2. 获取区域详情

**GET** `/map/area/{areaId}`

获取单个区域的详细信息，包括探索次数。

**路径参数**：

| 参数 | 类型 | 说明 |
|:---|:---|:---|
| `areaId` | String | 区域 ID |

**响应**：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "area": {
      "id": "mist_forest",
      "name": "迷雾森林",
      ...
    },
    "exploreCount": 3
  }
}
```

**后端逻辑**：
1. 根据 areaId 查询区域配置
2. 查询该角色在该区域的今日探索次数
3. 返回区域数据 + 探索计数

---

### 3. 进入区域探索

**POST** `/map/enter`

进入指定区域，触发野外战斗（随机遭遇怪物）。

**请求参数**：

```json
{
  "characterId": "uuid-string",
  "areaId": "mist_forest"
}
```

| 字段 | 类型 | 必填 | 说明 |
|:---|:---|:---:|:---|
| `characterId` | String | ✅ | 角色 ID |
| `areaId` | String | ✅ | 区域 ID |

**校验逻辑**：
1. 验证 characterId 属于当前登录用户
2. 验证 areaId 存在
3. 验证角色等级 >= 区域 unlockLevel
4. 可选：检查每日探索次数限制
5. 随机选择一只该区域的怪物（根据权重）
6. 创建战斗实例

**响应**：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "areaId": "mist_forest",
    "message": "进入了迷雾森林"
  }
}
```

**错误码**：

| code | 说明 |
|:---:|:---|
| 403 | 等级不足，无法进入该区域 |
| 404 | 区域不存在 |
| 429 | 今日探索次数已达上限 |

---

### 4. 进入副本

**POST** `/map/dungeon/enter`

进入指定区域的副本（阶段六后续功能）。

**请求参数**：

```json
{
  "characterId": "uuid-string",
  "areaId": "mist_forest",
  "dungeonId": "mist_forest_dungeon_1"
}
```

**当前状态**：预留接口，后续实现。

---

## 解锁规则

| 规则 | 说明 |
|:---|:---|
| 解锁条件 | 角色等级 >= 区域 unlockLevel |
| 当前区域 | 角色等级在区域 levelRange 范围内 |
| 前端计算 | 解锁状态由前端根据角色等级实时计算，不需要后端接口 |

---

## 前端实现文件

| 文件 | 职责 |
|:---|:---|
| `src/types/map.ts` | MapArea、AreaMonster、AreaDrop、AreaStatus 类型定义 |
| `src/config/map_config.ts` | 4 个区域的静态配置数据 |
| `src/stores/map.ts` | 地图状态管理（展开/折叠、解锁状态计算） |
| `src/api/map.ts` | 地图 API + Mock 数据 |
| `src/views/MapView.vue` | 世界地图页面 |
| `src/router/index.ts` | /map 路由 |

---

## 功能清单

| 功能 | 说明 |
|:---|:---|
| 区域卡片展示 | 4 个区域以 Liquid Glass 卡片纵向排列 |
| 解锁状态判断 | 根据角色等级计算（current/unlocked/locked/undiscovered） |
| 当前区域高亮 | 橙色边框 + 光晕效果 |
| 未解锁锁定 | 虚线边框，显示所需等级 |
| 未发现模糊 | 模糊效果，显示 "???" |
| 展开详情面板 | 点击已解锁区域展开，显示怪物和掉落信息 |
| 怪物类型着色 | 普通蓝色、精英紫色、Boss 橙色 |
| 掉落稀有度着色 | 稀有蓝、史诗紫、传说橙 |
| 进入探索按钮 | 触发野外战斗（对接后续功能） |
| 副本按钮 | 进入副本（对接后续功能） |
| 展开折叠动画 | max-height + opacity 过渡 |
| 响应式适配 | 720px 断点适配移动端 |

---

*文档版本: 1.0.0*
*最后更新: 2026-05-08*
