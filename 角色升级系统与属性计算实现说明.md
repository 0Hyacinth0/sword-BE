# 角色升级系统与属性计算实现说明

## 📋 实现概述

根据《角色升级系统-后端对接文档.md》和《角色属性计算公式-后端对接文档.md》，已完成以下功能的实现：

1. ✅ **经验升级系统** - 完整的经验获取、升级处理逻辑
2. ✅ **属性计算公式** - 前后端一致的属性计算逻辑
3. ✅ **角色头像支持** - 添加头像和立绘字段

---

## 🎯 一、经验升级系统实现

### 1.1 核心配置参数

已在 [ExperienceCalculator](file:///G:/Work/网页小游戏/网页小游戏后端/jeecg-boot/jeecg-boot-module/jeecg-module-webgame/src/main/java/org/jeecg/modules/webgame/character/util/ExperienceCalculator.java) 中实现：

```java
MAX_LEVEL = 100              // 最大等级上限
POINTS_PER_LEVEL = 3         // 每升一级获得的自由属性点数
BASE_EXP = 100               // 基础经验值系数
EXP_MULTIPLIER = 1.15        // 经验增长系数（指数曲线）
EXP_LINEAR = 50              // 线性增长系数
```

### 1.2 经验公式

**升级所需经验计算：**
```
nextExp = floor(BASE_EXP × EXP_MULTIPLIER^level + EXP_LINEAR × level)
```

**Java实现：**
```java
public static int calculateNextLevelExp(int level) {
    if (level >= MAX_LEVEL) return 0;
    return (int) Math.floor(BASE_EXP * Math.pow(EXP_MULTIPLIER, level) + EXP_LINEAR * level);
}
```

### 1.3 升级逻辑

实现了连续升级处理，支持一次性获得大量经验时跨越多个等级：

```java
public static LevelUpResultVO calculateLevelUp(int currentLevel, int currentExp, int expToAdd) {
    // 循环升级直到经验不足或满级
    while (level < MAX_LEVEL) {
        int nextExp = calculateNextLevelExp(level);
        if (exp < nextExp) break;
        
        exp -= nextExp;
        level++;
        levelsGained++;
    }
    
    int pointsGained = levelsGained * POINTS_PER_LEVEL;
    // ...返回升级结果
}
```

### 1.4 新增接口

#### POST `/webgame/character/add-experience`

**请求参数：**
```json
{
  "characterId": "uuid",
  "expToAdd": 500
}
```

**成功响应（有升级）：**
```json
{
  "code": 200,
  "message": "恭喜升级！获得 3 点属性点",
  "data": {
    "character": {
      "id": "uuid",
      "level": 16,
      "experience": 800,
      "nextLevelExp": 759,
      "availablePoints": 6,
      // ... 其他角色字段
    },
    "levelUp": {
      "oldLevel": 15,
      "newLevel": 16,
      "levelsGained": 1,
      "pointsGained": 3,
      "overflowExp": 800,
      "isNewMaxLevel": false
    }
  }
}
```

**成功响应（无升级）：**
```json
{
  "code": 200,
  "message": "经验增加成功",
  "data": {
    "character": { ... },
    "levelUp": null
  }
}
```

### 1.5 特性说明

1. **满级处理**：达到100级后，不再增加经验，但仍返回角色数据
2. **溢出经验**：连续升级时，多余经验会累积到最终等级
3. **属性点累积**：连续升级获得的属性点自动累加到 `availablePoints`
4. **并发安全**：使用 `@Transactional` 保证事务一致性

---

## 🧮 二、属性计算公式实现

### 2.1 公式参数常量

已在 [CharacterStatsCalculator](file:///G:/Work/网页小游戏/网页小游戏后端/jeecg-boot/jeecg-boot-module/jeecg-module-webgame/src/main/java/org/jeecg/modules/webgame/character/util/CharacterStatsCalculator.java) 中实现：

```java
STR_TO_HP = 5                  // 1 点力量 = 5 HP
INT_TO_MP = 5                  // 1 点智力 = 5 MP
STR_TO_PHYSICAL_ATTACK = 2     // 1 点力量 = 2 物攻
INT_TO_MAGIC_ATTACK = 2        // 1 点智力 = 2 魔攻
AGI_TO_PHYSICAL_ATTACK = 0.5   // 1 点敏捷 = 0.5 物攻
AGI_TO_DODGE = 0.005           // 1 点敏捷 = 0.5% 闪避率
AGI_TO_CRITICAL = 0.004        // 1 点敏捷 = 0.4% 暴击率
BASE_HP = 80                   // HP 基础值
BASE_MP = 20                   // MP 基础值
BASE_DEFENSE = 5               // 防御基础值
```

### 2.2 职业加成系数

| 职业 | HP倍率 | MP倍率 | 物攻倍率 | 魔攻倍率 | 防御加成 |
|:---:|:---:|:---:|:---:|:---:|:---:|
| 战士 | 1.2 | 0.8 | 1.15 | 0.8 | +3 |
| 法师 | 0.8 | 1.3 | 0.7 | 1.2 | +0 |
| 猎人 | 0.9 | 0.9 | 1.0 | 0.9 | +1 |

### 2.3 计算公式

```java
// HP = (基础值 + 力量×5) × 职业倍率
int maxHp = (int) Math.floor((BASE_HP + str * STR_TO_HP) * bonus.getHpMultiplier());

// MP = (基础值 + 智力×5) × 职业倍率
int maxMp = (int) Math.floor((BASE_MP + intelligence * INT_TO_MP) * bonus.getMpMultiplier());

// 物攻 = (力量×2 + 敏捷×0.5) × 职业倍率
int physicalAttack = (int) Math.floor(
    (str * STR_TO_PHYSICAL_ATTACK + agi * AGI_TO_PHYSICAL_ATTACK) 
    * bonus.getPhysicalAttackMultiplier()
);

// 魔攻 = 智力×2 × 职业倍率
int magicAttack = (int) Math.floor(
    (intelligence * INT_TO_MAGIC_ATTACK) * bonus.getMagicAttackMultiplier()
);

// 防御 = 基础值 + 职业额外加成
int defense = BASE_DEFENSE + bonus.getDefenseBonus();

// 闪避率 = 敏捷×0.005
double dodgeRate = agi * AGI_TO_DODGE;

// 暴击率 = 敏捷×0.004
double criticalRate = agi * AGI_TO_CRITICAL;
```

### 2.4 应用场景

#### 1. 创建角色时
自动根据职业初始化属性并计算衍生属性。

#### 2. 属性加点时
调用 `POST /webgame/character/update-attributes` 接口，后端会自动重新计算所有衍生属性。

#### 3. 升级时
如果升级导致可用属性点变化，下次加点时会基于新的属性点重新计算。

---

## 🖼️ 三、角色头像支持

### 3.1 数据库字段

已添加两个新字段到 `wg_character` 表：

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `avatar_url` | VARCHAR(255) | 角色头像URL（小图，用于角色列表卡片） |
| `portrait_url` | VARCHAR(255) | 角色立绘URL（大图，用于角色详情面板） |

### 3.2 迁移脚本

创建了Flyway迁移脚本：
[V3.9.2_1__webgame_character_add_avatar.sql](file:///G:/Work/网页小游戏/网页小游戏后端/jeecg-boot/jeecg-module-system/jeecg-system-start/src/main/resources/flyway/sql/mysql/V3.9.2_1__webgame_character_add_avatar.sql)

### 3.3 资源命名规则

```
/assets/avatar/{profession}_{gender}.png      // 头像（建议 64×64）
/assets/portrait/{profession}_{gender}.png     // 立绘（建议 280×400）
```

| profession | 名称 |
|:---:|:---|
| 1 | warrior |
| 2 | mage |
| 3 | hunter |

**说明**：前期无美术资源时，这两个字段返回 `null`，前端显示职业图标占位。

---

## 📁 四、文件清单

### 新增文件

1. **DTO类**
   - [AddExperienceDTO.java](file:///G:/Work/网页小游戏/网页小游戏后端/jeecg-boot/jeecg-boot-module/jeecg-module-webgame/src/main/java/org/jeecg/modules/webgame/character/dto/AddExperienceDTO.java) - 增加经验请求参数

2. **VO类**
   - [LevelUpResultVO.java](file:///G:/Work/网页小游戏/网页小游戏后端/jeecg-boot/jeecg-boot-module/jeecg-module-webgame/src/main/java/org/jeecg/modules/webgame/character/vo/LevelUpResultVO.java) - 升级结果
   - [AddExperienceResultVO.java](file:///G:/Work/网页小游戏/网页小游戏后端/jeecg-boot/jeecg-boot-module/jeecg-module-webgame/src/main/java/org/jeecg/modules/webgame/character/vo/AddExperienceResultVO.java) - 增加经验响应

3. **工具类**
   - [ExperienceCalculator.java](file:///G:/Work/网页小游戏/网页小游戏后端/jeecg-boot/jeecg-boot-module/jeecg-module-webgame/src/main/java/org/jeecg/modules/webgame/character/util/ExperienceCalculator.java) - 经验计算工具类

4. **数据库脚本**
   - [V3.9.2_0__webgame_character.sql](file:///G:/Work/网页小游戏/网页小游戏后端/jeecg-boot/jeecg-module-system/jeecg-system-start/src/main/resources/flyway/sql/mysql/V3.9.2_0__webgame_character.sql) - 创建角色表
   - [V3.9.2_1__webgame_character_add_avatar.sql](file:///G:/Work/网页小游戏/网页小游戏后端/jeecg-boot/jeecg-module-system/jeecg-system-start/src/main/resources/flyway/sql/mysql/V3.9.2_1__webgame_character_add_avatar.sql) - 添加头像字段

### 修改文件

1. **实体类**
   - [WgCharacter.java](file:///G:/Work/网页小游戏/网页小游戏后端/jeecg-boot/jeecg-boot-module/jeecg-module-webgame/src/main/java/org/jeecg/modules/webgame/character/entity/WgCharacter.java) - 添加 avatarUrl 和 portraitUrl 字段

2. **VO类**
   - [CharacterVO.java](file:///G:/Work/网页小游戏/网页小游戏后端/jeecg-boot/jeecg-boot-module/jeecg-module-webgame/src/main/java/org/jeecg/modules/webgame/character/vo/CharacterVO.java) - 添加头像字段

3. **Service接口**
   - [IWgCharacterService.java](file:///G:/Work/网页小游戏/网页小游戏后端/jeecg-boot/jeecg-boot-module/jeecg-module-webgame/src/main/java/org/jeecg/modules/webgame/character/service/IWgCharacterService.java) - 添加 addExperience 方法

4. **Service实现**
   - [WgCharacterServiceImpl.java](file:///G:/Work/网页小游戏/网页小游戏后端/jeecg-boot/jeecg-boot-module/jeecg-module-webgame/src/main/java/org/jeecg/modules/webgame/character/service/impl/WgCharacterServiceImpl.java) - 实现 addExperience 方法

5. **Controller**
   - [WgCharacterController.java](file:///G:/Work/网页小游戏/网页小游戏后端/jeecg-boot/jeecg-boot-module/jeecg-module-webgame/src/main/java/org/jeecg/modules/webgame/character/controller/WgCharacterController.java) - 添加 addExperience 接口

---

## 🧪 五、测试建议

### 5.1 经验升级测试

#### 测试用例1：正常增加经验（不升级）
```bash
curl -X POST "http://localhost:8080/webgame/character/add-experience" \
  -H "Content-Type: application/json" \
  -d '{
    "characterId": "your-character-id",
    "expToAdd": 50
  }'
```

**预期结果**：
- `levelUp` 为 `null`
- `experience` 增加 50
- 等级不变

#### 测试用例2：升级
```bash
curl -X POST "http://localhost:8080/webgame/character/add-experience" \
  -H "Content-Type: application/json" \
  -d '{
    "characterId": "your-character-id",
    "expToAdd": 500
  }'
```

**预期结果**：
- `levelUp` 不为 `null`
- `level` 增加
- `availablePoints` 增加 3（每级3点）
- `experience` 为溢出值

#### 测试用例3：连续升级
```bash
curl -X POST "http://localhost:8080/webgame/character/add-experience" \
  -H "Content-Type: application/json" \
  -d '{
    "characterId": "your-character-id",
    "expToAdd": 5000
  }'
```

**预期结果**：
- `levelsGained` > 1
- `pointsGained` = `levelsGained` × 3
- 正确计算溢出经验

#### 测试用例4：满级处理
```bash
# 先将角色升到100级，然后尝试继续增加经验
curl -X POST "http://localhost:8080/webgame/character/add-experience" \
  -H "Content-Type: application/json" \
  -d '{
    "characterId": "your-character-id",
    "expToAdd": 10000
  }'
```

**预期结果**：
- 等级保持100
- `levelUp` 为 `null`
- 经验不增加

### 5.2 属性加点测试

#### 测试用例1：正常加点
```bash
curl -X POST "http://localhost:8080/webgame/character/update-attributes" \
  -H "Content-Type: application/json" \
  -d '{
    "characterId": "your-character-id",
    "str": 2,
    "intelligence": 1,
    "agi": 0
  }'
```

**预期结果**：
- 属性点正确减少
- 衍生属性（HP、MP、攻击力等）根据公式重新计算
- 符合职业加成系数

#### 测试用例2：点数不足
```bash
curl -X POST "http://localhost:8080/webgame/character/update-attributes" \
  -H "Content-Type: application/json" \
  -d '{
    "characterId": "your-character-id",
    "str": 100,
    "intelligence": 0,
    "agi": 0
  }'
```

**预期结果**：
- 返回错误："可用属性点不足"

### 5.3 经验等级对照验证

验证前20级的经验值是否与文档一致：

| 等级 | 预期经验 | 实际经验 | 是否一致 |
|:---:|:---:|:---:|:---:|
| 1 | 100 | ? | ⏳ |
| 2 | 115 | ? | ⏳ |
| 3 | 132 | ? | ⏳ |
| ... | ... | ... | ... |

可以使用以下代码验证：
```java
for (int i = 1; i <= 20; i++) {
    System.out.println("等级 " + i + ": " + ExperienceCalculator.calculateNextLevelExp(i));
}
```

---

## ⚠️ 六、注意事项

### 6.1 精度处理
- HP/MP/攻击/防御使用 `Math.floor()` 取整
- 闪避/暴击保留小数形式（如 0.05 表示 5%）

### 6.2 前后端一致性
确保前端使用相同的：
- 经验公式参数
- 职业加成系数
- 属性计算公式

### 6.3 并发安全
- 增加经验和属性加点都使用了 `@Transactional` 保证事务一致性
- 建议在业务层添加分布式锁防止并发问题

### 6.4 数据库迁移
执行顺序：
1. 先执行 `V3.9.2_0__webgame_character.sql`（如果表不存在）
2. 再执行 `V3.9.2_1__webgame_character_add_avatar.sql`

或者直接使用最新的完整建表脚本。

### 6.5 Redis缓存清理
如果之前有Redis缓存问题，记得在重启应用前清理Shiro缓存：
```bash
redis-cli KEYS "shiro:*" | xargs redis-cli DEL
```

---

## 📊 七、接口汇总

| 接口 | 方法 | 路径 | 说明 |
|:---|:---:|:---|:---|
| 创建角色 | POST | `/webgame/character/create` | 创建新角色 |
| 获取角色详情 | GET | `/webgame/character/info/{id}` | 获取角色完整信息 |
| 属性加点 | POST | `/webgame/character/update-attributes` | 分配属性点 |
| **增加经验** | **POST** | **`/webgame/character/add-experience`** | **获取经验并处理升级** |
| 获取角色列表 | GET | `/webgame/character/list` | 获取用户的所有角色 |
| 删除角色 | DELETE | `/webgame/character/delete/{id}` | 删除角色 |

---

## 🎉 八、完成状态

| 功能 | 状态 | 备注 |
|:---|:---:|:---|
| 经验计算公式 | ✅ | 已实现并与文档一致 |
| 升级逻辑 | ✅ | 支持连续升级 |
| 增加经验接口 | ✅ | 已实现并返回升级结果 |
| 属性计算公式 | ✅ | 已验证与文档一致 |
| 职业加成系数 | ✅ | 三个职业均已配置 |
| 角色头像字段 | ✅ | 已添加数据库字段和VO字段 |
| 数据库迁移脚本 | ✅ | 已创建Flyway脚本 |
| 并发安全 | ✅ | 使用事务保证一致性 |

---

*实现日期：2026-05-08*  
*对应文档版本：v1.0.0*  
*实现人：AI Assistant*
