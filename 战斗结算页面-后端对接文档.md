# 战斗结算页面 - 后端对接文档

## 概述

本文档定义回合制战斗结算页面的数据结构、奖励计算规则和 API 接口。战斗结算在战斗结束时触发，展示经验/金币/物品奖励和战斗统计数据。

---

## 结算触发条件

| 结果 | 触发条件 | 说明 |
|------|----------|------|
| `victory` | 所有敌人 `isAlive = false` | 玩家方胜利 |
| `defeat` | 所有友方 `isAlive = false` | 玩家方失败 |
| `fled` | 玩家选择逃跑且成功 | 按概率判定 |

---

## 核心数据结构

### 1. 战斗奖励 (BattleRewards)

```typescript
interface BattleRewards {
  /** 获得经验 */
  exp: number
  /** 获得金币 */
  gold: number
  /** 获得物品列表 */
  items: BattleRewardItem[]
  /** 战宠获得经验（可选） */
  petExp?: number
  /** 是否升级（后端计算） */
  levelUp?: boolean
  /** 升级后等级 */
  newLevel?: number
}
```

### 2. 掉落物品 (BattleRewardItem)

```typescript
interface BattleRewardItem {
  /** 物品 ID */
  itemId: number
  /** 物品名称 */
  name: string
  /** 物品数量 */
  quantity: number
  /** 物品品质（用于前端着色） */
  quality?: 'common' | 'rare' | 'epic' | 'legendary'
  /** 物品类型（用于前端图标） */
  itemType?: 'equipment' | 'material' | 'consumable' | 'pet_egg'
}
```

### 3. 战斗统计 (BattleStatistics)

```typescript
interface BattleStatistics {
  /** 战斗总回合数 */
  totalRounds: number
  /** 玩家造成的总伤害 */
  totalDamageDealt: number
  /** 玩家承受的总伤害 */
  totalDamageTaken: number
  /** 玩家治疗总量 */
  totalHealed: number
  /** 暴击次数 */
  criticalHits: number
  /** 闪避次数 */
  dodgeCount: number
  /** 击杀敌人数 */
  enemiesKilled: number
}
```

---

## 奖励计算规则

### 经验公式

```
enemyExp = floor(enemy.maxHp × 0.5 + enemy.physicalAttack × 2)
totalExp = Σ(enemyExp)
petExp = floor(totalExp × 0.3)
```

### 金币公式

```
enemyGold = floor(enemy.maxHp × 0.2 + enemy.speed)
totalGold = Σ(enemyGold)
```

### 升级判定

后端根据以下逻辑判断是否升级：

```
newExp = character.exp + totalExp
if (newExp >= levelUpThreshold(character.level)) {
  levelUp = true
  newLevel = character.level + 1
  // 剩余经验转入下一级
  remainingExp = newExp - levelUpThreshold(character.level)
}
```

升级所需经验查 `level_config` 表。

---

## 掉落物品规则

### 掉落判定

每个被击败的敌人独立判定是否掉落物品：

| 掉落概率 | 判定方式 |
|----------|----------|
| 15% | `random() < 0.15` |

### 掉落物品类型分布

| 类型 | 概率 | 说明 |
|------|------|------|
| 装备 | 30% | 随机品质（见下表） |
| 材料 | 40% | 敌人相关材料 |
| 消耗品 | 30% | 回复药水等 |

### 装备品质分布

| 品质 | 概率 | 前端颜色 |
|------|------|----------|
| common（普通） | 60% | 默认色 |
| rare（稀有） | 25% | 蓝色 `var(--accent-blue)` |
| epic（史诗） | 12% | 紫色 `#af52de` |
| legendary（传说） | 3% | 金色 `var(--accent-gold)` |

### 副本掉落加成

| 副本类型 | 掉落率修正 | 品质加成 |
|----------|-----------|---------|
| 普通副本 | ×1.0 | 无 |
| 精英副本 | ×2.0 | rare+概率 +15% |
| 团队副本 | ×3.0 | epic+概率 +20% |

---

## API 接口

### POST `/battle/end/{battleId}`

结束战斗并获取结算数据。

**请求路径参数：**

| 参数 | 类型 | 说明 |
|------|------|------|
| battleId | String | 战斗 ID |

**响应：**

```json
{
  "code": 200,
  "message": "战斗胜利！",
  "data": {
    "outcome": "victory",
    "rewards": {
      "exp": 150,
      "gold": 45,
      "items": [
        {
          "itemId": 8023,
          "name": "精制铁剑",
          "quantity": 1,
          "quality": "rare",
          "itemType": "equipment"
        },
        {
          "itemId": 4003,
          "name": "暗影狼的碎片",
          "quantity": 2,
          "quality": "common",
          "itemType": "material"
        }
      ],
      "petExp": 45,
      "levelUp": false
    },
    "statistics": {
      "totalRounds": 8,
      "totalDamageDealt": 320,
      "totalDamageTaken": 85,
      "totalHealed": 60,
      "criticalHits": 2,
      "dodgeCount": 1,
      "enemiesKilled": 2
    }
  }
}
```

**失败/逃跑响应：**

```json
{
  "code": 200,
  "message": "战斗失败",
  "data": {
    "outcome": "defeat",
    "rewards": {
      "exp": 0,
      "gold": 0,
      "items": []
    },
    "statistics": {
      "totalRounds": 5,
      "totalDamageDealt": 120,
      "totalDamageTaken": 250,
      "totalHealed": 30,
      "criticalHits": 0,
      "dodgeCount": 0,
      "enemiesKilled": 1
    }
  }
}
```

---

## 前端展示规范

### 奖励数字动画

经验/金币/战宠经验数值采用递增动画展示：
- 延迟 400ms 后启动（等待卡片入场动画完成）
- 动画时长 800ms，使用 ease-out 缓动
- 公式：`displayValue = floor(target × (1 - (1 - progress)^3))`

### 物品品质颜色

遵循 DESIGN.md 稀有度色彩规范：

| 品质 | 亮色模式 | 暗色模式 | 用途 |
|------|----------|----------|------|
| common | `#6e6e73` | `#f4f1ff` | 默认 |
| rare | `#0071e3` | `#59a6ff` | 蓝色 |
| epic | `#af52de` | `#c282ff` | 紫色 |
| legendary | `#ff9500` | `#ff9b52` | 金色 |

### 物品卡片左侧边框

- `common`: 无特殊边框
- `rare`: 3px 蓝色左边框
- `epic`: 3px 紫色左边框
- `legendary`: 3px 金色左边框

### 升级提示

当 `levelUp = true` 时：
- 显示金色渐变横幅
- 文字：`LEVEL UP! Lv.{newLevel}`
- 带呼吸光晕动画

---

## 结算页面分区

```
┌──────────────────────────────┐
│  结果图标 + 标题 + 副标题     │  ← 胜利/失败/逃跑
├──────────────────────────────┤
│  EXP  ████████████  150      │  ← 经验值（带动画）
│  G    ████████████  45       │  ← 金币（带动画）
│  PET  ████████████  45       │  ← 战宠经验（可选）
├──────────────────────────────┤
│  [LEVEL UP! Lv.12]          │  ← 升级横幅（条件触发）
├──────────────────────────────┤
│  战利品                      │
│  ┌────────────────────────┐ │
│  │ E 精制铁剑         x1  │ │  ← 蓝色左边框（稀有）
│  │ M 暗影狼的碎片     x2  │ │
│  └────────────────────────┘ │
├──────────────────────────────┤
│  战斗统计                    │
│  ┌──────┐ ┌──────┐ ┌──────┐│
│  │  8   │ │ 320  │ │  2   ││
│  │总回合│ │总伤害│ │ 暴击 ││
│  └──────┘ └──────┘ └──────┘│
│  ┌──────┐ ┌──────┐ ┌──────┐│
│  │  1   │ │  60  │ │  2   ││
│  │ 闪避 │ │ 治疗 │ │ 击杀 ││
│  └──────┘ └──────┘ └──────┘│
├──────────────────────────────┤
│  [ 领取奖励 / 返回 ]         │  ← 操作按钮
└──────────────────────────────┘
```

---

## 后端处理流程

1. 战斗状态机进入 `BATTLE_END` 阶段
2. 根据 `outcome` 计算奖励：
   - `victory`：计算经验、金币、掉落物品、升级判定
   - `defeat`/`fled`：奖励为 0
3. 从战斗日志提取统计数据
4. 返回 `BattleEndResponse`
5. 前端调用 `/battle/end/{battleId}` 后，后端：
   - 将奖励写入角色数据（经验增加、金币增加、物品入背包）
   - 将战宠经验写入战宠数据
   - 处理升级（等级+1、属性点增加）
   - 记录战斗历史（可选）

---

## 注意事项

1. **奖励幂等性**：`/battle/end` 接口需防止重复调用，同一 battleId 只能结算一次
2. **掉落随机性**：后端使用配置化的掉落表，掉落物品由后端决定（前端仅展示）
3. **升级计算**：升级阈值查 `level_config` 表，后端返回 `levelUp` 标记
4. **战宠经验**：出战战宠获得角色经验的 30%
5. **失败惩罚**：当前版本失败不扣除任何东西，后续可加惩罚规则

---

*文档版本: 1.0.0*
*最后更新: 2026-05-07*
