# 野外刷怪（遭遇 → 进入战斗） - 后端对接文档

## 概述

玩家在世界地图区域点击"进入探索"按钮后，前端随机选择一只区域怪物并生成战斗单位数据，直接调用本地战斗引擎发起野外战斗。当前版本为纯前端战斗（不依赖后端 API），后续可扩展为服务器权威模式。

---

## 战斗流程

```
玩家点击"进入探索"
  → 前端从区域怪物列表中随机选择一只
  → 前端根据怪物等级/类型生成属性（createWildMonsterCombatant）
  → 前端获取角色属性和技能（calculateFullStats + getActiveBattleSkills）
  → 前端调用 battleStore.startWildBattle() 创建战斗状态
  → 前端自动推进战斗引擎到第一个玩家行动点
  → 跳转到 BattleView 进行回合制战斗
  → 战斗结束后结算奖励并返回主页
```

---

## 核心数据结构

### 野外怪物属性生成公式

怪物属性根据等级和类型缩放，前端硬编码公式：

```typescript
// 类型倍率
const typeMultiplier = monster.type === 'boss' ? 2.5
                     : monster.type === 'elite' ? 1.5
                     : 1.0
const lv = monster.level

// 属性计算
maxHp = floor((50 + lv × 15) × typeMultiplier)
maxMp = floor((20 + lv × 5) × typeMultiplier)
physicalAttack = floor((5 + lv × 3) × typeMultiplier)
magicAttack = floor((3 + lv × 2) × typeMultiplier)
defense = floor((3 + lv × 2) × typeMultiplier)
speed = floor(5 + lv × 1.5 × typeMultiplier)
dodgeRate = min(0.05 + lv × 0.005, 0.25)
criticalRate = min(0.03 + lv × 0.003, 0.2)
```

### 类型倍率对照

| 怪物类型 | 倍率 | 说明 |
|:---|:---:|:---|
| normal (普通) | 1.0 | 基础属性 |
| elite (精英) | 1.5 | 1.5 倍属性 |
| boss (Boss) | 2.5 | 2.5 倍属性 |

### 野外怪物通用技能

所有野外怪物共享以下技能模板：

| 技能 ID | 名称 | 威力 | 冷却 | MP 消耗 | 目标类型 |
|:---:|:---|:---:|:---:|:---:|:---|
| 9001 | 撕咬 | 120 | 0 | 0 | 单体敌方 |
| 9002 | 猛击 | 160 | 2 | 8 | 单体敌方 |

### Combatant 数据结构

```typescript
interface Combatant {
  uid: string                    // 唯一标识，格式：enemy-{monsterId}-{timestamp}
  sourceId: string               // 怪物 ID
  name: string                   // 怪物名称
  side: 'enemy'                  // 固定为敌方
  type: 'enemy'                  // 固定为敌人类型
  stats: {
    maxHp: number; hp: number
    maxMp: number; mp: number
    physicalAttack: number
    magicAttack: number
    defense: number
    speed: number
    dodgeRate: number
    criticalRate: number
  }
  skills: BattleSkill[]
  buffs: BuffEffect[]
  cooldowns: Record<number, number>
  isAlive: boolean
  actionValue: number            // ATB 行动值
}
```

---

## 前端实现（纯前端战斗）

当前版本的野外战斗完全在前端执行，不调用后端 API：

1. 怪物数据由前端根据区域配置和等级公式生成
2. 战斗引擎在前端本地运行（battleEngine.ts）
3. 战斗结算在前端本地计算
4. 无需后端创建/管理战斗会话

---

## 后端对接方案（未来扩展）

### 模式一：后端验证模式

前端仍本地计算战斗过程，但结算时后端验证：

**POST** `/battle/wild/settle`

战斗结束后提交结算数据，后端验证合法性并发放奖励。

**请求参数**：

```json
{
  "characterId": "uuid-string",
  "areaId": "mist_forest",
  "monsterId": "slime",
  "battleResult": "win",
  "rounds": 5,
  "battleLog": [...]
}
```

| 字段 | 类型 | 必填 | 说明 |
|:---|:---|:---:|:---|
| characterId | String | ✅ | 角色 ID |
| areaId | String | ✅ | 区域 ID |
| monsterId | String | ✅ | 击败的怪物 ID |
| battleResult | String | ✅ | 战斗结果：win / lose |
| rounds | Integer | ✅ | 战斗回合数 |
| battleLog | Array | ❌ | 战斗日志（用于反作弊校验） |

**响应**：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "exp": 25,
    "gold": 15,
    "items": [
      { "itemId": 2001, "name": "木盾碎片", "quantity": 1, "rarity": "Rare" }
    ],
    "levelUp": false,
    "petExp": 10
  }
}
```

**后端校验逻辑**：
1. 验证 characterId 属于当前登录用户
2. 验证 areaId 有效且角色等级满足
3. 验证 monsterId 属于该区域
4. 可选：根据角色属性和怪物属性模拟战斗，验证结果合理性
5. 计算奖励（经验、金币、掉落物品）
6. 发放奖励到角色

---

### 模式二：服务器权威模式

战斗全过程由后端驱动：

**POST** `/battle/wild/start`

发起野外战斗，后端创建战斗会话。

**请求参数**：

```json
{
  "characterId": "uuid-string",
  "areaId": "mist_forest"
}
```

**响应**：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "battleId": "battle-uuid",
    "enemies": [
      {
        "uid": "enemy-1",
        "name": "史莱姆",
        "stats": { "maxHp": 80, "hp": 80, ... },
        "skills": [...]
      }
    ],
    "actionOrder": ["player-1", "enemy-1"]
  }
}
```

**POST** `/battle/wild/action`

提交玩家行动。

**请求参数**：

```json
{
  "battleId": "battle-uuid",
  "action": {
    "type": "skill",
    "skillId": 5001,
    "targetUid": "enemy-1"
  }
}
```

**响应**：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "battleState": { ... },
    "damageResults": [...],
    "isBattleOver": false
  }
}
```

---

## 怪物属性配置（建议后端表）

当需要后端管理怪物属性时，建议新增以下配置表：

```sql
-- 野外怪物属性模板表
CREATE TABLE monster_templates (
  id VARCHAR(32) PRIMARY KEY,
  name VARCHAR(50) NOT NULL,
  type ENUM('normal', 'elite', 'boss') NOT NULL,
  base_hp INTEGER DEFAULT 50,
  hp_per_level DECIMAL(5,2) DEFAULT 15,
  base_mp INTEGER DEFAULT 20,
  mp_per_level DECIMAL(5,2) DEFAULT 5,
  base_physical_attack INTEGER DEFAULT 5,
  physical_attack_per_level DECIMAL(5,2) DEFAULT 3,
  base_magic_attack INTEGER DEFAULT 3,
  magic_attack_per_level DECIMAL(5,2) DEFAULT 2,
  base_defense INTEGER DEFAULT 3,
  defense_per_level DECIMAL(5,2) DEFAULT 2,
  base_speed INTEGER DEFAULT 5,
  speed_per_level DECIMAL(5,2) DEFAULT 1.5,
  type_multiplier DECIMAL(3,1) DEFAULT 1.0,
  skills JSON
);
```

---

## 奖励计算规则（参考）

### 经验值

```
baseExp = 10 + monster.level × 5
typeMultiplier = boss ? 3.0 : elite ? 2.0 : 1.0
totalExp = floor(baseExp × typeMultiplier)
```

### 金币

```
baseGold = 5 + monster.level × 2
typeMultiplier = boss ? 5.0 : elite ? 3.0 : 1.0
totalGold = floor(baseGold × typeMultiplier)
```

### 掉落物品

根据区域掉落表的稀有度和掉落率随机决定：

| 稀有度 | 基础掉落率 | 来源怪物类型 |
|:---|:---:|:---|
| Rare | 20% | 普通怪物 |
| Epic | 5% | 精英/Boss |
| Legendary | 1% | Boss |

---

## 前端实现文件

| 文件 | 职责 |
|:---|:---|
| `src/api/map.ts` | `createWildMonsterCombatant()` 怪物属性生成 + `WILD_MONSTER_SKILLS` 通用技能 |
| `src/stores/battle.ts` | `startWildBattle()` 发起野外战斗（纯前端） |
| `src/components/map/WorldMapPanel.vue` | `handleEnterArea()` 探索按钮处理 |
| `src/utils/battleEngine.ts` | 战斗引擎（回合制状态机） |
| `src/config/skill_config.ts` | `getActiveBattleSkills()` 获取角色技能 |
| `src/utils/attributeCalculator.ts` | `calculateFullStats()` 角色属性计算 |
| `src/config/map_config.ts` | 区域配置（怪物列表） |

---

*文档版本: 1.0.0*
*最后更新: 2026-05-08*
