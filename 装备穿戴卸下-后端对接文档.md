# 装备穿戴/卸下 - 后端对接文档

> 版本：v1.0.0
> 更新日期：2026-05-01
> 说明：装备穿戴与卸下接口规范

---

## 一、穿戴装备

**POST `/equipment/equip`**

**请求参数：**

```json
{
  "characterId": "a1b2c3d4-...",
  "inventoryId": "inv-011"
}
```

| 参数 | 类型 | 必填 | 说明 |
|:---|:---:|:---:|:---|
| `characterId` | string | ✅ | 角色 UUID |
| `inventoryId` | string | ✅ | 背包中装备物品的记录 ID |

**成功响应：**

```json
{
  "code": 200,
  "message": "装备成功",
  "data": { "角色完整信息（含更新后的装备）" }
}
```

**后端处理逻辑：**

1. 校验 `inventoryId` 是否存在且属于 `characterId`
2. 校验物品类型为 `equipment`（`category != 'equipment'` → 返回 400）
3. 根据物品的 `slotType` 确定目标槽位
4. 校验角色等级是否满足 `levelRequirement`
5. 若目标槽位已有装备：
   a. 将旧装备放回背包（插入 `character_inventory` 表，quantity=1）
   b. 若背包已满（32 种物品），返回错误
6. 从背包移除该物品（删除 `character_inventory` 记录）
7. 将新装备写入槽位（更新 `character_equipments` 表）
8. 重新计算角色衍生属性
9. 返回更新后的角色完整数据

**失败响应：**

```json
{ "code": 400, "message": "该物品不是装备", "data": null }
{ "code": 400, "message": "角色等级不足", "data": null }
{ "code": 400, "message": "背包已满，无法替换装备", "data": null }
{ "code": 404, "message": "物品不存在", "data": null }
```

---

## 二、卸下装备

**POST `/equipment/unequip`**

**请求参数：**

```json
{
  "characterId": "a1b2c3d4-...",
  "slotType": "weapon"
}
```

| 参数 | 类型 | 必填 | 说明 |
|:---|:---:|:---:|:---|
| `characterId` | string | ✅ | 角色 UUID |
| `slotType` | string | ✅ | 要卸下的槽位类型（weapon/helmet/chest/legs/accessory1/accessory2） |

**成功响应：**

```json
{
  "code": 200,
  "message": "卸下成功",
  "data": { "角色完整信息（含更新后的装备）" }
}
```

**后端处理逻辑：**

1. 查找该角色指定槽位的装备
2. 若槽位为空 → 返回 400
3. 校验背包是否有空间（物品种类 < 32）
4. 将装备放回背包（插入 `character_inventory`，quantity=1）
5. 清空该槽位（更新 `character_equipments` 表 slot 对应记录为 NULL）
6. 重新计算角色衍生属性
7. 返回更新后的角色完整数据

**失败响应：**

```json
{ "code": 400, "message": "该槽位没有装备", "data": null }
{ "code": 400, "message": "背包已满，无法卸下装备", "data": null }
{ "code": 404, "message": "角色不存在", "data": null }
```

---

## 三、前端交互流程

### 穿戴装备

1. 玩家在背包中点击装备类物品 → 弹出物品详情
2. 详情弹窗显示「装备」按钮（仅 equipment 类物品）
3. 点击「装备」→ 前端调用 `POST /equipment/equip`
4. 成功 → Toast 提示、刷新角色数据（装备栏更新）、刷新背包（物品消失）
5. 失败 → Toast 提示错误信息

### 卸下装备

1. 玩家在装备栏点击已有装备的槽位 → 展开装备详情卡片
2. 详情卡片底部显示「卸下装备」按钮
3. 点击「卸下装备」→ 前端调用 `POST /equipment/unequip`
4. 成功 → Toast 提示、刷新角色数据（槽位清空）、刷新背包（装备出现）
5. 失败 → Toast 提示错误信息

---

## 四、错误码汇总

| code | 场景 | 说明 |
|:---:|:---|:---|
| 200 | 成功 | 穿戴/卸下成功 |
| 400 | 物品类型错误 | 穿戴的物品不是装备类型 |
| 400 | 等级不足 | 角色等级不满足装备的 `levelRequirement` |
| 400 | 背包已满 | 卸下/替换时背包物品种类已达 32 |
| 400 | 槽位为空 | 卸下时指定槽位没有装备 |
| 403 | 无权限 | 操作非自己角色的装备 |
| 404 | 物品/角色不存在 | inventoryId 或 characterId 无效 |

---

如有疑问请联系前端对接人。
