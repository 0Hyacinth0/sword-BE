# 角色升级系统 - 快速启动指南

## 🚀 快速开始

### 第一步：执行数据库迁移

```bash
# 1. 确保 Flyway 已配置并启用
# 2. 启动应用时会自动执行以下脚本：
#    - V3.9.2_0__webgame_character.sql（创建角色表）
#    - V3.9.2_1__webgame_character_add_avatar.sql（添加头像字段）

# 或者手动执行SQL：
mysql -u root -p jeecg-boot < jeecg-module-system/jeecg-system-start/src/main/resources/flyway/sql/mysql/V3.9.2_0__webgame_character.sql
mysql -u root -p jeecg-boot < jeecg-module-system/jeecg-system-start/src/main/resources/flyway/sql/mysql/V3.9.2_1__webgame_character_add_avatar.sql
```

### 第二步：清理Redis缓存（如果之前有缓存问题）

```bash
# 方法1：使用 redis-cli
redis-cli KEYS "shiro:*" | xargs redis-cli DEL

# 方法2：重启 Redis
sudo systemctl restart redis
```

### 第三步：重启应用

```bash
# 停止应用
# 重新启动
mvn spring-boot:run
```

### 第四步：测试接口

#### 1. 创建角色
```bash
curl -X POST "http://localhost:8080/webgame/character/create" \
  -H "Content-Type: application/json" \
  -H "X-Access-Token: YOUR_TOKEN" \
  -d '{
    "characterName": "测试角色",
    "profession": 1
  }'
```

**响应示例：**
```json
{
  "code": 200,
  "message": "角色创建成功",
  "data": {
    "id": "uuid",
    "characterName": "测试角色",
    "profession": 1,
    "level": 1,
    "experience": 0,
    "strength": 10,
    "intelligence": 3,
    "agility": 5,
    "hp": 180,
    "maxHp": 180,
    // ...
  }
}
```

#### 2. 增加经验（不升级）
```bash
curl -X POST "http://localhost:8080/webgame/character/add-experience" \
  -H "Content-Type: application/json" \
  -d '{
    "characterId": "角色ID",
    "expToAdd": 50
  }'
```

**响应示例：**
```json
{
  "code": 200,
  "message": "经验增加成功",
  "data": {
    "character": {
      "level": 1,
      "experience": 50,
      // ...
    },
    "levelUp": null
  }
}
```

#### 3. 增加经验（触发升级）
```bash
curl -X POST "http://localhost:8080/webgame/character/add-experience" \
  -H "Content-Type: application/json" \
  -d '{
    "characterId": "角色ID",
    "expToAdd": 200
  }'
```

**响应示例：**
```json
{
  "code": 200,
  "message": "恭喜升级！获得 3 点属性点",
  "data": {
    "character": {
      "level": 2,
      "experience": 150,
      "availablePoints": 3,
      // ...
    },
    "levelUp": {
      "oldLevel": 1,
      "newLevel": 2,
      "levelsGained": 1,
      "pointsGained": 3,
      "overflowExp": 150,
      "isNewMaxLevel": false
    }
  }
}
```

#### 4. 属性加点
```bash
curl -X POST "http://localhost:8080/webgame/character/update-attributes" \
  -H "Content-Type: application/json" \
  -d '{
    "characterId": "角色ID",
    "str": 2,
    "intelligence": 1,
    "agi": 0
  }'
```

**响应示例：**
```json
{
  "code": 200,
  "message": "属性加点成功",
  "data": {
    "strength": 12,
    "intelligence": 4,
    "agility": 5,
    "availablePoints": 0,
    "maxHp": 190,
    "physicalAttack": 27,
    // ...
  }
}
```

#### 5. 获取角色详情
```bash
curl -X GET "http://localhost:8080/webgame/character/info/角色ID" \
  -H "X-Access-Token: YOUR_TOKEN"
```

---

## 📊 经验等级对照表（前20级）

| 等级 | 升级所需经验 | 累计经验 |
|:---:|:---:|:---:|
| 1 | 100 | 0 |
| 2 | 115 | 100 |
| 3 | 132 | 215 |
| 4 | 152 | 347 |
| 5 | 175 | 499 |
| 6 | 201 | 674 |
| 7 | 231 | 875 |
| 8 | 265 | 1106 |
| 9 | 303 | 1371 |
| 10 | 346 | 1674 |
| 11 | 403 | 2020 |
| 12 | 459 | 2423 |
| 13 | 522 | 2882 |
| 14 | 593 | 3404 |
| 15 | 671 | 3997 |
| 16 | 759 | 4668 |
| 17 | 857 | 5427 |
| 18 | 965 | 6284 |
| 19 | 1085 | 7249 |
| 20 | 1218 | 8334 |

---

## 🔍 验证公式

### 经验公式验证

```java
// 测试代码
for (int i = 1; i <= 20; i++) {
    int exp = ExperienceCalculator.calculateNextLevelExp(i);
    System.out.println("等级 " + i + ": " + exp);
}
```

**预期输出：**
```
等级 1: 100
等级 2: 115
等级 3: 132
...
```

### 属性计算验证

以战士（profession=1）为例，初始属性：力量10、智力3、敏捷5

**预期结果：**
```
HP = floor((80 + 10×5) × 1.2) = floor(130 × 1.2) = 156
MP = floor((20 + 3×5) × 0.8) = floor(35 × 0.8) = 28
物攻 = floor((10×2 + 5×0.5) × 1.15) = floor(22.5 × 1.15) = 25
魔攻 = floor(3×2 × 0.8) = floor(6 × 0.8) = 4
防御 = 5 + 3 = 8
闪避率 = 5 × 0.005 = 0.025
暴击率 = 5 × 0.004 = 0.02
```

---

## ⚠️ 常见问题

### Q1: 接口返回"用户不存在"？

**解决方案：**
1. 检查 ShiroConfig 是否配置了 `/webgame/character/**` 为 `anon`
2. 清理 Redis 缓存
3. 重启应用

### Q2: Redis 序列化异常？

**解决方案：**
```bash
redis-cli KEYS "shiro:*" | xargs redis-cli DEL
```

### Q3: 升级后属性点没有增加？

**检查：**
1. 确认 `POINTS_PER_LEVEL = 3`
2. 查看日志中是否有升级信息
3. 检查 `calculateAvailablePointsAfterLevelUp` 方法是否正确计算

### Q4: 属性加点后衍生属性没有变化？

**检查：**
1. 确认调用了 `recalculateDerivedStats(character)`
2. 检查属性计算公式是否正确
3. 验证职业加成系数是否匹配

---

## 📝 下一步

1. **实现装备系统** - 添加装备加成到属性计算
2. **实现战宠系统** - 添加战宠加成到属性计算
3. **实现战斗系统** - 使用这些属性进行战斗计算
4. **添加更多经验来源** - 任务、副本、活动等

---

*文档版本：2026-05-08*  
*适用项目：网页小游戏后端*
