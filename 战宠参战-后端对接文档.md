# 战宠参战系统 - 后端对接文档

## 概述

战宠作为独立战斗单位参与回合制战斗，拥有独立的 HP 条、MP 条和行动条。战宠由 AI 自动控制，优先使用高威力技能。战宠死亡时主人失去属性加成，战斗结束后战宠 HP/MP 自动恢复满。

---

## 数据结构扩展

### 1. Combatant 新增字段

```typescript
interface Combatant {
  // ... 原有字段 ...

  /** 主人 uid（仅 type=pet 时有值，链接到所属玩家） */
  masterUid?: string

  /** 战宠给予主人的属性加成（仅 type=pet 时有值） */
  petBonusToMaster?: PetBonusToMaster
}

/** 战宠给予主人的属性加成 */
interface PetBonusToMaster {
  maxHp: number          // HP 加成值
  physicalAttack: number // 物理攻击加成值
  magicAttack: number    // 魔法攻击加成值
  defense: number        // 防御加成值
  dodgeRate: number      // 闪避率加成值
  criticalRate: number   // 暴击率加成值
}
```

### 2. StartBattleRequest 扩展

```typescript
interface StartBattleRequest {
  characterId: string
  dungeonId?: string
  stageId?: string
  enemyGroupId?: string
  /** 出战战宠 ID（可选，无出战战宠时不传） */
  activePetId?: string
}
```

### 3. StartBattleResponse 扩展

```typescript
interface StartBattleResponse {
  battleId: string
  enemies: Combatant[]
  /** 战宠战斗数据（有出战战宠时返回） */
  pet?: Combatant
}
```

### 4. BattleEndResponse 扩展

```typescript
interface BattleEndResponse {
  outcome: BattleOutcome
  rewards: BattleRewards
  statistics?: BattleStatistics
  /** 战宠 HP/MP 已自动恢复满（后端标记，前端据此更新战宠状态） */
  petHpRestored?: boolean
}
```

---

## 战宠战斗行为规则

### 行动值 (ATB) 系统

战宠与角色、怪物共享同一行动条系统：

- 每单位每 tick 累积 `actionValue += speed`
- 当 `actionValue >= 1000` 时该单位可行动
- 战宠拥有独立的 `speed` 属性，速度越高行动越频繁
- 行动顺序条 UI 展示所有单位的预计行动顺序

### 战宠 AI 决策逻辑

```
1. 检查主人 HP 比例
   ├─ 主人 HP < 50%
   │   ├─ 有可用治疗/辅助技能 → 使用治疗/辅助技能（目标：主人）
   │   └─ 无可用辅助技能 → 继续 Step 2
   └─ 主人 HP >= 50% → 继续 Step 2

2. 获取所有可用攻击技能（非被动、MP 足够、不在冷却中）
   ├─ 按威力(power)降序排列
   ├─ 有可用攻击技能 → 使用威力最高的技能
   └─ 无可用攻击技能 → 继续 Step 3

3. 使用辅助/治疗技能（如有可用）

4. 兜底：普通攻击
```

**后端要求**：
- 战宠 AI 决策由后端执行（服务器权威架构）
- 前端仅负责发送 "战宠行动" 请求，后端返回具体行动结果
- 或由前端在 Mock 模式下本地计算（开发阶段）

### 技能使用判断条件

| 条件 | 检查逻辑 |
|:---|:---|
| 技能可用 | `type !== 'passive'` |
| MP 足够 | `stats.mp >= skill.mpCost` |
| 不在冷却中 | `cooldowns[skill.id] <= 0` |
| 威力排序 | `skill.power` 降序 |

---

## 战宠死亡处理

### 触发条件

当战宠 HP <= 0 时触发死亡处理：

1. 设置 `isAlive = false`
2. 查找战宠的主人（通过 `masterUid`）
3. 从主人属性中扣除战宠提供的加成值：

```
master.maxHp -= petBonus.maxHp
master.hp = min(master.hp, master.maxHp)  // 当前 HP 不超过新的 maxHp
master.physicalAttack -= petBonus.physicalAttack
master.magicAttack -= petBonus.magicAttack
master.defense -= petBonus.defense
master.dodgeRate -= petBonus.dodgeRate
master.criticalRate -= petBonus.criticalRate
// 所有属性不低于 0（maxHp 不低于 1）
```

4. 添加战斗日志：`"{战宠名} 阵亡，{主人名} 失去了战宠属性加成！"`

### 后端注意事项

- 战宠死亡不影响主人 HP（不会直接造成主人扣血）
- 属性扣减是永久性的（持续到战斗结束）
- 即使后续有复活技能复活战宠，主人的属性加成不会自动恢复（需单独处理）
- 主人因属性扣减导致 maxHp 降低后，若当前 HP > 新 maxHp，则截断到新 maxHp

---

## 战后 HP 恢复

### 规则

- 战斗结束后，所有参战战宠 HP 和 MP 自动恢复满
- 无论战宠是否在战斗中死亡，HP 都恢复到 maxHp
- 后端在 `BattleEndResponse` 中标记 `petHpRestored: true`
- 前端据此更新本地战宠状态缓存

### 后端实现建议

```sql
-- 战斗结束后更新战宠状态
UPDATE character_pets
SET hp = max_hp, mp = max_mp
WHERE character_id = ? AND is_active = true;
```

---

## 战宠属性计算公式（进入战斗时）

```
petHp = (baseHp + level × hpGrowth) × rarityMultiplier + equipHp
petAttack = (baseAttack + level × attackGrowth) × rarityMultiplier + equipAttack
petDefense = (baseDefense + level × defenseGrowth) × rarityMultiplier + equipDefense
petSpeed = (baseSpeed + level × speedGrowth) × rarityMultiplier + equipSpeed
```

**品质倍率**：N=1.0, R=1.2, SR=1.5, SSR=2.0

**战宠给主人的加成计算**：

```
bonusHp = floor(petHp × 0.1 × rarityMultiplier)
bonusAttack = floor(petAttack × 0.1 × rarityMultiplier)
bonusDefense = floor(petDefense × 0.1 × rarityMultiplier)
```

---

## 接口变更汇总

| 接口 | 变更 | 说明 |
|:---|:---|:---|
| `POST /battle/start` | Request 新增 `activePetId` | 传入出战战宠 ID |
| `POST /battle/start` | Response 新增 `pet` | 返回战宠战斗单位数据 |
| `POST /battle/action` | 无变更 | 战宠 AI 行动由后端自动处理 |
| `POST /battle/end/{id}` | Response 新增 `petHpRestored` | 标记战宠 HP 已恢复 |

---

## 前端实现文件

| 文件 | 职责 |
|:---|:---|
| `src/utils/battleEngine.ts` | 战宠 AI 决策、死亡处理、HP 恢复 |
| `src/api/battle.ts` | Mock 战宠数据、createPetCombatant 构造函数 |
| `src/stores/battle.ts` | 战宠创建、战后 HP 恢复 |
| `src/components/battle/CombatantBar.vue` | 战宠 HP/MP 条、行动动画（已有） |
| `src/components/battle/ActionOrderBar.vue` | 行动顺序条战宠展示（已有） |
| `src/views/BattleView.vue` | 战场布局含战宠（已有） |

---

## 战宠战斗时序图

```
战斗开始
   │
   ├─→ 前端传入 characterId + activePetId
   │
   ├─→ 后端计算角色属性（含战宠加成）
   │      ├─ 角色总属性 = 基础 + 装备 + 战宠加成
   │      └─ 创建角色 Combatant + 战宠 Combatant
   │
   ├─→ 返回 BattleState（含角色+战宠+敌人）
   │
   ▼ ATB 行动循环
   │
   ├─→ 推进行动值（所有单位共享 ATB）
   │      ├─ 战宠 speed 独立计算
   │      └─ 行动值满的单位获得行动权
   │
   ├─→ 战宠获得行动权
   │      ├─ 战宠 AI 自动决策（前端 Mock / 后端计算）
   │      ├─ 优先高威力技能 → 冷却中顺延 → 兜底普攻
   │      ├─ 主人 HP < 50% 时优先辅助/治疗
   │      └─ 执行技能/攻击 → 伤害计算 → 动画播放
   │
   ├─→ 战宠死亡
   │      ├─ 设置 pet.isAlive = false
   │      ├─ 扣减主人属性（petBonusToMaster 各项取反）
   │      ├─ 添加日志："XX 阵亡，YY 失去了战宠属性加成"
   │      └─ 主人继续战斗（无属性加成）
   │
   ▼ 战斗结束
   │
   ├─→ 战宠 HP/MP 自动恢复满
   ├─→ 返回奖励（含 petExp）
   └─→ 前端更新战宠状态
```

---

*文档版本: 1.0.0*
*最后更新: 2026-05-07*
