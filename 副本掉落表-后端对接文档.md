# 副本掉落表 - 后端对接文档

## 概述

本文档描述副本掉落表系统的前后端对接规范。掉落表为每个副本定义专属的物品掉落池，区分普通楼层和 Boss 楼层，支持装备、材料、战宠蛋三类掉落。

---

## 前端实现概览

| 文件 | 作用 |
|:---|:---|
| `src/types/dungeon.ts` | `DungeonDropEntry`、`DungeonDropTable` 类型定义 |
| `src/config/dungeon_drop_config.ts` | 8 个副本的掉落表静态配置 |
| `src/utils/dungeonDrops.ts` | `calculateDungeonDrops()` 掉落解析函数 |
| `src/stores/dungeon.ts` | `handleFloorComplete()` 使用掉落表生成奖励 |
| `src/components/dungeon/DungeonPanel.vue` | 掉落预览 UI |
| `src/components/dungeon/DungeonFloorResultOverlay.vue` | 宠物蛋特殊展示 |

---

## 数据结构

### DungeonDropEntry（掉落条目）

```typescript
interface DungeonDropEntry {
  itemId: number        // 物品 ID
  name: string          // 物品名称
  itemType: 'equipment' | 'material' | 'pet_egg'  // 物品类型
  quality: 'common' | 'rare' | 'epic' | 'legendary'  // 品质
  dropRate: number      // 基础掉落率 (0.0-1.0)
  minQuantity: number   // 最小数量
  maxQuantity: number   // 最大数量
}
```

### DungeonDropTable（副本掉落表）

```typescript
interface DungeonDropTable {
  floorDrops: DungeonDropEntry[]  // 普通/精英楼层掉落池
  bossDrops: DungeonDropEntry[]   // Boss 楼层掉落池
  dropRateMultiplier: number      // 掉落率倍率（普通=1.0, 精英=1.5）
}
```

---

## 物品 ID 映射

### 材料类（2001-2005）

| ID | 名称 | 品质 |
|:---:|:---|:---:|
| 2001 | 铁矿石 | common |
| 2002 | 精钢矿石 | rare |
| 2003 | 秘法水晶 | epic |
| 2004 | 龙鳞碎片 | legendary |
| 2005 | 战宠进化石 | epic |

### 装备类（3001-3005）

| ID | 名称 | 品质 | 槽位 |
|:---:|:---|:---:|:---|
| 3001 | 烈焰之刃 | epic | weapon |
| 3002 | 秘银头盔 | rare | helmet |
| 3003 | 守护胸甲 | rare | chest |
| 3004 | 疾风护腿 | common | legs |
| 3005 | 灵巧之戒 | epic | accessory1 |

### 战宠蛋类（6001-6008）

| ID | 名称 | 品质 | 对应战宠类型 |
|:---:|:---|:---:|:---:|
| 6001 | 火焰精灵蛋 | rare | 1001（火焰精灵） |
| 6002 | 风狼蛋 | rare | 1002（风狼） |
| 6003 | 冰晶凤凰蛋 | epic | 1003（冰晶凤凰） |
| 6004 | 土岩兽蛋 | common | 1005（土岩兽） |
| 6005 | 光辉精灵蛋 | rare | 1006（光辉精灵） |
| 6006 | 暗影猫蛋 | rare | 1007（暗影猫） |
| 6007 | 水灵龟蛋 | common | 1008（水灵龟） |
| 6008 | 雷鹰蛋 | rare | 1009（雷鹰） |

> **说明**：战宠蛋不包含进化后的宠物类型（1004 烈焰凤凰、1010 风暴雷神），这些需要通过进化获得。

---

## 副本掉落表配置

### 副本 ID 与掉落率倍率

| 副本 ID | 名称 | 难度 | 倍率 |
|:---|:---|:---:|:---:|
| `mist_forest_normal` | 迷雾密林 | 普通 | 1.0 |
| `mist_forest_elite` | 暗影树海 | 精英 | 1.5 |
| `bone_wasteland_normal` | 亡者墓地 | 普通 | 1.0 |
| `bone_wasteland_elite` | 骨龙王座 | 精英 | 1.5 |
| `fire_valley_normal` | 熔岩裂隙 | 普通 | 1.0 |
| `fire_valley_elite` | 炼狱深渊 | 精英 | 1.5 |
| `frost_snowfield_normal` | 冰晶洞窟 | 普通 | 1.0 |
| `frost_snowfield_elite` | 龙眠冰殿 | 精英 | 1.5 |

### 区域战宠蛋分布

| 区域 | 可掉落战宠蛋 |
|:---|:---|
| 迷雾森林 | 风狼蛋(6002)、光辉精灵蛋(6005) |
| 白骨荒野 | 土岩兽蛋(6004)、暗影猫蛋(6006) |
| 火焰山谷 | 火焰精灵蛋(6001)、雷鹰蛋(6008) |
| 冰霜雪原 | 冰晶凤凰蛋(6003)、水灵龟蛋(6007) |

---

## 掉落率设计

### 楼层类型掉落率基准

| 楼层类型 | 材料 | 装备 | 战宠蛋 |
|:---|:---:|:---:|:---:|
| 普通副本普通层 | 15-25% | 5-8% | 0% |
| 精英副本普通层 | 20-35% | 8-15% | 0% |
| 普通副本 Boss 层 | 30-50% | 10-20% | 3-5% |
| 精英副本 Boss 层 | 40-60% | 15-30% | 5-10% |

### 实际掉落率计算公式

```
actualDropRate = min(entry.dropRate * table.dropRateMultiplier, 1.0)
```

- **精英副本**掉落率倍率为 1.5，相当于比普通副本高 50%
- 最终掉落率上限为 100%（1.0）

---

## 后端配置表建议

### drop_tables 表

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `dungeon_id` | VARCHAR(32) PK | 副本 ID |
| `drop_rate_multiplier` | DECIMAL(3,2) | 掉落率倍率 |

### drop_entries 表

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| `id` | SERIAL PK | 自增主键 |
| `dungeon_id` | VARCHAR(32) FK | 所属副本 |
| `floor_type` | ENUM('floor', 'boss') | 楼层类型 |
| `item_id` | INT FK | 物品 ID（关联 items 表） |
| `drop_rate` | DECIMAL(4,3) | 基础掉落率 |
| `min_quantity` | INT | 最小数量 |
| `max_quantity` | INT | 最大数量 |

---

## 掉落解析流程

1. **前端调用**：`handleFloorComplete()` 时调用 `calculateDungeonDrops(dungeonId, isBossFloor)`
2. **查找掉落表**：根据 `dungeonId` 获取 `DungeonDropTable`
3. **选择掉落池**：`isBossFloor ? bossDrops : floorDrops`
4. **掷骰判定**：对每个条目，`Math.random() < dropRate * multiplier` 则掉落
5. **数量随机**：`quantity = min + Math.floor(Math.random() * (max - min + 1))`
6. **返回结果**：`BattleRewardItem[]` 数组

### Mock 前端实现（dungeonDrops.ts）

```typescript
export function calculateDungeonDrops(
  dungeonId: string,
  isBossFloor: boolean
): BattleRewardItem[] {
  const table = getDungeonDropTable(dungeonId)
  if (!table) return []

  const pool = isBossFloor ? table.bossDrops : table.floorDrops
  return resolveDrops(pool, table.dropRateMultiplier)
}
```

---

## 与战斗奖励的关系

| 奖励类型 | 计算来源 |
|:---|:---|
| 经验值 | `calculateRewards(enemies)` — 按击杀敌人属性计算 |
| 金币 | `calculateRewards(enemies)` — 按击杀敌人属性计算 |
| 战宠经验 | `calculateRewards(enemies)` — 按经验值的 30% |
| 物品掉落 | `calculateDungeonDrops()` — **副本专属掉落表** |

副本楼层奖励结构：
```typescript
const floorRewards = {
  exp: baseRewards.exp,           // 通用经验计算
  gold: baseRewards.gold,         // 通用金币计算
  items: calculateDungeonDrops(), // 副本掉落表
  petExp: baseRewards.petExp      // 战宠经验
}
```

---

## 前端 UI 展示

### DungeonPanel.vue（副本选择页）

- **掉落预览区域**：每个副本卡片显示"可能掉落"标签
- **合并展示**：普通层 + Boss 层掉落去重后显示
- **样式区分**：
  - 材料：绿色（与品质色叠加）
  - 装备：蓝/紫/金（按品质）
  - 战宠蛋：金色 + 特殊边框

### DungeonFloorResultOverlay.vue（楼层结算弹窗）

- **宠物蛋特殊样式**：金色背景 + 边框 + 蛋图标（🥚）
- **品质着色**：common（灰）、rare（蓝）、epic（紫）、legendary（金）

---

## 接口规范

当前为纯前端 Mock 实现，后端对接时需要：

### GET `/dungeon/drop-table/{dungeonId}`

获取指定副本的掉落表配置。

**响应示例**：
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "dungeonId": "mist_forest_elite",
    "dropRateMultiplier": 1.5,
    "floorDrops": [
      { "itemId": 2001, "dropRate": 0.30, "minQuantity": 2, "maxQuantity": 4 }
    ],
    "bossDrops": [
      { "itemId": 6002, "dropRate": 0.05, "minQuantity": 1, "maxQuantity": 1 }
    ]
  }
}
```

### POST `/dungeon/resolve-drops`（可选）

后端权威掉落判定接口。

**请求参数**：
```json
{
  "dungeonId": "mist_forest_elite",
  "floorNumber": 3,
  "isBossFloor": true,
  "characterId": "uuid-xxx"
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "掉落解析成功",
  "data": {
    "items": [
      { "itemId": 2003, "name": "秘法水晶", "quantity": 2, "quality": "epic", "itemType": "material" }
    ]
  }
}
```

---

## 战宠蛋使用流程（后续实现）

战宠蛋作为特殊材料类物品存入背包，使用时：

1. 前端调用 `/pet/hatch-egg` 接口
2. 后端验证背包中有该蛋
3. 根据蛋的 `itemId` 确定战宠类型（6001→1001）
4. 创建战宠实例，扣除蛋

> **当前实现**：战宠蛋仅存在于掉落表和楼层结算展示，暂未实现孵化流程。

---

## 测试要点

1. **掉落表配置**：8 个副本各有独立的掉落池
2. **楼层类型区分**：普通层不掉落战宠蛋，Boss 层可掉落
3. **倍率生效**：精英副本掉落率 = base × 1.5
4. **区域对应**：火焰山谷副本掉落火焰精灵蛋/雷鹰蛋，冰霜雪原副本掉落冰晶凤凰蛋/水灵龟蛋
5. **UI 展示**：宠物蛋显示金色边框和蛋图标

---

## 版本记录

| 版本 | 日期 | 说明 |
|:---:|:---:|:---|
| v1.0 | 2026-05-09 | 初始版本，纯前端 Mock 实现 |