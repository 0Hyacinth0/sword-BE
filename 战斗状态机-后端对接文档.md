# 战斗状态机 - 后端对接文档

## 概述

本文档定义回合制战斗状态机的后端 API 接口规范。战斗系统采用 **前端优先计算 + 后端校验** 的混合架构：

- **前端**：负责状态机流转、伤害计算、闪避/暴击判定（保证流畅体验）
- **后端**：负责战斗合法性校验、数据持久化、奖励发放、防作弊验证

---

## 状态机流程

```
回合开始(ROUND_START)
  → Buff/Debuff结算(BUFF_SETTLEMENT)
  → 行动选择(ACTION_SELECT)：玩家手动 / AI自动
  → 伤害计算(DAMAGE_CALCULATION)
  → 闪避/暴击判定(DODGE_CRIT_CHECK)
  → 结算(SETTLEMENT)
  → 回合结束(ROUND_END) → 循环回回合开始
  → 战斗结束(BATTLE_END)
```

---

## API 接口

### 1. 发起战斗

**POST** `/battle/start`

发起一场新战斗，后端创建战斗实例并返回敌人数据。

#### 请求体

```json
{
  "characterId": "string",       // 角色 ID
  "dungeonId": "string",        // 副本 ID（可选，副本战斗时传）
  "stageId": "string",          // 关卡 ID（可选）
  "enemyGroupId": "string"      // 怪物组 ID（可选，野外遭遇时传）
}
```

#### 响应体

```json
{
  "code": 200,
  "message": "战斗开始",
  "data": {
    "battleId": "string",        // 战斗实例唯一 ID
    "enemies": [                 // 敌方单位列表
      {
        "uid": "string",         // 战斗内唯一 ID（后端生成）
        "sourceId": "string",    // 怪物模板 ID
        "name": "string",        // 怪物名称
        "side": "enemy",         // 固定为 "enemy"
        "type": "enemy",         // 固定为 "enemy"
        "stats": {
          "maxHp": 120,
          "hp": 120,
          "maxMp": 30,
          "mp": 30,
          "physicalAttack": 18,
          "magicAttack": 5,
          "defense": 8,
          "speed": 15,
          "dodgeRate": 0.08,
          "criticalRate": 0.05
        },
        "skills": [              // 怪物技能列表
          {
            "id": 9001,
            "name": "撕咬",
            "type": "active_attack",
            "power": 120,
            "cooldown": 0,
            "mpCost": 0,
            "targetType": "single_enemy",
            "description": "用利齿撕咬目标"
          }
        ],
        "buffs": [],             // 初始 Buff
        "cooldowns": {},         // 初始冷却
        "isAlive": true
      }
    ]
  }
}
```

#### 后端逻辑

1. 根据 `dungeonId`/`stageId`/`enemyGroupId` 确定怪物组合
2. 从怪物配置表生成怪物实例（计算属性、技能）
3. 创建战斗记录到数据库（状态：进行中）
4. 返回战斗 ID 和敌人数据

---

### 2. 提交玩家行动

**POST** `/battle/action`

玩家每回合提交一次行动。后端校验合法性并同步状态。

#### 请求体

```json
{
  "battleId": "string",
  "action": {
    "type": "attack|skill|item|defend|flee",
    "actorUid": "string",        // 行动者 UID
    "targetUid": "string",       // 目标 UID（单体攻击/技能）
    "skillId": 5001,             // 技能 ID（type=skill 时）
    "itemId": "string"           // 物品 ID（type=item 时）
  }
}
```

#### 响应体

```json
{
  "code": 200,
  "message": "行动执行成功",
  "data": {
    "battleState": {             // 完整战斗状态快照
      "phase": "SETTLEMENT",
      "round": 3,
      "combatants": [...],
      "actionOrder": [...],
      "currentActorIndex": 1,
      "waitingForPlayerAction": false,
      "log": [...],
      "outcome": null,
      "rewards": null,
      "lastDamageResults": [...],
      "lastBuffResults": [...]
    }
  }
}
```

#### 后端逻辑

1. 验证 `battleId` 有效且属于该玩家
2. 验证行动合法性（行动者存活、目标存活、MP 足够、技能未冷却）
3. 执行与前端相同的状态机逻辑（后端为权威端）
4. 返回最新战斗状态

#### 校验要点

| 校验项 | 说明 |
|--------|------|
| 行动者合法性 | `actorUid` 对应单位是否存活、是否轮到其行动 |
| 目标合法性 | `targetUid` 对应单位是否存活、是否为敌方 |
| MP 检查 | 技能 MP 消耗是否足够 |
| 冷却检查 | 技能冷却是否已就绪 |
| 物品检查 | 物品是否存在且可用 |

---

### 3. 结束战斗

**POST** `/battle/end/{battleId}`

战斗结束后领取奖励。后端计算并发放经验、金币、掉落物品。

#### 路径参数

| 参数 | 说明 |
|------|------|
| `battleId` | 战斗实例 ID |

#### 响应体

```json
{
  "code": 200,
  "message": "战斗胜利！",
  "data": {
    "outcome": "victory|defeat|fled",
    "rewards": {
      "exp": 150,
      "gold": 35,
      "items": [
        {
          "itemId": 2001,
          "name": "小回复药水",
          "quantity": 2
        }
      ]
    }
  }
}
```

#### 后端逻辑

1. 确认战斗已结束（所有敌方或友方全灭，或逃跑成功）
2. 根据战斗结果计算奖励：
   - **经验**：基于敌人等级和属性
   - **金币**：基于敌人等级
   - **掉落物品**：根据掉落表概率计算
3. 发放奖励到角色（更新经验、金币、背包）
4. 更新战斗记录状态为"已完成"
5. 检查是否触发升级

---

## 核心公式（前后端一致）

### 伤害计算

```
基础伤害 = 攻击者物理攻击 × 技能倍率 - 防御者防御 × 0.5
最终伤害 = max(1, floor(基础伤害 × (1 + random(-0.1, 0.1))))
```

### 暴击

```
暴击率 = min(攻击者criticalRate, 0.8)
暴击判定: random() < 暴击率
暴击伤害 = 最终伤害 × 1.5
```

### 闪避

```
闪避率 = min(防御者dodgeRate, 0.8)
闪避判定: random() < 闪避率
闪避成功: 伤害 = 0
```

### Buff/Debuff 效果

```
实际属性 = 基础属性 + Σ(Buff值) - Σ(Debuff值)
DoT伤害 = max(1, floor(目标maxHp × 0.1))  // 每回合
```

### 逃跑概率

```
逃跑成功率 = min(0.3 + 攻击者speed × 0.01, 0.8)
```

### 出手顺序

```
按速度(speed)降序排列
```

---

## 数据结构定义

### CombatantStats

| 字段 | 类型 | 说明 |
|------|------|------|
| maxHp | number | 最大生命值 |
| hp | number | 当前生命值 |
| maxMp | number | 最大魔法值 |
| mp | number | 当前魔法值 |
| physicalAttack | number | 物理攻击 |
| magicAttack | number | 魔法攻击 |
| defense | number | 防御力 |
| speed | number | 速度 |
| dodgeRate | number | 闪避率(0~1) |
| criticalRate | number | 暴击率(0~1) |

### BuffTemplate

| 字段 | 类型 | 说明 |
|------|------|------|
| name | string | Buff 名称 |
| isDebuff | boolean | 是否为 Debuff |
| stat | string | 影响的属性 |
| value | number | 效果值(正数增加，负数减少) |
| duration | number | 持续回合数 |

### BattleSkill

| 字段 | 类型 | 说明 |
|------|------|------|
| id | number | 技能 ID |
| name | string | 技能名称 |
| type | string | 技能类型(active_attack/active_heal/active_buff/passive) |
| power | number? | 威力倍率 |
| cooldown | number? | 冷却回合数 |
| mpCost | number? | MP 消耗 |
| targetType | string? | 目标类型 |
| attachedBuff | BuffTemplate? | 附带 Buff |
| description | string? | 技能描述 |

---

## 错误码

| code | 说明 |
|------|------|
| 200 | 成功 |
| 400 | 参数错误（行动不合法） |
| 404 | 战斗不存在 |
| 409 | 战斗已结束 |
| 1001 | MP 不足 |
| 1002 | 技能冷却中 |
| 1003 | 目标已死亡 |
| 1004 | 非玩家回合 |

---

## 注意事项

1. **前端优先计算**：前端本地执行状态机以保证流畅体验，同时异步提交行动给后端
2. **后端为权威**：后端独立计算战斗结果，若前后端不一致以 backend 为准
3. **断线重连**：战斗状态存储在数据库，玩家重连后可通过 `GET /battle/state/{battleId}` 恢复
4. **战斗超时**：若玩家超过 5 分钟未行动，自动判负
5. **并发控制**：同一战斗同时只允许一个行动提交，需做幂等处理
